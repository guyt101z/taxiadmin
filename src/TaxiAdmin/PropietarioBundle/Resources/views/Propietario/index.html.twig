{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Propietario list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Idusuario</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('propietario_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.idUsuario }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('propietario_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('propietario_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('propietario_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}

<?php

/**
 * Base class that represents a row from the 'movil' table.
 *
 * 
 *
 * This class was autogenerated by Propel 1.4.2 on:
 *
 * Sun May 26 12:23:23 2013
 *
 * @package    lib.model.om
 */
abstract class BaseMovil extends BaseObject  implements Persistent {


	/**
	 * The Peer class.
	 * Instance provides a convenient way of calling static methods on a class
	 * that calling code may not be able to identify.
	 * @var        MovilPeer
	 */
	protected static $peer;

	/**
	 * The value for the id field.
	 * @var        int
	 */
	protected $id;

	/**
	 * The value for the matricula field.
	 * @var        string
	 */
	protected $matricula;

	/**
	 * The value for the marca field.
	 * @var        string
	 */
	protected $marca;

	/**
	 * The value for the modelo field.
	 * @var        string
	 */
	protected $modelo;

	/**
	 * The value for the anio field.
	 * @var        int
	 */
	protected $anio;

	/**
	 * The value for the numerochasis field.
	 * @var        string
	 */
	protected $numerochasis;

	/**
	 * The value for the combustible field.
	 * @var        string
	 */
	protected $combustible;

	/**
	 * The value for the numeromovil field.
	 * @var        int
	 */
	protected $numeromovil;

	/**
	 * The value for the iddespacho field.
	 * @var        int
	 */
	protected $iddespacho;

	/**
	 * The value for the kminiciales field.
	 * @var        int
	 */
	protected $kminiciales;

	/**
	 * The value for the idaseguradora field.
	 * @var        int
	 */
	protected $idaseguradora;

	/**
	 * The value for the fechaalta field.
	 * @var        string
	 */
	protected $fechaalta;

	/**
	 * The value for the fechabaja field.
	 * @var        string
	 */
	protected $fechabaja;

	/**
	 * The value for the habilitado field.
	 * Note: this column has a database default value of: true
	 * @var        boolean
	 */
	protected $habilitado;

	/**
	 * The value for the usuario field.
	 * @var        int
	 */
	protected $usuario;

	/**
	 * @var        Despacho
	 */
	protected $aDespacho;

	/**
	 * @var        Aseguradora
	 */
	protected $aAseguradora;

	/**
	 * @var        Usuario
	 */
	protected $aUsuarioRelatedByUsuario;

	/**
	 * @var        array Multa[] Collection to store aggregation of Multa objects.
	 */
	protected $collMultas;

	/**
	 * @var        Criteria The criteria used to select the current contents of collMultas.
	 */
	private $lastMultaCriteria = null;

	/**
	 * @var        array Accidente[] Collection to store aggregation of Accidente objects.
	 */
	protected $collAccidentes;

	/**
	 * @var        Criteria The criteria used to select the current contents of collAccidentes.
	 */
	private $lastAccidenteCriteria = null;

	/**
	 * @var        array Recaudacion[] Collection to store aggregation of Recaudacion objects.
	 */
	protected $collRecaudacions;

	/**
	 * @var        Criteria The criteria used to select the current contents of collRecaudacions.
	 */
	private $lastRecaudacionCriteria = null;

	/**
	 * @var        array Gastomovil[] Collection to store aggregation of Gastomovil objects.
	 */
	protected $collGastomovils;

	/**
	 * @var        Criteria The criteria used to select the current contents of collGastomovils.
	 */
	private $lastGastomovilCriteria = null;

	/**
	 * @var        array Pagoaseguradora[] Collection to store aggregation of Pagoaseguradora objects.
	 */
	protected $collPagoaseguradoras;

	/**
	 * @var        Criteria The criteria used to select the current contents of collPagoaseguradoras.
	 */
	private $lastPagoaseguradoraCriteria = null;

	/**
	 * @var        array Trabajotaller[] Collection to store aggregation of Trabajotaller objects.
	 */
	protected $collTrabajotallers;

	/**
	 * @var        Criteria The criteria used to select the current contents of collTrabajotallers.
	 */
	private $lastTrabajotallerCriteria = null;

	/**
	 * @var        MovilAseguradora one-to-one related MovilAseguradora object
	 */
	protected $singleMovilAseguradora;

	/**
	 * @var        array MovilDespacho[] Collection to store aggregation of MovilDespacho objects.
	 */
	protected $collMovilDespachos;

	/**
	 * @var        Criteria The criteria used to select the current contents of collMovilDespachos.
	 */
	private $lastMovilDespachoCriteria = null;

	/**
	 * @var        array MovilEmpresa[] Collection to store aggregation of MovilEmpresa objects.
	 */
	protected $collMovilEmpresas;

	/**
	 * @var        Criteria The criteria used to select the current contents of collMovilEmpresas.
	 */
	private $lastMovilEmpresaCriteria = null;

	/**
	 * Flag to prevent endless save loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var        boolean
	 */
	protected $alreadyInSave = false;

	/**
	 * Flag to prevent endless validation loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var        boolean
	 */
	protected $alreadyInValidation = false;

	// symfony behavior
	
	const PEER = 'MovilPeer';

	/**
	 * Applies default values to this object.
	 * This method should be called from the object's constructor (or
	 * equivalent initialization method).
	 * @see        __construct()
	 */
	public function applyDefaultValues()
	{
		$this->habilitado = true;
	}

	/**
	 * Initializes internal state of BaseMovil object.
	 * @see        applyDefaults()
	 */
	public function __construct()
	{
		parent::__construct();
		$this->applyDefaultValues();
	}

	/**
	 * Get the [id] column value.
	 * 
	 * @return     int
	 */
	public function getId()
	{
		return $this->id;
	}

	/**
	 * Get the [matricula] column value.
	 * 
	 * @return     string
	 */
	public function getMatricula()
	{
		return $this->matricula;
	}

	/**
	 * Get the [marca] column value.
	 * 
	 * @return     string
	 */
	public function getMarca()
	{
		return $this->marca;
	}

	/**
	 * Get the [modelo] column value.
	 * 
	 * @return     string
	 */
	public function getModelo()
	{
		return $this->modelo;
	}

	/**
	 * Get the [anio] column value.
	 * 
	 * @return     int
	 */
	public function getAnio()
	{
		return $this->anio;
	}

	/**
	 * Get the [numerochasis] column value.
	 * 
	 * @return     string
	 */
	public function getNumerochasis()
	{
		return $this->numerochasis;
	}

	/**
	 * Get the [combustible] column value.
	 * 
	 * @return     string
	 */
	public function getCombustible()
	{
		return $this->combustible;
	}

	/**
	 * Get the [numeromovil] column value.
	 * 
	 * @return     int
	 */
	public function getNumeromovil()
	{
		return $this->numeromovil;
	}

	/**
	 * Get the [iddespacho] column value.
	 * 
	 * @return     int
	 */
	public function getIddespacho()
	{
		return $this->iddespacho;
	}

	/**
	 * Get the [kminiciales] column value.
	 * 
	 * @return     int
	 */
	public function getKminiciales()
	{
		return $this->kminiciales;
	}

	/**
	 * Get the [idaseguradora] column value.
	 * 
	 * @return     int
	 */
	public function getIdaseguradora()
	{
		return $this->idaseguradora;
	}

	/**
	 * Get the [optionally formatted] temporal [fechaalta] column value.
	 * 
	 *
	 * @param      string $format The date/time format string (either date()-style or strftime()-style).
	 *							If format is NULL, then the raw DateTime object will be returned.
	 * @return     mixed Formatted date/time value as string or DateTime object (if format is NULL), NULL if column is NULL, and 0 if column value is 0000-00-00 00:00:00
	 * @throws     PropelException - if unable to parse/validate the date/time value.
	 */
	public function getFechaalta($format = 'Y-m-d H:i:s')
	{
		if ($this->fechaalta === null) {
			return null;
		}


		if ($this->fechaalta === '0000-00-00 00:00:00') {
			// while technically this is not a default value of NULL,
			// this seems to be closest in meaning.
			return null;
		} else {
			try {
				$dt = new DateTime($this->fechaalta);
			} catch (Exception $x) {
				throw new PropelException("Internally stored date/time/timestamp value could not be converted to DateTime: " . var_export($this->fechaalta, true), $x);
			}
		}

		if ($format === null) {
			// Because propel.useDateTimeClass is TRUE, we return a DateTime object.
			return $dt;
		} elseif (strpos($format, '%') !== false) {
			return strftime($format, $dt->format('U'));
		} else {
			return $dt->format($format);
		}
	}

	/**
	 * Get the [optionally formatted] temporal [fechabaja] column value.
	 * 
	 *
	 * @param      string $format The date/time format string (either date()-style or strftime()-style).
	 *							If format is NULL, then the raw DateTime object will be returned.
	 * @return     mixed Formatted date/time value as string or DateTime object (if format is NULL), NULL if column is NULL, and 0 if column value is 0000-00-00 00:00:00
	 * @throws     PropelException - if unable to parse/validate the date/time value.
	 */
	public function getFechabaja($format = 'Y-m-d H:i:s')
	{
		if ($this->fechabaja === null) {
			return null;
		}


		if ($this->fechabaja === '0000-00-00 00:00:00') {
			// while technically this is not a default value of NULL,
			// this seems to be closest in meaning.
			return null;
		} else {
			try {
				$dt = new DateTime($this->fechabaja);
			} catch (Exception $x) {
				throw new PropelException("Internally stored date/time/timestamp value could not be converted to DateTime: " . var_export($this->fechabaja, true), $x);
			}
		}

		if ($format === null) {
			// Because propel.useDateTimeClass is TRUE, we return a DateTime object.
			return $dt;
		} elseif (strpos($format, '%') !== false) {
			return strftime($format, $dt->format('U'));
		} else {
			return $dt->format($format);
		}
	}

	/**
	 * Get the [habilitado] column value.
	 * 
	 * @return     boolean
	 */
	public function getHabilitado()
	{
		return $this->habilitado;
	}

	/**
	 * Get the [usuario] column value.
	 * 
	 * @return     int
	 */
	public function getUsuario()
	{
		return $this->usuario;
	}

	/**
	 * Set the value of [id] column.
	 * 
	 * @param      int $v new value
	 * @return     Movil The current object (for fluent API support)
	 */
	public function setId($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->id !== $v) {
			$this->id = $v;
			$this->modifiedColumns[] = MovilPeer::ID;
		}

		return $this;
	} // setId()

	/**
	 * Set the value of [matricula] column.
	 * 
	 * @param      string $v new value
	 * @return     Movil The current object (for fluent API support)
	 */
	public function setMatricula($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->matricula !== $v) {
			$this->matricula = $v;
			$this->modifiedColumns[] = MovilPeer::MATRICULA;
		}

		return $this;
	} // setMatricula()

	/**
	 * Set the value of [marca] column.
	 * 
	 * @param      string $v new value
	 * @return     Movil The current object (for fluent API support)
	 */
	public function setMarca($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->marca !== $v) {
			$this->marca = $v;
			$this->modifiedColumns[] = MovilPeer::MARCA;
		}

		return $this;
	} // setMarca()

	/**
	 * Set the value of [modelo] column.
	 * 
	 * @param      string $v new value
	 * @return     Movil The current object (for fluent API support)
	 */
	public function setModelo($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->modelo !== $v) {
			$this->modelo = $v;
			$this->modifiedColumns[] = MovilPeer::MODELO;
		}

		return $this;
	} // setModelo()

	/**
	 * Set the value of [anio] column.
	 * 
	 * @param      int $v new value
	 * @return     Movil The current object (for fluent API support)
	 */
	public function setAnio($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->anio !== $v) {
			$this->anio = $v;
			$this->modifiedColumns[] = MovilPeer::ANIO;
		}

		return $this;
	} // setAnio()

	/**
	 * Set the value of [numerochasis] column.
	 * 
	 * @param      string $v new value
	 * @return     Movil The current object (for fluent API support)
	 */
	public function setNumerochasis($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->numerochasis !== $v) {
			$this->numerochasis = $v;
			$this->modifiedColumns[] = MovilPeer::NUMEROCHASIS;
		}

		return $this;
	} // setNumerochasis()

	/**
	 * Set the value of [combustible] column.
	 * 
	 * @param      string $v new value
	 * @return     Movil The current object (for fluent API support)
	 */
	public function setCombustible($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->combustible !== $v) {
			$this->combustible = $v;
			$this->modifiedColumns[] = MovilPeer::COMBUSTIBLE;
		}

		return $this;
	} // setCombustible()

	/**
	 * Set the value of [numeromovil] column.
	 * 
	 * @param      int $v new value
	 * @return     Movil The current object (for fluent API support)
	 */
	public function setNumeromovil($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->numeromovil !== $v) {
			$this->numeromovil = $v;
			$this->modifiedColumns[] = MovilPeer::NUMEROMOVIL;
		}

		return $this;
	} // setNumeromovil()

	/**
	 * Set the value of [iddespacho] column.
	 * 
	 * @param      int $v new value
	 * @return     Movil The current object (for fluent API support)
	 */
	public function setIddespacho($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->iddespacho !== $v) {
			$this->iddespacho = $v;
			$this->modifiedColumns[] = MovilPeer::IDDESPACHO;
		}

		if ($this->aDespacho !== null && $this->aDespacho->getId() !== $v) {
			$this->aDespacho = null;
		}

		return $this;
	} // setIddespacho()

	/**
	 * Set the value of [kminiciales] column.
	 * 
	 * @param      int $v new value
	 * @return     Movil The current object (for fluent API support)
	 */
	public function setKminiciales($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->kminiciales !== $v) {
			$this->kminiciales = $v;
			$this->modifiedColumns[] = MovilPeer::KMINICIALES;
		}

		return $this;
	} // setKminiciales()

	/**
	 * Set the value of [idaseguradora] column.
	 * 
	 * @param      int $v new value
	 * @return     Movil The current object (for fluent API support)
	 */
	public function setIdaseguradora($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->idaseguradora !== $v) {
			$this->idaseguradora = $v;
			$this->modifiedColumns[] = MovilPeer::IDASEGURADORA;
		}

		if ($this->aAseguradora !== null && $this->aAseguradora->getId() !== $v) {
			$this->aAseguradora = null;
		}

		return $this;
	} // setIdaseguradora()

	/**
	 * Sets the value of [fechaalta] column to a normalized version of the date/time value specified.
	 * 
	 * @param      mixed $v string, integer (timestamp), or DateTime value.  Empty string will
	 *						be treated as NULL for temporal objects.
	 * @return     Movil The current object (for fluent API support)
	 */
	public function setFechaalta($v)
	{
		// we treat '' as NULL for temporal objects because DateTime('') == DateTime('now')
		// -- which is unexpected, to say the least.
		if ($v === null || $v === '') {
			$dt = null;
		} elseif ($v instanceof DateTime) {
			$dt = $v;
		} else {
			// some string/numeric value passed; we normalize that so that we can
			// validate it.
			try {
				if (is_numeric($v)) { // if it's a unix timestamp
					$dt = new DateTime('@'.$v, new DateTimeZone('UTC'));
					// We have to explicitly specify and then change the time zone because of a
					// DateTime bug: http://bugs.php.net/bug.php?id=43003
					$dt->setTimeZone(new DateTimeZone(date_default_timezone_get()));
				} else {
					$dt = new DateTime($v);
				}
			} catch (Exception $x) {
				throw new PropelException('Error parsing date/time value: ' . var_export($v, true), $x);
			}
		}

		if ( $this->fechaalta !== null || $dt !== null ) {
			// (nested ifs are a little easier to read in this case)

			$currNorm = ($this->fechaalta !== null && $tmpDt = new DateTime($this->fechaalta)) ? $tmpDt->format('Y-m-d H:i:s') : null;
			$newNorm = ($dt !== null) ? $dt->format('Y-m-d H:i:s') : null;

			if ( ($currNorm !== $newNorm) // normalized values don't match 
					)
			{
				$this->fechaalta = ($dt ? $dt->format('Y-m-d H:i:s') : null);
				$this->modifiedColumns[] = MovilPeer::FECHAALTA;
			}
		} // if either are not null

		return $this;
	} // setFechaalta()

	/**
	 * Sets the value of [fechabaja] column to a normalized version of the date/time value specified.
	 * 
	 * @param      mixed $v string, integer (timestamp), or DateTime value.  Empty string will
	 *						be treated as NULL for temporal objects.
	 * @return     Movil The current object (for fluent API support)
	 */
	public function setFechabaja($v)
	{
		// we treat '' as NULL for temporal objects because DateTime('') == DateTime('now')
		// -- which is unexpected, to say the least.
		if ($v === null || $v === '') {
			$dt = null;
		} elseif ($v instanceof DateTime) {
			$dt = $v;
		} else {
			// some string/numeric value passed; we normalize that so that we can
			// validate it.
			try {
				if (is_numeric($v)) { // if it's a unix timestamp
					$dt = new DateTime('@'.$v, new DateTimeZone('UTC'));
					// We have to explicitly specify and then change the time zone because of a
					// DateTime bug: http://bugs.php.net/bug.php?id=43003
					$dt->setTimeZone(new DateTimeZone(date_default_timezone_get()));
				} else {
					$dt = new DateTime($v);
				}
			} catch (Exception $x) {
				throw new PropelException('Error parsing date/time value: ' . var_export($v, true), $x);
			}
		}

		if ( $this->fechabaja !== null || $dt !== null ) {
			// (nested ifs are a little easier to read in this case)

			$currNorm = ($this->fechabaja !== null && $tmpDt = new DateTime($this->fechabaja)) ? $tmpDt->format('Y-m-d H:i:s') : null;
			$newNorm = ($dt !== null) ? $dt->format('Y-m-d H:i:s') : null;

			if ( ($currNorm !== $newNorm) // normalized values don't match 
					)
			{
				$this->fechabaja = ($dt ? $dt->format('Y-m-d H:i:s') : null);
				$this->modifiedColumns[] = MovilPeer::FECHABAJA;
			}
		} // if either are not null

		return $this;
	} // setFechabaja()

	/**
	 * Set the value of [habilitado] column.
	 * 
	 * @param      boolean $v new value
	 * @return     Movil The current object (for fluent API support)
	 */
	public function setHabilitado($v)
	{
		if ($v !== null) {
			$v = (boolean) $v;
		}

		if ($this->habilitado !== $v || $this->isNew()) {
			$this->habilitado = $v;
			$this->modifiedColumns[] = MovilPeer::HABILITADO;
		}

		return $this;
	} // setHabilitado()

	/**
	 * Set the value of [usuario] column.
	 * 
	 * @param      int $v new value
	 * @return     Movil The current object (for fluent API support)
	 */
	public function setUsuario($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->usuario !== $v) {
			$this->usuario = $v;
			$this->modifiedColumns[] = MovilPeer::USUARIO;
		}

		if ($this->aUsuarioRelatedByUsuario !== null && $this->aUsuarioRelatedByUsuario->getId() !== $v) {
			$this->aUsuarioRelatedByUsuario = null;
		}

		return $this;
	} // setUsuario()

	/**
	 * Indicates whether the columns in this object are only set to default values.
	 *
	 * This method can be used in conjunction with isModified() to indicate whether an object is both
	 * modified _and_ has some values set which are non-default.
	 *
	 * @return     boolean Whether the columns in this object are only been set with default values.
	 */
	public function hasOnlyDefaultValues()
	{
			if ($this->habilitado !== true) {
				return false;
			}

		// otherwise, everything was equal, so return TRUE
		return true;
	} // hasOnlyDefaultValues()

	/**
	 * Hydrates (populates) the object variables with values from the database resultset.
	 *
	 * An offset (0-based "start column") is specified so that objects can be hydrated
	 * with a subset of the columns in the resultset rows.  This is needed, for example,
	 * for results of JOIN queries where the resultset row includes columns from two or
	 * more tables.
	 *
	 * @param      array $row The row returned by PDOStatement->fetch(PDO::FETCH_NUM)
	 * @param      int $startcol 0-based offset column which indicates which restultset column to start with.
	 * @param      boolean $rehydrate Whether this object is being re-hydrated from the database.
	 * @return     int next starting column
	 * @throws     PropelException  - Any caught Exception will be rewrapped as a PropelException.
	 */
	public function hydrate($row, $startcol = 0, $rehydrate = false)
	{
		try {

			$this->id = ($row[$startcol + 0] !== null) ? (int) $row[$startcol + 0] : null;
			$this->matricula = ($row[$startcol + 1] !== null) ? (string) $row[$startcol + 1] : null;
			$this->marca = ($row[$startcol + 2] !== null) ? (string) $row[$startcol + 2] : null;
			$this->modelo = ($row[$startcol + 3] !== null) ? (string) $row[$startcol + 3] : null;
			$this->anio = ($row[$startcol + 4] !== null) ? (int) $row[$startcol + 4] : null;
			$this->numerochasis = ($row[$startcol + 5] !== null) ? (string) $row[$startcol + 5] : null;
			$this->combustible = ($row[$startcol + 6] !== null) ? (string) $row[$startcol + 6] : null;
			$this->numeromovil = ($row[$startcol + 7] !== null) ? (int) $row[$startcol + 7] : null;
			$this->iddespacho = ($row[$startcol + 8] !== null) ? (int) $row[$startcol + 8] : null;
			$this->kminiciales = ($row[$startcol + 9] !== null) ? (int) $row[$startcol + 9] : null;
			$this->idaseguradora = ($row[$startcol + 10] !== null) ? (int) $row[$startcol + 10] : null;
			$this->fechaalta = ($row[$startcol + 11] !== null) ? (string) $row[$startcol + 11] : null;
			$this->fechabaja = ($row[$startcol + 12] !== null) ? (string) $row[$startcol + 12] : null;
			$this->habilitado = ($row[$startcol + 13] !== null) ? (boolean) $row[$startcol + 13] : null;
			$this->usuario = ($row[$startcol + 14] !== null) ? (int) $row[$startcol + 14] : null;
			$this->resetModified();

			$this->setNew(false);

			if ($rehydrate) {
				$this->ensureConsistency();
			}

			// FIXME - using NUM_COLUMNS may be clearer.
			return $startcol + 15; // 15 = MovilPeer::NUM_COLUMNS - MovilPeer::NUM_LAZY_LOAD_COLUMNS).

		} catch (Exception $e) {
			throw new PropelException("Error populating Movil object", $e);
		}
	}

	/**
	 * Checks and repairs the internal consistency of the object.
	 *
	 * This method is executed after an already-instantiated object is re-hydrated
	 * from the database.  It exists to check any foreign keys to make sure that
	 * the objects related to the current object are correct based on foreign key.
	 *
	 * You can override this method in the stub class, but you should always invoke
	 * the base method from the overridden method (i.e. parent::ensureConsistency()),
	 * in case your model changes.
	 *
	 * @throws     PropelException
	 */
	public function ensureConsistency()
	{

		if ($this->aDespacho !== null && $this->iddespacho !== $this->aDespacho->getId()) {
			$this->aDespacho = null;
		}
		if ($this->aAseguradora !== null && $this->idaseguradora !== $this->aAseguradora->getId()) {
			$this->aAseguradora = null;
		}
		if ($this->aUsuarioRelatedByUsuario !== null && $this->usuario !== $this->aUsuarioRelatedByUsuario->getId()) {
			$this->aUsuarioRelatedByUsuario = null;
		}
	} // ensureConsistency

	/**
	 * Reloads this object from datastore based on primary key and (optionally) resets all associated objects.
	 *
	 * This will only work if the object has been saved and has a valid primary key set.
	 *
	 * @param      boolean $deep (optional) Whether to also de-associated any related objects.
	 * @param      PropelPDO $con (optional) The PropelPDO connection to use.
	 * @return     void
	 * @throws     PropelException - if this object is deleted, unsaved or doesn't have pk match in db
	 */
	public function reload($deep = false, PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("Cannot reload a deleted object.");
		}

		if ($this->isNew()) {
			throw new PropelException("Cannot reload an unsaved object.");
		}

		if ($con === null) {
			$con = Propel::getConnection(MovilPeer::DATABASE_NAME, Propel::CONNECTION_READ);
		}

		// We don't need to alter the object instance pool; we're just modifying this instance
		// already in the pool.

		$stmt = MovilPeer::doSelectStmt($this->buildPkeyCriteria(), $con);
		$row = $stmt->fetch(PDO::FETCH_NUM);
		$stmt->closeCursor();
		if (!$row) {
			throw new PropelException('Cannot find matching row in the database to reload object values.');
		}
		$this->hydrate($row, 0, true); // rehydrate

		if ($deep) {  // also de-associate any related objects?

			$this->aDespacho = null;
			$this->aAseguradora = null;
			$this->aUsuarioRelatedByUsuario = null;
			$this->collMultas = null;
			$this->lastMultaCriteria = null;

			$this->collAccidentes = null;
			$this->lastAccidenteCriteria = null;

			$this->collRecaudacions = null;
			$this->lastRecaudacionCriteria = null;

			$this->collGastomovils = null;
			$this->lastGastomovilCriteria = null;

			$this->collPagoaseguradoras = null;
			$this->lastPagoaseguradoraCriteria = null;

			$this->collTrabajotallers = null;
			$this->lastTrabajotallerCriteria = null;

			$this->singleMovilAseguradora = null;

			$this->collMovilDespachos = null;
			$this->lastMovilDespachoCriteria = null;

			$this->collMovilEmpresas = null;
			$this->lastMovilEmpresaCriteria = null;

		} // if (deep)
	}

	/**
	 * Removes this object from datastore and sets delete attribute.
	 *
	 * @param      PropelPDO $con
	 * @return     void
	 * @throws     PropelException
	 * @see        BaseObject::setDeleted()
	 * @see        BaseObject::isDeleted()
	 */
	public function delete(PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("This object has already been deleted.");
		}

		if ($con === null) {
			$con = Propel::getConnection(MovilPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}
		
		$con->beginTransaction();
		try {
			$ret = $this->preDelete($con);
			// symfony_behaviors behavior
			foreach (sfMixer::getCallables('BaseMovil:delete:pre') as $callable)
			{
			  if (call_user_func($callable, $this, $con))
			  {
			    $con->commit();
			
			    return;
			  }
			}

			if ($ret) {
				MovilPeer::doDelete($this, $con);
				$this->postDelete($con);
				// symfony_behaviors behavior
				foreach (sfMixer::getCallables('BaseMovil:delete:post') as $callable)
				{
				  call_user_func($callable, $this, $con);
				}

				$this->setDeleted(true);
				$con->commit();
			} else {
				$con->commit();
			}
		} catch (PropelException $e) {
			$con->rollBack();
			throw $e;
		}
	}

	/**
	 * Persists this object to the database.
	 *
	 * If the object is new, it inserts it; otherwise an update is performed.
	 * All modified related objects will also be persisted in the doSave()
	 * method.  This method wraps all precipitate database operations in a
	 * single transaction.
	 *
	 * @param      PropelPDO $con
	 * @return     int The number of rows affected by this insert/update and any referring fk objects' save() operations.
	 * @throws     PropelException
	 * @see        doSave()
	 */
	public function save(PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("You cannot save an object that has been deleted.");
		}

		if ($con === null) {
			$con = Propel::getConnection(MovilPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}
		
		$con->beginTransaction();
		$isInsert = $this->isNew();
		try {
			$ret = $this->preSave($con);
			// symfony_behaviors behavior
			foreach (sfMixer::getCallables('BaseMovil:save:pre') as $callable)
			{
			  if (is_integer($affectedRows = call_user_func($callable, $this, $con)))
			  {
			    $con->commit();
			
			    return $affectedRows;
			  }
			}

			if ($isInsert) {
				$ret = $ret && $this->preInsert($con);
			} else {
				$ret = $ret && $this->preUpdate($con);
			}
			if ($ret) {
				$affectedRows = $this->doSave($con);
				if ($isInsert) {
					$this->postInsert($con);
				} else {
					$this->postUpdate($con);
				}
				$this->postSave($con);
				// symfony_behaviors behavior
				foreach (sfMixer::getCallables('BaseMovil:save:post') as $callable)
				{
				  call_user_func($callable, $this, $con, $affectedRows);
				}

				MovilPeer::addInstanceToPool($this);
			} else {
				$affectedRows = 0;
			}
			$con->commit();
			return $affectedRows;
		} catch (PropelException $e) {
			$con->rollBack();
			throw $e;
		}
	}

	/**
	 * Performs the work of inserting or updating the row in the database.
	 *
	 * If the object is new, it inserts it; otherwise an update is performed.
	 * All related objects are also updated in this method.
	 *
	 * @param      PropelPDO $con
	 * @return     int The number of rows affected by this insert/update and any referring fk objects' save() operations.
	 * @throws     PropelException
	 * @see        save()
	 */
	protected function doSave(PropelPDO $con)
	{
		$affectedRows = 0; // initialize var to track total num of affected rows
		if (!$this->alreadyInSave) {
			$this->alreadyInSave = true;

			// We call the save method on the following object(s) if they
			// were passed to this object by their coresponding set
			// method.  This object relates to these object(s) by a
			// foreign key reference.

			if ($this->aDespacho !== null) {
				if ($this->aDespacho->isModified() || $this->aDespacho->isNew()) {
					$affectedRows += $this->aDespacho->save($con);
				}
				$this->setDespacho($this->aDespacho);
			}

			if ($this->aAseguradora !== null) {
				if ($this->aAseguradora->isModified() || $this->aAseguradora->isNew()) {
					$affectedRows += $this->aAseguradora->save($con);
				}
				$this->setAseguradora($this->aAseguradora);
			}

			if ($this->aUsuarioRelatedByUsuario !== null) {
				if ($this->aUsuarioRelatedByUsuario->isModified() || $this->aUsuarioRelatedByUsuario->isNew()) {
					$affectedRows += $this->aUsuarioRelatedByUsuario->save($con);
				}
				$this->setUsuarioRelatedByUsuario($this->aUsuarioRelatedByUsuario);
			}

			if ($this->isNew() ) {
				$this->modifiedColumns[] = MovilPeer::ID;
			}

			// If this object has been modified, then save it to the database.
			if ($this->isModified()) {
				if ($this->isNew()) {
					$pk = MovilPeer::doInsert($this, $con);
					$affectedRows += 1; // we are assuming that there is only 1 row per doInsert() which
										 // should always be true here (even though technically
										 // BasePeer::doInsert() can insert multiple rows).

					$this->setId($pk);  //[IMV] update autoincrement primary key

					$this->setNew(false);
				} else {
					$affectedRows += MovilPeer::doUpdate($this, $con);
				}

				$this->resetModified(); // [HL] After being saved an object is no longer 'modified'
			}

			if ($this->collMultas !== null) {
				foreach ($this->collMultas as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collAccidentes !== null) {
				foreach ($this->collAccidentes as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collRecaudacions !== null) {
				foreach ($this->collRecaudacions as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collGastomovils !== null) {
				foreach ($this->collGastomovils as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collPagoaseguradoras !== null) {
				foreach ($this->collPagoaseguradoras as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collTrabajotallers !== null) {
				foreach ($this->collTrabajotallers as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->singleMovilAseguradora !== null) {
				if (!$this->singleMovilAseguradora->isDeleted()) {
						$affectedRows += $this->singleMovilAseguradora->save($con);
				}
			}

			if ($this->collMovilDespachos !== null) {
				foreach ($this->collMovilDespachos as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collMovilEmpresas !== null) {
				foreach ($this->collMovilEmpresas as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			$this->alreadyInSave = false;

		}
		return $affectedRows;
	} // doSave()

	/**
	 * Array of ValidationFailed objects.
	 * @var        array ValidationFailed[]
	 */
	protected $validationFailures = array();

	/**
	 * Gets any ValidationFailed objects that resulted from last call to validate().
	 *
	 *
	 * @return     array ValidationFailed[]
	 * @see        validate()
	 */
	public function getValidationFailures()
	{
		return $this->validationFailures;
	}

	/**
	 * Validates the objects modified field values and all objects related to this table.
	 *
	 * If $columns is either a column name or an array of column names
	 * only those columns are validated.
	 *
	 * @param      mixed $columns Column name or an array of column names.
	 * @return     boolean Whether all columns pass validation.
	 * @see        doValidate()
	 * @see        getValidationFailures()
	 */
	public function validate($columns = null)
	{
		$res = $this->doValidate($columns);
		if ($res === true) {
			$this->validationFailures = array();
			return true;
		} else {
			$this->validationFailures = $res;
			return false;
		}
	}

	/**
	 * This function performs the validation work for complex object models.
	 *
	 * In addition to checking the current object, all related objects will
	 * also be validated.  If all pass then <code>true</code> is returned; otherwise
	 * an aggreagated array of ValidationFailed objects will be returned.
	 *
	 * @param      array $columns Array of column names to validate.
	 * @return     mixed <code>true</code> if all validations pass; array of <code>ValidationFailed</code> objets otherwise.
	 */
	protected function doValidate($columns = null)
	{
		if (!$this->alreadyInValidation) {
			$this->alreadyInValidation = true;
			$retval = null;

			$failureMap = array();


			// We call the validate method on the following object(s) if they
			// were passed to this object by their coresponding set
			// method.  This object relates to these object(s) by a
			// foreign key reference.

			if ($this->aDespacho !== null) {
				if (!$this->aDespacho->validate($columns)) {
					$failureMap = array_merge($failureMap, $this->aDespacho->getValidationFailures());
				}
			}

			if ($this->aAseguradora !== null) {
				if (!$this->aAseguradora->validate($columns)) {
					$failureMap = array_merge($failureMap, $this->aAseguradora->getValidationFailures());
				}
			}

			if ($this->aUsuarioRelatedByUsuario !== null) {
				if (!$this->aUsuarioRelatedByUsuario->validate($columns)) {
					$failureMap = array_merge($failureMap, $this->aUsuarioRelatedByUsuario->getValidationFailures());
				}
			}


			if (($retval = MovilPeer::doValidate($this, $columns)) !== true) {
				$failureMap = array_merge($failureMap, $retval);
			}


				if ($this->collMultas !== null) {
					foreach ($this->collMultas as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collAccidentes !== null) {
					foreach ($this->collAccidentes as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collRecaudacions !== null) {
					foreach ($this->collRecaudacions as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collGastomovils !== null) {
					foreach ($this->collGastomovils as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collPagoaseguradoras !== null) {
					foreach ($this->collPagoaseguradoras as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collTrabajotallers !== null) {
					foreach ($this->collTrabajotallers as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->singleMovilAseguradora !== null) {
					if (!$this->singleMovilAseguradora->validate($columns)) {
						$failureMap = array_merge($failureMap, $this->singleMovilAseguradora->getValidationFailures());
					}
				}

				if ($this->collMovilDespachos !== null) {
					foreach ($this->collMovilDespachos as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collMovilEmpresas !== null) {
					foreach ($this->collMovilEmpresas as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}


			$this->alreadyInValidation = false;
		}

		return (!empty($failureMap) ? $failureMap : true);
	}

	/**
	 * Retrieves a field from the object by name passed in as a string.
	 *
	 * @param      string $name name
	 * @param      string $type The type of fieldname the $name is of:
	 *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
	 * @return     mixed Value of field.
	 */
	public function getByName($name, $type = BasePeer::TYPE_PHPNAME)
	{
		$pos = MovilPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
		$field = $this->getByPosition($pos);
		return $field;
	}

	/**
	 * Retrieves a field from the object by Position as specified in the xml schema.
	 * Zero-based.
	 *
	 * @param      int $pos position in xml schema
	 * @return     mixed Value of field at $pos
	 */
	public function getByPosition($pos)
	{
		switch($pos) {
			case 0:
				return $this->getId();
				break;
			case 1:
				return $this->getMatricula();
				break;
			case 2:
				return $this->getMarca();
				break;
			case 3:
				return $this->getModelo();
				break;
			case 4:
				return $this->getAnio();
				break;
			case 5:
				return $this->getNumerochasis();
				break;
			case 6:
				return $this->getCombustible();
				break;
			case 7:
				return $this->getNumeromovil();
				break;
			case 8:
				return $this->getIddespacho();
				break;
			case 9:
				return $this->getKminiciales();
				break;
			case 10:
				return $this->getIdaseguradora();
				break;
			case 11:
				return $this->getFechaalta();
				break;
			case 12:
				return $this->getFechabaja();
				break;
			case 13:
				return $this->getHabilitado();
				break;
			case 14:
				return $this->getUsuario();
				break;
			default:
				return null;
				break;
		} // switch()
	}

	/**
	 * Exports the object as an array.
	 *
	 * You can specify the key type of the array by passing one of the class
	 * type constants.
	 *
	 * @param      string $keyType (optional) One of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                        BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM. Defaults to BasePeer::TYPE_PHPNAME.
	 * @param      boolean $includeLazyLoadColumns (optional) Whether to include lazy loaded columns.  Defaults to TRUE.
	 * @return     an associative array containing the field names (as keys) and field values
	 */
	public function toArray($keyType = BasePeer::TYPE_PHPNAME, $includeLazyLoadColumns = true)
	{
		$keys = MovilPeer::getFieldNames($keyType);
		$result = array(
			$keys[0] => $this->getId(),
			$keys[1] => $this->getMatricula(),
			$keys[2] => $this->getMarca(),
			$keys[3] => $this->getModelo(),
			$keys[4] => $this->getAnio(),
			$keys[5] => $this->getNumerochasis(),
			$keys[6] => $this->getCombustible(),
			$keys[7] => $this->getNumeromovil(),
			$keys[8] => $this->getIddespacho(),
			$keys[9] => $this->getKminiciales(),
			$keys[10] => $this->getIdaseguradora(),
			$keys[11] => $this->getFechaalta(),
			$keys[12] => $this->getFechabaja(),
			$keys[13] => $this->getHabilitado(),
			$keys[14] => $this->getUsuario(),
		);
		return $result;
	}

	/**
	 * Sets a field from the object by name passed in as a string.
	 *
	 * @param      string $name peer name
	 * @param      mixed $value field value
	 * @param      string $type The type of fieldname the $name is of:
	 *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
	 * @return     void
	 */
	public function setByName($name, $value, $type = BasePeer::TYPE_PHPNAME)
	{
		$pos = MovilPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
		return $this->setByPosition($pos, $value);
	}

	/**
	 * Sets a field from the object by Position as specified in the xml schema.
	 * Zero-based.
	 *
	 * @param      int $pos position in xml schema
	 * @param      mixed $value field value
	 * @return     void
	 */
	public function setByPosition($pos, $value)
	{
		switch($pos) {
			case 0:
				$this->setId($value);
				break;
			case 1:
				$this->setMatricula($value);
				break;
			case 2:
				$this->setMarca($value);
				break;
			case 3:
				$this->setModelo($value);
				break;
			case 4:
				$this->setAnio($value);
				break;
			case 5:
				$this->setNumerochasis($value);
				break;
			case 6:
				$this->setCombustible($value);
				break;
			case 7:
				$this->setNumeromovil($value);
				break;
			case 8:
				$this->setIddespacho($value);
				break;
			case 9:
				$this->setKminiciales($value);
				break;
			case 10:
				$this->setIdaseguradora($value);
				break;
			case 11:
				$this->setFechaalta($value);
				break;
			case 12:
				$this->setFechabaja($value);
				break;
			case 13:
				$this->setHabilitado($value);
				break;
			case 14:
				$this->setUsuario($value);
				break;
		} // switch()
	}

	/**
	 * Populates the object using an array.
	 *
	 * This is particularly useful when populating an object from one of the
	 * request arrays (e.g. $_POST).  This method goes through the column
	 * names, checking to see whether a matching key exists in populated
	 * array. If so the setByName() method is called for that column.
	 *
	 * You can specify the key type of the array by additionally passing one
	 * of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME,
	 * BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
	 * The default key type is the column's phpname (e.g. 'AuthorId')
	 *
	 * @param      array  $arr     An array to populate the object from.
	 * @param      string $keyType The type of keys the array uses.
	 * @return     void
	 */
	public function fromArray($arr, $keyType = BasePeer::TYPE_PHPNAME)
	{
		$keys = MovilPeer::getFieldNames($keyType);

		if (array_key_exists($keys[0], $arr)) $this->setId($arr[$keys[0]]);
		if (array_key_exists($keys[1], $arr)) $this->setMatricula($arr[$keys[1]]);
		if (array_key_exists($keys[2], $arr)) $this->setMarca($arr[$keys[2]]);
		if (array_key_exists($keys[3], $arr)) $this->setModelo($arr[$keys[3]]);
		if (array_key_exists($keys[4], $arr)) $this->setAnio($arr[$keys[4]]);
		if (array_key_exists($keys[5], $arr)) $this->setNumerochasis($arr[$keys[5]]);
		if (array_key_exists($keys[6], $arr)) $this->setCombustible($arr[$keys[6]]);
		if (array_key_exists($keys[7], $arr)) $this->setNumeromovil($arr[$keys[7]]);
		if (array_key_exists($keys[8], $arr)) $this->setIddespacho($arr[$keys[8]]);
		if (array_key_exists($keys[9], $arr)) $this->setKminiciales($arr[$keys[9]]);
		if (array_key_exists($keys[10], $arr)) $this->setIdaseguradora($arr[$keys[10]]);
		if (array_key_exists($keys[11], $arr)) $this->setFechaalta($arr[$keys[11]]);
		if (array_key_exists($keys[12], $arr)) $this->setFechabaja($arr[$keys[12]]);
		if (array_key_exists($keys[13], $arr)) $this->setHabilitado($arr[$keys[13]]);
		if (array_key_exists($keys[14], $arr)) $this->setUsuario($arr[$keys[14]]);
	}

	/**
	 * Build a Criteria object containing the values of all modified columns in this object.
	 *
	 * @return     Criteria The Criteria object containing all modified values.
	 */
	public function buildCriteria()
	{
		$criteria = new Criteria(MovilPeer::DATABASE_NAME);

		if ($this->isColumnModified(MovilPeer::ID)) $criteria->add(MovilPeer::ID, $this->id);
		if ($this->isColumnModified(MovilPeer::MATRICULA)) $criteria->add(MovilPeer::MATRICULA, $this->matricula);
		if ($this->isColumnModified(MovilPeer::MARCA)) $criteria->add(MovilPeer::MARCA, $this->marca);
		if ($this->isColumnModified(MovilPeer::MODELO)) $criteria->add(MovilPeer::MODELO, $this->modelo);
		if ($this->isColumnModified(MovilPeer::ANIO)) $criteria->add(MovilPeer::ANIO, $this->anio);
		if ($this->isColumnModified(MovilPeer::NUMEROCHASIS)) $criteria->add(MovilPeer::NUMEROCHASIS, $this->numerochasis);
		if ($this->isColumnModified(MovilPeer::COMBUSTIBLE)) $criteria->add(MovilPeer::COMBUSTIBLE, $this->combustible);
		if ($this->isColumnModified(MovilPeer::NUMEROMOVIL)) $criteria->add(MovilPeer::NUMEROMOVIL, $this->numeromovil);
		if ($this->isColumnModified(MovilPeer::IDDESPACHO)) $criteria->add(MovilPeer::IDDESPACHO, $this->iddespacho);
		if ($this->isColumnModified(MovilPeer::KMINICIALES)) $criteria->add(MovilPeer::KMINICIALES, $this->kminiciales);
		if ($this->isColumnModified(MovilPeer::IDASEGURADORA)) $criteria->add(MovilPeer::IDASEGURADORA, $this->idaseguradora);
		if ($this->isColumnModified(MovilPeer::FECHAALTA)) $criteria->add(MovilPeer::FECHAALTA, $this->fechaalta);
		if ($this->isColumnModified(MovilPeer::FECHABAJA)) $criteria->add(MovilPeer::FECHABAJA, $this->fechabaja);
		if ($this->isColumnModified(MovilPeer::HABILITADO)) $criteria->add(MovilPeer::HABILITADO, $this->habilitado);
		if ($this->isColumnModified(MovilPeer::USUARIO)) $criteria->add(MovilPeer::USUARIO, $this->usuario);

		return $criteria;
	}

	/**
	 * Builds a Criteria object containing the primary key for this object.
	 *
	 * Unlike buildCriteria() this method includes the primary key values regardless
	 * of whether or not they have been modified.
	 *
	 * @return     Criteria The Criteria object containing value(s) for primary key(s).
	 */
	public function buildPkeyCriteria()
	{
		$criteria = new Criteria(MovilPeer::DATABASE_NAME);

		$criteria->add(MovilPeer::ID, $this->id);

		return $criteria;
	}

	/**
	 * Returns the primary key for this object (row).
	 * @return     int
	 */
	public function getPrimaryKey()
	{
		return $this->getId();
	}

	/**
	 * Generic method to set the primary key (id column).
	 *
	 * @param      int $key Primary key.
	 * @return     void
	 */
	public function setPrimaryKey($key)
	{
		$this->setId($key);
	}

	/**
	 * Sets contents of passed object to values from current object.
	 *
	 * If desired, this method can also make copies of all associated (fkey referrers)
	 * objects.
	 *
	 * @param      object $copyObj An object of Movil (or compatible) type.
	 * @param      boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
	 * @throws     PropelException
	 */
	public function copyInto($copyObj, $deepCopy = false)
	{

		$copyObj->setMatricula($this->matricula);

		$copyObj->setMarca($this->marca);

		$copyObj->setModelo($this->modelo);

		$copyObj->setAnio($this->anio);

		$copyObj->setNumerochasis($this->numerochasis);

		$copyObj->setCombustible($this->combustible);

		$copyObj->setNumeromovil($this->numeromovil);

		$copyObj->setIddespacho($this->iddespacho);

		$copyObj->setKminiciales($this->kminiciales);

		$copyObj->setIdaseguradora($this->idaseguradora);

		$copyObj->setFechaalta($this->fechaalta);

		$copyObj->setFechabaja($this->fechabaja);

		$copyObj->setHabilitado($this->habilitado);

		$copyObj->setUsuario($this->usuario);


		if ($deepCopy) {
			// important: temporarily setNew(false) because this affects the behavior of
			// the getter/setter methods for fkey referrer objects.
			$copyObj->setNew(false);

			foreach ($this->getMultas() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addMulta($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getAccidentes() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addAccidente($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getRecaudacions() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addRecaudacion($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getGastomovils() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addGastomovil($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getPagoaseguradoras() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addPagoaseguradora($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getTrabajotallers() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addTrabajotaller($relObj->copy($deepCopy));
				}
			}

			$relObj = $this->getMovilAseguradora();
			if ($relObj) {
				$copyObj->setMovilAseguradora($relObj->copy($deepCopy));
			}

			foreach ($this->getMovilDespachos() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addMovilDespacho($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getMovilEmpresas() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addMovilEmpresa($relObj->copy($deepCopy));
				}
			}

		} // if ($deepCopy)


		$copyObj->setNew(true);

		$copyObj->setId(NULL); // this is a auto-increment column, so set to default value

	}

	/**
	 * Makes a copy of this object that will be inserted as a new row in table when saved.
	 * It creates a new object filling in the simple attributes, but skipping any primary
	 * keys that are defined for the table.
	 *
	 * If desired, this method can also make copies of all associated (fkey referrers)
	 * objects.
	 *
	 * @param      boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
	 * @return     Movil Clone of current object.
	 * @throws     PropelException
	 */
	public function copy($deepCopy = false)
	{
		// we use get_class(), because this might be a subclass
		$clazz = get_class($this);
		$copyObj = new $clazz();
		$this->copyInto($copyObj, $deepCopy);
		return $copyObj;
	}

	/**
	 * Returns a peer instance associated with this om.
	 *
	 * Since Peer classes are not to have any instance attributes, this method returns the
	 * same instance for all member of this class. The method could therefore
	 * be static, but this would prevent one from overriding the behavior.
	 *
	 * @return     MovilPeer
	 */
	public function getPeer()
	{
		if (self::$peer === null) {
			self::$peer = new MovilPeer();
		}
		return self::$peer;
	}

	/**
	 * Declares an association between this object and a Despacho object.
	 *
	 * @param      Despacho $v
	 * @return     Movil The current object (for fluent API support)
	 * @throws     PropelException
	 */
	public function setDespacho(Despacho $v = null)
	{
		if ($v === null) {
			$this->setIddespacho(NULL);
		} else {
			$this->setIddespacho($v->getId());
		}

		$this->aDespacho = $v;

		// Add binding for other direction of this n:n relationship.
		// If this object has already been added to the Despacho object, it will not be re-added.
		if ($v !== null) {
			$v->addMovil($this);
		}

		return $this;
	}


	/**
	 * Get the associated Despacho object
	 *
	 * @param      PropelPDO Optional Connection object.
	 * @return     Despacho The associated Despacho object.
	 * @throws     PropelException
	 */
	public function getDespacho(PropelPDO $con = null)
	{
		if ($this->aDespacho === null && ($this->iddespacho !== null)) {
			$this->aDespacho = DespachoPeer::retrieveByPk($this->iddespacho);
			/* The following can be used additionally to
			   guarantee the related object contains a reference
			   to this object.  This level of coupling may, however, be
			   undesirable since it could result in an only partially populated collection
			   in the referenced object.
			   $this->aDespacho->addMovils($this);
			 */
		}
		return $this->aDespacho;
	}

	/**
	 * Declares an association between this object and a Aseguradora object.
	 *
	 * @param      Aseguradora $v
	 * @return     Movil The current object (for fluent API support)
	 * @throws     PropelException
	 */
	public function setAseguradora(Aseguradora $v = null)
	{
		if ($v === null) {
			$this->setIdaseguradora(NULL);
		} else {
			$this->setIdaseguradora($v->getId());
		}

		$this->aAseguradora = $v;

		// Add binding for other direction of this n:n relationship.
		// If this object has already been added to the Aseguradora object, it will not be re-added.
		if ($v !== null) {
			$v->addMovil($this);
		}

		return $this;
	}


	/**
	 * Get the associated Aseguradora object
	 *
	 * @param      PropelPDO Optional Connection object.
	 * @return     Aseguradora The associated Aseguradora object.
	 * @throws     PropelException
	 */
	public function getAseguradora(PropelPDO $con = null)
	{
		if ($this->aAseguradora === null && ($this->idaseguradora !== null)) {
			$this->aAseguradora = AseguradoraPeer::retrieveByPk($this->idaseguradora);
			/* The following can be used additionally to
			   guarantee the related object contains a reference
			   to this object.  This level of coupling may, however, be
			   undesirable since it could result in an only partially populated collection
			   in the referenced object.
			   $this->aAseguradora->addMovils($this);
			 */
		}
		return $this->aAseguradora;
	}

	/**
	 * Declares an association between this object and a Usuario object.
	 *
	 * @param      Usuario $v
	 * @return     Movil The current object (for fluent API support)
	 * @throws     PropelException
	 */
	public function setUsuarioRelatedByUsuario(Usuario $v = null)
	{
		if ($v === null) {
			$this->setUsuario(NULL);
		} else {
			$this->setUsuario($v->getId());
		}

		$this->aUsuarioRelatedByUsuario = $v;

		// Add binding for other direction of this n:n relationship.
		// If this object has already been added to the Usuario object, it will not be re-added.
		if ($v !== null) {
			$v->addMovil($this);
		}

		return $this;
	}


	/**
	 * Get the associated Usuario object
	 *
	 * @param      PropelPDO Optional Connection object.
	 * @return     Usuario The associated Usuario object.
	 * @throws     PropelException
	 */
	public function getUsuarioRelatedByUsuario(PropelPDO $con = null)
	{
		if ($this->aUsuarioRelatedByUsuario === null && ($this->usuario !== null)) {
			$this->aUsuarioRelatedByUsuario = UsuarioPeer::retrieveByPk($this->usuario);
			/* The following can be used additionally to
			   guarantee the related object contains a reference
			   to this object.  This level of coupling may, however, be
			   undesirable since it could result in an only partially populated collection
			   in the referenced object.
			   $this->aUsuarioRelatedByUsuario->addMovils($this);
			 */
		}
		return $this->aUsuarioRelatedByUsuario;
	}

	/**
	 * Clears out the collMultas collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addMultas()
	 */
	public function clearMultas()
	{
		$this->collMultas = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collMultas collection (array).
	 *
	 * By default this just sets the collMultas collection to an empty array (like clearcollMultas());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initMultas()
	{
		$this->collMultas = array();
	}

	/**
	 * Gets an array of Multa objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Movil has previously been saved, it will retrieve
	 * related Multas from storage. If this Movil is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array Multa[]
	 * @throws     PropelException
	 */
	public function getMultas($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(MovilPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collMultas === null) {
			if ($this->isNew()) {
			   $this->collMultas = array();
			} else {

				$criteria->add(MultaPeer::IDMOVIL, $this->id);

				MultaPeer::addSelectColumns($criteria);
				$this->collMultas = MultaPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(MultaPeer::IDMOVIL, $this->id);

				MultaPeer::addSelectColumns($criteria);
				if (!isset($this->lastMultaCriteria) || !$this->lastMultaCriteria->equals($criteria)) {
					$this->collMultas = MultaPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastMultaCriteria = $criteria;
		return $this->collMultas;
	}

	/**
	 * Returns the number of related Multa objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related Multa objects.
	 * @throws     PropelException
	 */
	public function countMultas(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(MovilPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collMultas === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(MultaPeer::IDMOVIL, $this->id);

				$count = MultaPeer::doCount($criteria, false, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(MultaPeer::IDMOVIL, $this->id);

				if (!isset($this->lastMultaCriteria) || !$this->lastMultaCriteria->equals($criteria)) {
					$count = MultaPeer::doCount($criteria, false, $con);
				} else {
					$count = count($this->collMultas);
				}
			} else {
				$count = count($this->collMultas);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a Multa object to this object
	 * through the Multa foreign key attribute.
	 *
	 * @param      Multa $l Multa
	 * @return     void
	 * @throws     PropelException
	 */
	public function addMulta(Multa $l)
	{
		if ($this->collMultas === null) {
			$this->initMultas();
		}
		if (!in_array($l, $this->collMultas, true)) { // only add it if the **same** object is not already associated
			array_push($this->collMultas, $l);
			$l->setMovil($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Movil is new, it will return
	 * an empty collection; or if this Movil has previously
	 * been saved, it will retrieve related Multas from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Movil.
	 */
	public function getMultasJoinChofer($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(MovilPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collMultas === null) {
			if ($this->isNew()) {
				$this->collMultas = array();
			} else {

				$criteria->add(MultaPeer::IDMOVIL, $this->id);

				$this->collMultas = MultaPeer::doSelectJoinChofer($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(MultaPeer::IDMOVIL, $this->id);

			if (!isset($this->lastMultaCriteria) || !$this->lastMultaCriteria->equals($criteria)) {
				$this->collMultas = MultaPeer::doSelectJoinChofer($criteria, $con, $join_behavior);
			}
		}
		$this->lastMultaCriteria = $criteria;

		return $this->collMultas;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Movil is new, it will return
	 * an empty collection; or if this Movil has previously
	 * been saved, it will retrieve related Multas from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Movil.
	 */
	public function getMultasJoinUsuarioRelatedByUsuario($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(MovilPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collMultas === null) {
			if ($this->isNew()) {
				$this->collMultas = array();
			} else {

				$criteria->add(MultaPeer::IDMOVIL, $this->id);

				$this->collMultas = MultaPeer::doSelectJoinUsuarioRelatedByUsuario($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(MultaPeer::IDMOVIL, $this->id);

			if (!isset($this->lastMultaCriteria) || !$this->lastMultaCriteria->equals($criteria)) {
				$this->collMultas = MultaPeer::doSelectJoinUsuarioRelatedByUsuario($criteria, $con, $join_behavior);
			}
		}
		$this->lastMultaCriteria = $criteria;

		return $this->collMultas;
	}

	/**
	 * Clears out the collAccidentes collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addAccidentes()
	 */
	public function clearAccidentes()
	{
		$this->collAccidentes = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collAccidentes collection (array).
	 *
	 * By default this just sets the collAccidentes collection to an empty array (like clearcollAccidentes());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initAccidentes()
	{
		$this->collAccidentes = array();
	}

	/**
	 * Gets an array of Accidente objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Movil has previously been saved, it will retrieve
	 * related Accidentes from storage. If this Movil is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array Accidente[]
	 * @throws     PropelException
	 */
	public function getAccidentes($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(MovilPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collAccidentes === null) {
			if ($this->isNew()) {
			   $this->collAccidentes = array();
			} else {

				$criteria->add(AccidentePeer::IDMOVIL, $this->id);

				AccidentePeer::addSelectColumns($criteria);
				$this->collAccidentes = AccidentePeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(AccidentePeer::IDMOVIL, $this->id);

				AccidentePeer::addSelectColumns($criteria);
				if (!isset($this->lastAccidenteCriteria) || !$this->lastAccidenteCriteria->equals($criteria)) {
					$this->collAccidentes = AccidentePeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastAccidenteCriteria = $criteria;
		return $this->collAccidentes;
	}

	/**
	 * Returns the number of related Accidente objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related Accidente objects.
	 * @throws     PropelException
	 */
	public function countAccidentes(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(MovilPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collAccidentes === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(AccidentePeer::IDMOVIL, $this->id);

				$count = AccidentePeer::doCount($criteria, false, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(AccidentePeer::IDMOVIL, $this->id);

				if (!isset($this->lastAccidenteCriteria) || !$this->lastAccidenteCriteria->equals($criteria)) {
					$count = AccidentePeer::doCount($criteria, false, $con);
				} else {
					$count = count($this->collAccidentes);
				}
			} else {
				$count = count($this->collAccidentes);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a Accidente object to this object
	 * through the Accidente foreign key attribute.
	 *
	 * @param      Accidente $l Accidente
	 * @return     void
	 * @throws     PropelException
	 */
	public function addAccidente(Accidente $l)
	{
		if ($this->collAccidentes === null) {
			$this->initAccidentes();
		}
		if (!in_array($l, $this->collAccidentes, true)) { // only add it if the **same** object is not already associated
			array_push($this->collAccidentes, $l);
			$l->setMovil($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Movil is new, it will return
	 * an empty collection; or if this Movil has previously
	 * been saved, it will retrieve related Accidentes from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Movil.
	 */
	public function getAccidentesJoinChofer($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(MovilPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collAccidentes === null) {
			if ($this->isNew()) {
				$this->collAccidentes = array();
			} else {

				$criteria->add(AccidentePeer::IDMOVIL, $this->id);

				$this->collAccidentes = AccidentePeer::doSelectJoinChofer($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(AccidentePeer::IDMOVIL, $this->id);

			if (!isset($this->lastAccidenteCriteria) || !$this->lastAccidenteCriteria->equals($criteria)) {
				$this->collAccidentes = AccidentePeer::doSelectJoinChofer($criteria, $con, $join_behavior);
			}
		}
		$this->lastAccidenteCriteria = $criteria;

		return $this->collAccidentes;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Movil is new, it will return
	 * an empty collection; or if this Movil has previously
	 * been saved, it will retrieve related Accidentes from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Movil.
	 */
	public function getAccidentesJoinUsuarioRelatedByUsuario($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(MovilPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collAccidentes === null) {
			if ($this->isNew()) {
				$this->collAccidentes = array();
			} else {

				$criteria->add(AccidentePeer::IDMOVIL, $this->id);

				$this->collAccidentes = AccidentePeer::doSelectJoinUsuarioRelatedByUsuario($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(AccidentePeer::IDMOVIL, $this->id);

			if (!isset($this->lastAccidenteCriteria) || !$this->lastAccidenteCriteria->equals($criteria)) {
				$this->collAccidentes = AccidentePeer::doSelectJoinUsuarioRelatedByUsuario($criteria, $con, $join_behavior);
			}
		}
		$this->lastAccidenteCriteria = $criteria;

		return $this->collAccidentes;
	}

	/**
	 * Clears out the collRecaudacions collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addRecaudacions()
	 */
	public function clearRecaudacions()
	{
		$this->collRecaudacions = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collRecaudacions collection (array).
	 *
	 * By default this just sets the collRecaudacions collection to an empty array (like clearcollRecaudacions());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initRecaudacions()
	{
		$this->collRecaudacions = array();
	}

	/**
	 * Gets an array of Recaudacion objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Movil has previously been saved, it will retrieve
	 * related Recaudacions from storage. If this Movil is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array Recaudacion[]
	 * @throws     PropelException
	 */
	public function getRecaudacions($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(MovilPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collRecaudacions === null) {
			if ($this->isNew()) {
			   $this->collRecaudacions = array();
			} else {

				$criteria->add(RecaudacionPeer::IDMOVIL, $this->id);

				RecaudacionPeer::addSelectColumns($criteria);
				$this->collRecaudacions = RecaudacionPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(RecaudacionPeer::IDMOVIL, $this->id);

				RecaudacionPeer::addSelectColumns($criteria);
				if (!isset($this->lastRecaudacionCriteria) || !$this->lastRecaudacionCriteria->equals($criteria)) {
					$this->collRecaudacions = RecaudacionPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastRecaudacionCriteria = $criteria;
		return $this->collRecaudacions;
	}

	/**
	 * Returns the number of related Recaudacion objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related Recaudacion objects.
	 * @throws     PropelException
	 */
	public function countRecaudacions(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(MovilPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collRecaudacions === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(RecaudacionPeer::IDMOVIL, $this->id);

				$count = RecaudacionPeer::doCount($criteria, false, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(RecaudacionPeer::IDMOVIL, $this->id);

				if (!isset($this->lastRecaudacionCriteria) || !$this->lastRecaudacionCriteria->equals($criteria)) {
					$count = RecaudacionPeer::doCount($criteria, false, $con);
				} else {
					$count = count($this->collRecaudacions);
				}
			} else {
				$count = count($this->collRecaudacions);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a Recaudacion object to this object
	 * through the Recaudacion foreign key attribute.
	 *
	 * @param      Recaudacion $l Recaudacion
	 * @return     void
	 * @throws     PropelException
	 */
	public function addRecaudacion(Recaudacion $l)
	{
		if ($this->collRecaudacions === null) {
			$this->initRecaudacions();
		}
		if (!in_array($l, $this->collRecaudacions, true)) { // only add it if the **same** object is not already associated
			array_push($this->collRecaudacions, $l);
			$l->setMovil($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Movil is new, it will return
	 * an empty collection; or if this Movil has previously
	 * been saved, it will retrieve related Recaudacions from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Movil.
	 */
	public function getRecaudacionsJoinChofer($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(MovilPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collRecaudacions === null) {
			if ($this->isNew()) {
				$this->collRecaudacions = array();
			} else {

				$criteria->add(RecaudacionPeer::IDMOVIL, $this->id);

				$this->collRecaudacions = RecaudacionPeer::doSelectJoinChofer($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(RecaudacionPeer::IDMOVIL, $this->id);

			if (!isset($this->lastRecaudacionCriteria) || !$this->lastRecaudacionCriteria->equals($criteria)) {
				$this->collRecaudacions = RecaudacionPeer::doSelectJoinChofer($criteria, $con, $join_behavior);
			}
		}
		$this->lastRecaudacionCriteria = $criteria;

		return $this->collRecaudacions;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Movil is new, it will return
	 * an empty collection; or if this Movil has previously
	 * been saved, it will retrieve related Recaudacions from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Movil.
	 */
	public function getRecaudacionsJoinUsuarioRelatedByUsuario($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(MovilPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collRecaudacions === null) {
			if ($this->isNew()) {
				$this->collRecaudacions = array();
			} else {

				$criteria->add(RecaudacionPeer::IDMOVIL, $this->id);

				$this->collRecaudacions = RecaudacionPeer::doSelectJoinUsuarioRelatedByUsuario($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(RecaudacionPeer::IDMOVIL, $this->id);

			if (!isset($this->lastRecaudacionCriteria) || !$this->lastRecaudacionCriteria->equals($criteria)) {
				$this->collRecaudacions = RecaudacionPeer::doSelectJoinUsuarioRelatedByUsuario($criteria, $con, $join_behavior);
			}
		}
		$this->lastRecaudacionCriteria = $criteria;

		return $this->collRecaudacions;
	}

	/**
	 * Clears out the collGastomovils collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addGastomovils()
	 */
	public function clearGastomovils()
	{
		$this->collGastomovils = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collGastomovils collection (array).
	 *
	 * By default this just sets the collGastomovils collection to an empty array (like clearcollGastomovils());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initGastomovils()
	{
		$this->collGastomovils = array();
	}

	/**
	 * Gets an array of Gastomovil objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Movil has previously been saved, it will retrieve
	 * related Gastomovils from storage. If this Movil is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array Gastomovil[]
	 * @throws     PropelException
	 */
	public function getGastomovils($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(MovilPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collGastomovils === null) {
			if ($this->isNew()) {
			   $this->collGastomovils = array();
			} else {

				$criteria->add(GastomovilPeer::IDMOVIL, $this->id);

				GastomovilPeer::addSelectColumns($criteria);
				$this->collGastomovils = GastomovilPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(GastomovilPeer::IDMOVIL, $this->id);

				GastomovilPeer::addSelectColumns($criteria);
				if (!isset($this->lastGastomovilCriteria) || !$this->lastGastomovilCriteria->equals($criteria)) {
					$this->collGastomovils = GastomovilPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastGastomovilCriteria = $criteria;
		return $this->collGastomovils;
	}

	/**
	 * Returns the number of related Gastomovil objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related Gastomovil objects.
	 * @throws     PropelException
	 */
	public function countGastomovils(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(MovilPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collGastomovils === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(GastomovilPeer::IDMOVIL, $this->id);

				$count = GastomovilPeer::doCount($criteria, false, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(GastomovilPeer::IDMOVIL, $this->id);

				if (!isset($this->lastGastomovilCriteria) || !$this->lastGastomovilCriteria->equals($criteria)) {
					$count = GastomovilPeer::doCount($criteria, false, $con);
				} else {
					$count = count($this->collGastomovils);
				}
			} else {
				$count = count($this->collGastomovils);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a Gastomovil object to this object
	 * through the Gastomovil foreign key attribute.
	 *
	 * @param      Gastomovil $l Gastomovil
	 * @return     void
	 * @throws     PropelException
	 */
	public function addGastomovil(Gastomovil $l)
	{
		if ($this->collGastomovils === null) {
			$this->initGastomovils();
		}
		if (!in_array($l, $this->collGastomovils, true)) { // only add it if the **same** object is not already associated
			array_push($this->collGastomovils, $l);
			$l->setMovil($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Movil is new, it will return
	 * an empty collection; or if this Movil has previously
	 * been saved, it will retrieve related Gastomovils from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Movil.
	 */
	public function getGastomovilsJoinUsuarioRelatedByUsuario($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(MovilPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collGastomovils === null) {
			if ($this->isNew()) {
				$this->collGastomovils = array();
			} else {

				$criteria->add(GastomovilPeer::IDMOVIL, $this->id);

				$this->collGastomovils = GastomovilPeer::doSelectJoinUsuarioRelatedByUsuario($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(GastomovilPeer::IDMOVIL, $this->id);

			if (!isset($this->lastGastomovilCriteria) || !$this->lastGastomovilCriteria->equals($criteria)) {
				$this->collGastomovils = GastomovilPeer::doSelectJoinUsuarioRelatedByUsuario($criteria, $con, $join_behavior);
			}
		}
		$this->lastGastomovilCriteria = $criteria;

		return $this->collGastomovils;
	}

	/**
	 * Clears out the collPagoaseguradoras collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addPagoaseguradoras()
	 */
	public function clearPagoaseguradoras()
	{
		$this->collPagoaseguradoras = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collPagoaseguradoras collection (array).
	 *
	 * By default this just sets the collPagoaseguradoras collection to an empty array (like clearcollPagoaseguradoras());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initPagoaseguradoras()
	{
		$this->collPagoaseguradoras = array();
	}

	/**
	 * Gets an array of Pagoaseguradora objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Movil has previously been saved, it will retrieve
	 * related Pagoaseguradoras from storage. If this Movil is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array Pagoaseguradora[]
	 * @throws     PropelException
	 */
	public function getPagoaseguradoras($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(MovilPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collPagoaseguradoras === null) {
			if ($this->isNew()) {
			   $this->collPagoaseguradoras = array();
			} else {

				$criteria->add(PagoaseguradoraPeer::IDMOVIL, $this->id);

				PagoaseguradoraPeer::addSelectColumns($criteria);
				$this->collPagoaseguradoras = PagoaseguradoraPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(PagoaseguradoraPeer::IDMOVIL, $this->id);

				PagoaseguradoraPeer::addSelectColumns($criteria);
				if (!isset($this->lastPagoaseguradoraCriteria) || !$this->lastPagoaseguradoraCriteria->equals($criteria)) {
					$this->collPagoaseguradoras = PagoaseguradoraPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastPagoaseguradoraCriteria = $criteria;
		return $this->collPagoaseguradoras;
	}

	/**
	 * Returns the number of related Pagoaseguradora objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related Pagoaseguradora objects.
	 * @throws     PropelException
	 */
	public function countPagoaseguradoras(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(MovilPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collPagoaseguradoras === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(PagoaseguradoraPeer::IDMOVIL, $this->id);

				$count = PagoaseguradoraPeer::doCount($criteria, false, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(PagoaseguradoraPeer::IDMOVIL, $this->id);

				if (!isset($this->lastPagoaseguradoraCriteria) || !$this->lastPagoaseguradoraCriteria->equals($criteria)) {
					$count = PagoaseguradoraPeer::doCount($criteria, false, $con);
				} else {
					$count = count($this->collPagoaseguradoras);
				}
			} else {
				$count = count($this->collPagoaseguradoras);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a Pagoaseguradora object to this object
	 * through the Pagoaseguradora foreign key attribute.
	 *
	 * @param      Pagoaseguradora $l Pagoaseguradora
	 * @return     void
	 * @throws     PropelException
	 */
	public function addPagoaseguradora(Pagoaseguradora $l)
	{
		if ($this->collPagoaseguradoras === null) {
			$this->initPagoaseguradoras();
		}
		if (!in_array($l, $this->collPagoaseguradoras, true)) { // only add it if the **same** object is not already associated
			array_push($this->collPagoaseguradoras, $l);
			$l->setMovil($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Movil is new, it will return
	 * an empty collection; or if this Movil has previously
	 * been saved, it will retrieve related Pagoaseguradoras from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Movil.
	 */
	public function getPagoaseguradorasJoinEmpresa($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(MovilPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collPagoaseguradoras === null) {
			if ($this->isNew()) {
				$this->collPagoaseguradoras = array();
			} else {

				$criteria->add(PagoaseguradoraPeer::IDMOVIL, $this->id);

				$this->collPagoaseguradoras = PagoaseguradoraPeer::doSelectJoinEmpresa($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(PagoaseguradoraPeer::IDMOVIL, $this->id);

			if (!isset($this->lastPagoaseguradoraCriteria) || !$this->lastPagoaseguradoraCriteria->equals($criteria)) {
				$this->collPagoaseguradoras = PagoaseguradoraPeer::doSelectJoinEmpresa($criteria, $con, $join_behavior);
			}
		}
		$this->lastPagoaseguradoraCriteria = $criteria;

		return $this->collPagoaseguradoras;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Movil is new, it will return
	 * an empty collection; or if this Movil has previously
	 * been saved, it will retrieve related Pagoaseguradoras from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Movil.
	 */
	public function getPagoaseguradorasJoinAseguradora($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(MovilPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collPagoaseguradoras === null) {
			if ($this->isNew()) {
				$this->collPagoaseguradoras = array();
			} else {

				$criteria->add(PagoaseguradoraPeer::IDMOVIL, $this->id);

				$this->collPagoaseguradoras = PagoaseguradoraPeer::doSelectJoinAseguradora($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(PagoaseguradoraPeer::IDMOVIL, $this->id);

			if (!isset($this->lastPagoaseguradoraCriteria) || !$this->lastPagoaseguradoraCriteria->equals($criteria)) {
				$this->collPagoaseguradoras = PagoaseguradoraPeer::doSelectJoinAseguradora($criteria, $con, $join_behavior);
			}
		}
		$this->lastPagoaseguradoraCriteria = $criteria;

		return $this->collPagoaseguradoras;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Movil is new, it will return
	 * an empty collection; or if this Movil has previously
	 * been saved, it will retrieve related Pagoaseguradoras from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Movil.
	 */
	public function getPagoaseguradorasJoinUsuarioRelatedByUsuario($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(MovilPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collPagoaseguradoras === null) {
			if ($this->isNew()) {
				$this->collPagoaseguradoras = array();
			} else {

				$criteria->add(PagoaseguradoraPeer::IDMOVIL, $this->id);

				$this->collPagoaseguradoras = PagoaseguradoraPeer::doSelectJoinUsuarioRelatedByUsuario($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(PagoaseguradoraPeer::IDMOVIL, $this->id);

			if (!isset($this->lastPagoaseguradoraCriteria) || !$this->lastPagoaseguradoraCriteria->equals($criteria)) {
				$this->collPagoaseguradoras = PagoaseguradoraPeer::doSelectJoinUsuarioRelatedByUsuario($criteria, $con, $join_behavior);
			}
		}
		$this->lastPagoaseguradoraCriteria = $criteria;

		return $this->collPagoaseguradoras;
	}

	/**
	 * Clears out the collTrabajotallers collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addTrabajotallers()
	 */
	public function clearTrabajotallers()
	{
		$this->collTrabajotallers = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collTrabajotallers collection (array).
	 *
	 * By default this just sets the collTrabajotallers collection to an empty array (like clearcollTrabajotallers());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initTrabajotallers()
	{
		$this->collTrabajotallers = array();
	}

	/**
	 * Gets an array of Trabajotaller objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Movil has previously been saved, it will retrieve
	 * related Trabajotallers from storage. If this Movil is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array Trabajotaller[]
	 * @throws     PropelException
	 */
	public function getTrabajotallers($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(MovilPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collTrabajotallers === null) {
			if ($this->isNew()) {
			   $this->collTrabajotallers = array();
			} else {

				$criteria->add(TrabajotallerPeer::IDMOVIL, $this->id);

				TrabajotallerPeer::addSelectColumns($criteria);
				$this->collTrabajotallers = TrabajotallerPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(TrabajotallerPeer::IDMOVIL, $this->id);

				TrabajotallerPeer::addSelectColumns($criteria);
				if (!isset($this->lastTrabajotallerCriteria) || !$this->lastTrabajotallerCriteria->equals($criteria)) {
					$this->collTrabajotallers = TrabajotallerPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastTrabajotallerCriteria = $criteria;
		return $this->collTrabajotallers;
	}

	/**
	 * Returns the number of related Trabajotaller objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related Trabajotaller objects.
	 * @throws     PropelException
	 */
	public function countTrabajotallers(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(MovilPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collTrabajotallers === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(TrabajotallerPeer::IDMOVIL, $this->id);

				$count = TrabajotallerPeer::doCount($criteria, false, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(TrabajotallerPeer::IDMOVIL, $this->id);

				if (!isset($this->lastTrabajotallerCriteria) || !$this->lastTrabajotallerCriteria->equals($criteria)) {
					$count = TrabajotallerPeer::doCount($criteria, false, $con);
				} else {
					$count = count($this->collTrabajotallers);
				}
			} else {
				$count = count($this->collTrabajotallers);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a Trabajotaller object to this object
	 * through the Trabajotaller foreign key attribute.
	 *
	 * @param      Trabajotaller $l Trabajotaller
	 * @return     void
	 * @throws     PropelException
	 */
	public function addTrabajotaller(Trabajotaller $l)
	{
		if ($this->collTrabajotallers === null) {
			$this->initTrabajotallers();
		}
		if (!in_array($l, $this->collTrabajotallers, true)) { // only add it if the **same** object is not already associated
			array_push($this->collTrabajotallers, $l);
			$l->setMovil($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Movil is new, it will return
	 * an empty collection; or if this Movil has previously
	 * been saved, it will retrieve related Trabajotallers from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Movil.
	 */
	public function getTrabajotallersJoinTaller($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(MovilPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collTrabajotallers === null) {
			if ($this->isNew()) {
				$this->collTrabajotallers = array();
			} else {

				$criteria->add(TrabajotallerPeer::IDMOVIL, $this->id);

				$this->collTrabajotallers = TrabajotallerPeer::doSelectJoinTaller($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(TrabajotallerPeer::IDMOVIL, $this->id);

			if (!isset($this->lastTrabajotallerCriteria) || !$this->lastTrabajotallerCriteria->equals($criteria)) {
				$this->collTrabajotallers = TrabajotallerPeer::doSelectJoinTaller($criteria, $con, $join_behavior);
			}
		}
		$this->lastTrabajotallerCriteria = $criteria;

		return $this->collTrabajotallers;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Movil is new, it will return
	 * an empty collection; or if this Movil has previously
	 * been saved, it will retrieve related Trabajotallers from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Movil.
	 */
	public function getTrabajotallersJoinUsuarioRelatedByUsuario($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(MovilPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collTrabajotallers === null) {
			if ($this->isNew()) {
				$this->collTrabajotallers = array();
			} else {

				$criteria->add(TrabajotallerPeer::IDMOVIL, $this->id);

				$this->collTrabajotallers = TrabajotallerPeer::doSelectJoinUsuarioRelatedByUsuario($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(TrabajotallerPeer::IDMOVIL, $this->id);

			if (!isset($this->lastTrabajotallerCriteria) || !$this->lastTrabajotallerCriteria->equals($criteria)) {
				$this->collTrabajotallers = TrabajotallerPeer::doSelectJoinUsuarioRelatedByUsuario($criteria, $con, $join_behavior);
			}
		}
		$this->lastTrabajotallerCriteria = $criteria;

		return $this->collTrabajotallers;
	}

	/**
	 * Gets a single MovilAseguradora object, which is related to this object by a one-to-one relationship.
	 *
	 * @param      PropelPDO $con
	 * @return     MovilAseguradora
	 * @throws     PropelException
	 */
	public function getMovilAseguradora(PropelPDO $con = null)
	{

		if ($this->singleMovilAseguradora === null && !$this->isNew()) {
			$this->singleMovilAseguradora = MovilAseguradoraPeer::retrieveByPK($this->id, $con);
		}

		return $this->singleMovilAseguradora;
	}

	/**
	 * Sets a single MovilAseguradora object as related to this object by a one-to-one relationship.
	 *
	 * @param      MovilAseguradora $l MovilAseguradora
	 * @return     Movil The current object (for fluent API support)
	 * @throws     PropelException
	 */
	public function setMovilAseguradora(MovilAseguradora $v)
	{
		$this->singleMovilAseguradora = $v;

		// Make sure that that the passed-in MovilAseguradora isn't already associated with this object
		if ($v->getMovil() === null) {
			$v->setMovil($this);
		}

		return $this;
	}

	/**
	 * Clears out the collMovilDespachos collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addMovilDespachos()
	 */
	public function clearMovilDespachos()
	{
		$this->collMovilDespachos = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collMovilDespachos collection (array).
	 *
	 * By default this just sets the collMovilDespachos collection to an empty array (like clearcollMovilDespachos());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initMovilDespachos()
	{
		$this->collMovilDespachos = array();
	}

	/**
	 * Gets an array of MovilDespacho objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Movil has previously been saved, it will retrieve
	 * related MovilDespachos from storage. If this Movil is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array MovilDespacho[]
	 * @throws     PropelException
	 */
	public function getMovilDespachos($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(MovilPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collMovilDespachos === null) {
			if ($this->isNew()) {
			   $this->collMovilDespachos = array();
			} else {

				$criteria->add(MovilDespachoPeer::IDMOVIL, $this->id);

				MovilDespachoPeer::addSelectColumns($criteria);
				$this->collMovilDespachos = MovilDespachoPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(MovilDespachoPeer::IDMOVIL, $this->id);

				MovilDespachoPeer::addSelectColumns($criteria);
				if (!isset($this->lastMovilDespachoCriteria) || !$this->lastMovilDespachoCriteria->equals($criteria)) {
					$this->collMovilDespachos = MovilDespachoPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastMovilDespachoCriteria = $criteria;
		return $this->collMovilDespachos;
	}

	/**
	 * Returns the number of related MovilDespacho objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related MovilDespacho objects.
	 * @throws     PropelException
	 */
	public function countMovilDespachos(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(MovilPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collMovilDespachos === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(MovilDespachoPeer::IDMOVIL, $this->id);

				$count = MovilDespachoPeer::doCount($criteria, false, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(MovilDespachoPeer::IDMOVIL, $this->id);

				if (!isset($this->lastMovilDespachoCriteria) || !$this->lastMovilDespachoCriteria->equals($criteria)) {
					$count = MovilDespachoPeer::doCount($criteria, false, $con);
				} else {
					$count = count($this->collMovilDespachos);
				}
			} else {
				$count = count($this->collMovilDespachos);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a MovilDespacho object to this object
	 * through the MovilDespacho foreign key attribute.
	 *
	 * @param      MovilDespacho $l MovilDespacho
	 * @return     void
	 * @throws     PropelException
	 */
	public function addMovilDespacho(MovilDespacho $l)
	{
		if ($this->collMovilDespachos === null) {
			$this->initMovilDespachos();
		}
		if (!in_array($l, $this->collMovilDespachos, true)) { // only add it if the **same** object is not already associated
			array_push($this->collMovilDespachos, $l);
			$l->setMovil($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Movil is new, it will return
	 * an empty collection; or if this Movil has previously
	 * been saved, it will retrieve related MovilDespachos from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Movil.
	 */
	public function getMovilDespachosJoinDespacho($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(MovilPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collMovilDespachos === null) {
			if ($this->isNew()) {
				$this->collMovilDespachos = array();
			} else {

				$criteria->add(MovilDespachoPeer::IDMOVIL, $this->id);

				$this->collMovilDespachos = MovilDespachoPeer::doSelectJoinDespacho($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(MovilDespachoPeer::IDMOVIL, $this->id);

			if (!isset($this->lastMovilDespachoCriteria) || !$this->lastMovilDespachoCriteria->equals($criteria)) {
				$this->collMovilDespachos = MovilDespachoPeer::doSelectJoinDespacho($criteria, $con, $join_behavior);
			}
		}
		$this->lastMovilDespachoCriteria = $criteria;

		return $this->collMovilDespachos;
	}

	/**
	 * Clears out the collMovilEmpresas collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addMovilEmpresas()
	 */
	public function clearMovilEmpresas()
	{
		$this->collMovilEmpresas = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collMovilEmpresas collection (array).
	 *
	 * By default this just sets the collMovilEmpresas collection to an empty array (like clearcollMovilEmpresas());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initMovilEmpresas()
	{
		$this->collMovilEmpresas = array();
	}

	/**
	 * Gets an array of MovilEmpresa objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Movil has previously been saved, it will retrieve
	 * related MovilEmpresas from storage. If this Movil is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array MovilEmpresa[]
	 * @throws     PropelException
	 */
	public function getMovilEmpresas($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(MovilPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collMovilEmpresas === null) {
			if ($this->isNew()) {
			   $this->collMovilEmpresas = array();
			} else {

				$criteria->add(MovilEmpresaPeer::IDMOVIL, $this->id);

				MovilEmpresaPeer::addSelectColumns($criteria);
				$this->collMovilEmpresas = MovilEmpresaPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(MovilEmpresaPeer::IDMOVIL, $this->id);

				MovilEmpresaPeer::addSelectColumns($criteria);
				if (!isset($this->lastMovilEmpresaCriteria) || !$this->lastMovilEmpresaCriteria->equals($criteria)) {
					$this->collMovilEmpresas = MovilEmpresaPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastMovilEmpresaCriteria = $criteria;
		return $this->collMovilEmpresas;
	}

	/**
	 * Returns the number of related MovilEmpresa objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related MovilEmpresa objects.
	 * @throws     PropelException
	 */
	public function countMovilEmpresas(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(MovilPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collMovilEmpresas === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(MovilEmpresaPeer::IDMOVIL, $this->id);

				$count = MovilEmpresaPeer::doCount($criteria, false, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(MovilEmpresaPeer::IDMOVIL, $this->id);

				if (!isset($this->lastMovilEmpresaCriteria) || !$this->lastMovilEmpresaCriteria->equals($criteria)) {
					$count = MovilEmpresaPeer::doCount($criteria, false, $con);
				} else {
					$count = count($this->collMovilEmpresas);
				}
			} else {
				$count = count($this->collMovilEmpresas);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a MovilEmpresa object to this object
	 * through the MovilEmpresa foreign key attribute.
	 *
	 * @param      MovilEmpresa $l MovilEmpresa
	 * @return     void
	 * @throws     PropelException
	 */
	public function addMovilEmpresa(MovilEmpresa $l)
	{
		if ($this->collMovilEmpresas === null) {
			$this->initMovilEmpresas();
		}
		if (!in_array($l, $this->collMovilEmpresas, true)) { // only add it if the **same** object is not already associated
			array_push($this->collMovilEmpresas, $l);
			$l->setMovil($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Movil is new, it will return
	 * an empty collection; or if this Movil has previously
	 * been saved, it will retrieve related MovilEmpresas from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Movil.
	 */
	public function getMovilEmpresasJoinEmpresa($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(MovilPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collMovilEmpresas === null) {
			if ($this->isNew()) {
				$this->collMovilEmpresas = array();
			} else {

				$criteria->add(MovilEmpresaPeer::IDMOVIL, $this->id);

				$this->collMovilEmpresas = MovilEmpresaPeer::doSelectJoinEmpresa($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(MovilEmpresaPeer::IDMOVIL, $this->id);

			if (!isset($this->lastMovilEmpresaCriteria) || !$this->lastMovilEmpresaCriteria->equals($criteria)) {
				$this->collMovilEmpresas = MovilEmpresaPeer::doSelectJoinEmpresa($criteria, $con, $join_behavior);
			}
		}
		$this->lastMovilEmpresaCriteria = $criteria;

		return $this->collMovilEmpresas;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Movil is new, it will return
	 * an empty collection; or if this Movil has previously
	 * been saved, it will retrieve related MovilEmpresas from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Movil.
	 */
	public function getMovilEmpresasJoinUsuarioRelatedByUsuario($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(MovilPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collMovilEmpresas === null) {
			if ($this->isNew()) {
				$this->collMovilEmpresas = array();
			} else {

				$criteria->add(MovilEmpresaPeer::IDMOVIL, $this->id);

				$this->collMovilEmpresas = MovilEmpresaPeer::doSelectJoinUsuarioRelatedByUsuario($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(MovilEmpresaPeer::IDMOVIL, $this->id);

			if (!isset($this->lastMovilEmpresaCriteria) || !$this->lastMovilEmpresaCriteria->equals($criteria)) {
				$this->collMovilEmpresas = MovilEmpresaPeer::doSelectJoinUsuarioRelatedByUsuario($criteria, $con, $join_behavior);
			}
		}
		$this->lastMovilEmpresaCriteria = $criteria;

		return $this->collMovilEmpresas;
	}

	/**
	 * Resets all collections of referencing foreign keys.
	 *
	 * This method is a user-space workaround for PHP's inability to garbage collect objects
	 * with circular references.  This is currently necessary when using Propel in certain
	 * daemon or large-volumne/high-memory operations.
	 *
	 * @param      boolean $deep Whether to also clear the references on all associated objects.
	 */
	public function clearAllReferences($deep = false)
	{
		if ($deep) {
			if ($this->collMultas) {
				foreach ((array) $this->collMultas as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collAccidentes) {
				foreach ((array) $this->collAccidentes as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collRecaudacions) {
				foreach ((array) $this->collRecaudacions as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collGastomovils) {
				foreach ((array) $this->collGastomovils as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collPagoaseguradoras) {
				foreach ((array) $this->collPagoaseguradoras as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collTrabajotallers) {
				foreach ((array) $this->collTrabajotallers as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->singleMovilAseguradora) {
				$this->singleMovilAseguradora->clearAllReferences($deep);
			}
			if ($this->collMovilDespachos) {
				foreach ((array) $this->collMovilDespachos as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collMovilEmpresas) {
				foreach ((array) $this->collMovilEmpresas as $o) {
					$o->clearAllReferences($deep);
				}
			}
		} // if ($deep)

		$this->collMultas = null;
		$this->collAccidentes = null;
		$this->collRecaudacions = null;
		$this->collGastomovils = null;
		$this->collPagoaseguradoras = null;
		$this->collTrabajotallers = null;
		$this->singleMovilAseguradora = null;
		$this->collMovilDespachos = null;
		$this->collMovilEmpresas = null;
			$this->aDespacho = null;
			$this->aAseguradora = null;
			$this->aUsuarioRelatedByUsuario = null;
	}

	// symfony_behaviors behavior
	
	/**
	 * Calls methods defined via {@link sfMixer}.
	 */
	public function __call($method, $arguments)
	{
	  if (!$callable = sfMixer::getCallable('BaseMovil:'.$method))
	  {
	    throw new sfException(sprintf('Call to undefined method BaseMovil::%s', $method));
	  }
	
	  array_unshift($arguments, $this);
	
	  return call_user_func_array($callable, $arguments);
	}

} // BaseMovil

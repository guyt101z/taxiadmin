<?php

/**
 * Base class that represents a row from the 'usuario' table.
 *
 * 
 *
 * This class was autogenerated by Propel 1.4.2 on:
 *
 * Mon Jun 10 22:16:03 2013
 *
 * @package    lib.model.om
 */
abstract class BaseUsuario extends BaseObject  implements Persistent {


	/**
	 * The Peer class.
	 * Instance provides a convenient way of calling static methods on a class
	 * that calling code may not be able to identify.
	 * @var        UsuarioPeer
	 */
	protected static $peer;

	/**
	 * The value for the id field.
	 * @var        int
	 */
	protected $id;

	/**
	 * The value for the tipo field.
	 * @var        string
	 */
	protected $tipo;

	/**
	 * The value for the nombre field.
	 * @var        string
	 */
	protected $nombre;

	/**
	 * The value for the apellidos field.
	 * @var        string
	 */
	protected $apellidos;

	/**
	 * The value for the celular field.
	 * @var        string
	 */
	protected $celular;

	/**
	 * The value for the telefono field.
	 * @var        string
	 */
	protected $telefono;

	/**
	 * The value for the direccion field.
	 * @var        string
	 */
	protected $direccion;

	/**
	 * The value for the email field.
	 * @var        string
	 */
	protected $email;

	/**
	 * The value for the clave field.
	 * @var        string
	 */
	protected $clave;

	/**
	 * The value for the fechaalta field.
	 * @var        string
	 */
	protected $fechaalta;

	/**
	 * The value for the fechabaja field.
	 * @var        string
	 */
	protected $fechabaja;

	/**
	 * The value for the habilitado field.
	 * Note: this column has a database default value of: true
	 * @var        boolean
	 */
	protected $habilitado;

	/**
	 * @var        array Propietario[] Collection to store aggregation of Propietario objects.
	 */
	protected $collPropietarios;

	/**
	 * @var        Criteria The criteria used to select the current contents of collPropietarios.
	 */
	private $lastPropietarioCriteria = null;

	/**
	 * @var        array Banco[] Collection to store aggregation of Banco objects.
	 */
	protected $collBancos;

	/**
	 * @var        Criteria The criteria used to select the current contents of collBancos.
	 */
	private $lastBancoCriteria = null;

	/**
	 * @var        array Empresa[] Collection to store aggregation of Empresa objects.
	 */
	protected $collEmpresas;

	/**
	 * @var        Criteria The criteria used to select the current contents of collEmpresas.
	 */
	private $lastEmpresaCriteria = null;

	/**
	 * @var        array Taller[] Collection to store aggregation of Taller objects.
	 */
	protected $collTallers;

	/**
	 * @var        Criteria The criteria used to select the current contents of collTallers.
	 */
	private $lastTallerCriteria = null;

	/**
	 * @var        array Aseguradora[] Collection to store aggregation of Aseguradora objects.
	 */
	protected $collAseguradoras;

	/**
	 * @var        Criteria The criteria used to select the current contents of collAseguradoras.
	 */
	private $lastAseguradoraCriteria = null;

	/**
	 * @var        array Despacho[] Collection to store aggregation of Despacho objects.
	 */
	protected $collDespachos;

	/**
	 * @var        Criteria The criteria used to select the current contents of collDespachos.
	 */
	private $lastDespachoCriteria = null;

	/**
	 * @var        array Chofer[] Collection to store aggregation of Chofer objects.
	 */
	protected $collChofers;

	/**
	 * @var        Criteria The criteria used to select the current contents of collChofers.
	 */
	private $lastChoferCriteria = null;

	/**
	 * @var        array Movil[] Collection to store aggregation of Movil objects.
	 */
	protected $collMovils;

	/**
	 * @var        Criteria The criteria used to select the current contents of collMovils.
	 */
	private $lastMovilCriteria = null;

	/**
	 * @var        array Multa[] Collection to store aggregation of Multa objects.
	 */
	protected $collMultas;

	/**
	 * @var        Criteria The criteria used to select the current contents of collMultas.
	 */
	private $lastMultaCriteria = null;

	/**
	 * @var        array Accidente[] Collection to store aggregation of Accidente objects.
	 */
	protected $collAccidentes;

	/**
	 * @var        Criteria The criteria used to select the current contents of collAccidentes.
	 */
	private $lastAccidenteCriteria = null;

	/**
	 * @var        array Recaudacion[] Collection to store aggregation of Recaudacion objects.
	 */
	protected $collRecaudacions;

	/**
	 * @var        Criteria The criteria used to select the current contents of collRecaudacions.
	 */
	private $lastRecaudacionCriteria = null;

	/**
	 * @var        array Gastorecaudacion[] Collection to store aggregation of Gastorecaudacion objects.
	 */
	protected $collGastorecaudacions;

	/**
	 * @var        Criteria The criteria used to select the current contents of collGastorecaudacions.
	 */
	private $lastGastorecaudacionCriteria = null;

	/**
	 * @var        array Gastomovil[] Collection to store aggregation of Gastomovil objects.
	 */
	protected $collGastomovils;

	/**
	 * @var        Criteria The criteria used to select the current contents of collGastomovils.
	 */
	private $lastGastomovilCriteria = null;

	/**
	 * @var        array Gastoempresa[] Collection to store aggregation of Gastoempresa objects.
	 */
	protected $collGastoempresas;

	/**
	 * @var        Criteria The criteria used to select the current contents of collGastoempresas.
	 */
	private $lastGastoempresaCriteria = null;

	/**
	 * @var        array Configuracion[] Collection to store aggregation of Configuracion objects.
	 */
	protected $collConfiguracions;

	/**
	 * @var        Criteria The criteria used to select the current contents of collConfiguracions.
	 */
	private $lastConfiguracionCriteria = null;

	/**
	 * @var        array Pagoaseguradora[] Collection to store aggregation of Pagoaseguradora objects.
	 */
	protected $collPagoaseguradoras;

	/**
	 * @var        Criteria The criteria used to select the current contents of collPagoaseguradoras.
	 */
	private $lastPagoaseguradoraCriteria = null;

	/**
	 * @var        array Trabajotaller[] Collection to store aggregation of Trabajotaller objects.
	 */
	protected $collTrabajotallers;

	/**
	 * @var        Criteria The criteria used to select the current contents of collTrabajotallers.
	 */
	private $lastTrabajotallerCriteria = null;

	/**
	 * @var        array Adelanto[] Collection to store aggregation of Adelanto objects.
	 */
	protected $collAdelantos;

	/**
	 * @var        Criteria The criteria used to select the current contents of collAdelantos.
	 */
	private $lastAdelantoCriteria = null;

	/**
	 * @var        array Pagoadelanto[] Collection to store aggregation of Pagoadelanto objects.
	 */
	protected $collPagoadelantos;

	/**
	 * @var        Criteria The criteria used to select the current contents of collPagoadelantos.
	 */
	private $lastPagoadelantoCriteria = null;

	/**
	 * @var        array EmpresaPropietario[] Collection to store aggregation of EmpresaPropietario objects.
	 */
	protected $collEmpresaPropietarios;

	/**
	 * @var        Criteria The criteria used to select the current contents of collEmpresaPropietarios.
	 */
	private $lastEmpresaPropietarioCriteria = null;

	/**
	 * @var        array MovilAseguradora[] Collection to store aggregation of MovilAseguradora objects.
	 */
	protected $collMovilAseguradoras;

	/**
	 * @var        Criteria The criteria used to select the current contents of collMovilAseguradoras.
	 */
	private $lastMovilAseguradoraCriteria = null;

	/**
	 * @var        array MovilEmpresa[] Collection to store aggregation of MovilEmpresa objects.
	 */
	protected $collMovilEmpresas;

	/**
	 * @var        Criteria The criteria used to select the current contents of collMovilEmpresas.
	 */
	private $lastMovilEmpresaCriteria = null;

	/**
	 * @var        array ChoferEmpresa[] Collection to store aggregation of ChoferEmpresa objects.
	 */
	protected $collChoferEmpresas;

	/**
	 * @var        Criteria The criteria used to select the current contents of collChoferEmpresas.
	 */
	private $lastChoferEmpresaCriteria = null;

	/**
	 * Flag to prevent endless save loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var        boolean
	 */
	protected $alreadyInSave = false;

	/**
	 * Flag to prevent endless validation loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var        boolean
	 */
	protected $alreadyInValidation = false;

	// symfony behavior
	
	const PEER = 'UsuarioPeer';

	/**
	 * Applies default values to this object.
	 * This method should be called from the object's constructor (or
	 * equivalent initialization method).
	 * @see        __construct()
	 */
	public function applyDefaultValues()
	{
		$this->habilitado = true;
	}

	/**
	 * Initializes internal state of BaseUsuario object.
	 * @see        applyDefaults()
	 */
	public function __construct()
	{
		parent::__construct();
		$this->applyDefaultValues();
	}

	/**
	 * Get the [id] column value.
	 * 
	 * @return     int
	 */
	public function getId()
	{
		return $this->id;
	}

	/**
	 * Get the [tipo] column value.
	 * 
	 * @return     string
	 */
	public function getTipo()
	{
		return $this->tipo;
	}

	/**
	 * Get the [nombre] column value.
	 * 
	 * @return     string
	 */
	public function getNombre()
	{
		return $this->nombre;
	}

	/**
	 * Get the [apellidos] column value.
	 * 
	 * @return     string
	 */
	public function getApellidos()
	{
		return $this->apellidos;
	}

	/**
	 * Get the [celular] column value.
	 * 
	 * @return     string
	 */
	public function getCelular()
	{
		return $this->celular;
	}

	/**
	 * Get the [telefono] column value.
	 * 
	 * @return     string
	 */
	public function getTelefono()
	{
		return $this->telefono;
	}

	/**
	 * Get the [direccion] column value.
	 * 
	 * @return     string
	 */
	public function getDireccion()
	{
		return $this->direccion;
	}

	/**
	 * Get the [email] column value.
	 * 
	 * @return     string
	 */
	public function getEmail()
	{
		return $this->email;
	}

	/**
	 * Get the [clave] column value.
	 * 
	 * @return     string
	 */
	public function getClave()
	{
		return $this->clave;
	}

	/**
	 * Get the [optionally formatted] temporal [fechaalta] column value.
	 * 
	 *
	 * @param      string $format The date/time format string (either date()-style or strftime()-style).
	 *							If format is NULL, then the raw DateTime object will be returned.
	 * @return     mixed Formatted date/time value as string or DateTime object (if format is NULL), NULL if column is NULL, and 0 if column value is 0000-00-00 00:00:00
	 * @throws     PropelException - if unable to parse/validate the date/time value.
	 */
	public function getFechaalta($format = 'Y-m-d H:i:s')
	{
		if ($this->fechaalta === null) {
			return null;
		}


		if ($this->fechaalta === '0000-00-00 00:00:00') {
			// while technically this is not a default value of NULL,
			// this seems to be closest in meaning.
			return null;
		} else {
			try {
				$dt = new DateTime($this->fechaalta);
			} catch (Exception $x) {
				throw new PropelException("Internally stored date/time/timestamp value could not be converted to DateTime: " . var_export($this->fechaalta, true), $x);
			}
		}

		if ($format === null) {
			// Because propel.useDateTimeClass is TRUE, we return a DateTime object.
			return $dt;
		} elseif (strpos($format, '%') !== false) {
			return strftime($format, $dt->format('U'));
		} else {
			return $dt->format($format);
		}
	}

	/**
	 * Get the [optionally formatted] temporal [fechabaja] column value.
	 * 
	 *
	 * @param      string $format The date/time format string (either date()-style or strftime()-style).
	 *							If format is NULL, then the raw DateTime object will be returned.
	 * @return     mixed Formatted date/time value as string or DateTime object (if format is NULL), NULL if column is NULL, and 0 if column value is 0000-00-00 00:00:00
	 * @throws     PropelException - if unable to parse/validate the date/time value.
	 */
	public function getFechabaja($format = 'Y-m-d H:i:s')
	{
		if ($this->fechabaja === null) {
			return null;
		}


		if ($this->fechabaja === '0000-00-00 00:00:00') {
			// while technically this is not a default value of NULL,
			// this seems to be closest in meaning.
			return null;
		} else {
			try {
				$dt = new DateTime($this->fechabaja);
			} catch (Exception $x) {
				throw new PropelException("Internally stored date/time/timestamp value could not be converted to DateTime: " . var_export($this->fechabaja, true), $x);
			}
		}

		if ($format === null) {
			// Because propel.useDateTimeClass is TRUE, we return a DateTime object.
			return $dt;
		} elseif (strpos($format, '%') !== false) {
			return strftime($format, $dt->format('U'));
		} else {
			return $dt->format($format);
		}
	}

	/**
	 * Get the [habilitado] column value.
	 * 
	 * @return     boolean
	 */
	public function getHabilitado()
	{
		return $this->habilitado;
	}

	/**
	 * Set the value of [id] column.
	 * 
	 * @param      int $v new value
	 * @return     Usuario The current object (for fluent API support)
	 */
	public function setId($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->id !== $v) {
			$this->id = $v;
			$this->modifiedColumns[] = UsuarioPeer::ID;
		}

		return $this;
	} // setId()

	/**
	 * Set the value of [tipo] column.
	 * 
	 * @param      string $v new value
	 * @return     Usuario The current object (for fluent API support)
	 */
	public function setTipo($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->tipo !== $v) {
			$this->tipo = $v;
			$this->modifiedColumns[] = UsuarioPeer::TIPO;
		}

		return $this;
	} // setTipo()

	/**
	 * Set the value of [nombre] column.
	 * 
	 * @param      string $v new value
	 * @return     Usuario The current object (for fluent API support)
	 */
	public function setNombre($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->nombre !== $v) {
			$this->nombre = $v;
			$this->modifiedColumns[] = UsuarioPeer::NOMBRE;
		}

		return $this;
	} // setNombre()

	/**
	 * Set the value of [apellidos] column.
	 * 
	 * @param      string $v new value
	 * @return     Usuario The current object (for fluent API support)
	 */
	public function setApellidos($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->apellidos !== $v) {
			$this->apellidos = $v;
			$this->modifiedColumns[] = UsuarioPeer::APELLIDOS;
		}

		return $this;
	} // setApellidos()

	/**
	 * Set the value of [celular] column.
	 * 
	 * @param      string $v new value
	 * @return     Usuario The current object (for fluent API support)
	 */
	public function setCelular($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->celular !== $v) {
			$this->celular = $v;
			$this->modifiedColumns[] = UsuarioPeer::CELULAR;
		}

		return $this;
	} // setCelular()

	/**
	 * Set the value of [telefono] column.
	 * 
	 * @param      string $v new value
	 * @return     Usuario The current object (for fluent API support)
	 */
	public function setTelefono($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->telefono !== $v) {
			$this->telefono = $v;
			$this->modifiedColumns[] = UsuarioPeer::TELEFONO;
		}

		return $this;
	} // setTelefono()

	/**
	 * Set the value of [direccion] column.
	 * 
	 * @param      string $v new value
	 * @return     Usuario The current object (for fluent API support)
	 */
	public function setDireccion($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->direccion !== $v) {
			$this->direccion = $v;
			$this->modifiedColumns[] = UsuarioPeer::DIRECCION;
		}

		return $this;
	} // setDireccion()

	/**
	 * Set the value of [email] column.
	 * 
	 * @param      string $v new value
	 * @return     Usuario The current object (for fluent API support)
	 */
	public function setEmail($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->email !== $v) {
			$this->email = $v;
			$this->modifiedColumns[] = UsuarioPeer::EMAIL;
		}

		return $this;
	} // setEmail()

	/**
	 * Set the value of [clave] column.
	 * 
	 * @param      string $v new value
	 * @return     Usuario The current object (for fluent API support)
	 */
	public function setClave($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->clave !== $v) {
			$this->clave = $v;
			$this->modifiedColumns[] = UsuarioPeer::CLAVE;
		}

		return $this;
	} // setClave()

	/**
	 * Sets the value of [fechaalta] column to a normalized version of the date/time value specified.
	 * 
	 * @param      mixed $v string, integer (timestamp), or DateTime value.  Empty string will
	 *						be treated as NULL for temporal objects.
	 * @return     Usuario The current object (for fluent API support)
	 */
	public function setFechaalta($v)
	{
		// we treat '' as NULL for temporal objects because DateTime('') == DateTime('now')
		// -- which is unexpected, to say the least.
		if ($v === null || $v === '') {
			$dt = null;
		} elseif ($v instanceof DateTime) {
			$dt = $v;
		} else {
			// some string/numeric value passed; we normalize that so that we can
			// validate it.
			try {
				if (is_numeric($v)) { // if it's a unix timestamp
					$dt = new DateTime('@'.$v, new DateTimeZone('UTC'));
					// We have to explicitly specify and then change the time zone because of a
					// DateTime bug: http://bugs.php.net/bug.php?id=43003
					$dt->setTimeZone(new DateTimeZone(date_default_timezone_get()));
				} else {
					$dt = new DateTime($v);
				}
			} catch (Exception $x) {
				throw new PropelException('Error parsing date/time value: ' . var_export($v, true), $x);
			}
		}

		if ( $this->fechaalta !== null || $dt !== null ) {
			// (nested ifs are a little easier to read in this case)

			$currNorm = ($this->fechaalta !== null && $tmpDt = new DateTime($this->fechaalta)) ? $tmpDt->format('Y-m-d H:i:s') : null;
			$newNorm = ($dt !== null) ? $dt->format('Y-m-d H:i:s') : null;

			if ( ($currNorm !== $newNorm) // normalized values don't match 
					)
			{
				$this->fechaalta = ($dt ? $dt->format('Y-m-d H:i:s') : null);
				$this->modifiedColumns[] = UsuarioPeer::FECHAALTA;
			}
		} // if either are not null

		return $this;
	} // setFechaalta()

	/**
	 * Sets the value of [fechabaja] column to a normalized version of the date/time value specified.
	 * 
	 * @param      mixed $v string, integer (timestamp), or DateTime value.  Empty string will
	 *						be treated as NULL for temporal objects.
	 * @return     Usuario The current object (for fluent API support)
	 */
	public function setFechabaja($v)
	{
		// we treat '' as NULL for temporal objects because DateTime('') == DateTime('now')
		// -- which is unexpected, to say the least.
		if ($v === null || $v === '') {
			$dt = null;
		} elseif ($v instanceof DateTime) {
			$dt = $v;
		} else {
			// some string/numeric value passed; we normalize that so that we can
			// validate it.
			try {
				if (is_numeric($v)) { // if it's a unix timestamp
					$dt = new DateTime('@'.$v, new DateTimeZone('UTC'));
					// We have to explicitly specify and then change the time zone because of a
					// DateTime bug: http://bugs.php.net/bug.php?id=43003
					$dt->setTimeZone(new DateTimeZone(date_default_timezone_get()));
				} else {
					$dt = new DateTime($v);
				}
			} catch (Exception $x) {
				throw new PropelException('Error parsing date/time value: ' . var_export($v, true), $x);
			}
		}

		if ( $this->fechabaja !== null || $dt !== null ) {
			// (nested ifs are a little easier to read in this case)

			$currNorm = ($this->fechabaja !== null && $tmpDt = new DateTime($this->fechabaja)) ? $tmpDt->format('Y-m-d H:i:s') : null;
			$newNorm = ($dt !== null) ? $dt->format('Y-m-d H:i:s') : null;

			if ( ($currNorm !== $newNorm) // normalized values don't match 
					)
			{
				$this->fechabaja = ($dt ? $dt->format('Y-m-d H:i:s') : null);
				$this->modifiedColumns[] = UsuarioPeer::FECHABAJA;
			}
		} // if either are not null

		return $this;
	} // setFechabaja()

	/**
	 * Set the value of [habilitado] column.
	 * 
	 * @param      boolean $v new value
	 * @return     Usuario The current object (for fluent API support)
	 */
	public function setHabilitado($v)
	{
		if ($v !== null) {
			$v = (boolean) $v;
		}

		if ($this->habilitado !== $v || $this->isNew()) {
			$this->habilitado = $v;
			$this->modifiedColumns[] = UsuarioPeer::HABILITADO;
		}

		return $this;
	} // setHabilitado()

	/**
	 * Indicates whether the columns in this object are only set to default values.
	 *
	 * This method can be used in conjunction with isModified() to indicate whether an object is both
	 * modified _and_ has some values set which are non-default.
	 *
	 * @return     boolean Whether the columns in this object are only been set with default values.
	 */
	public function hasOnlyDefaultValues()
	{
			if ($this->habilitado !== true) {
				return false;
			}

		// otherwise, everything was equal, so return TRUE
		return true;
	} // hasOnlyDefaultValues()

	/**
	 * Hydrates (populates) the object variables with values from the database resultset.
	 *
	 * An offset (0-based "start column") is specified so that objects can be hydrated
	 * with a subset of the columns in the resultset rows.  This is needed, for example,
	 * for results of JOIN queries where the resultset row includes columns from two or
	 * more tables.
	 *
	 * @param      array $row The row returned by PDOStatement->fetch(PDO::FETCH_NUM)
	 * @param      int $startcol 0-based offset column which indicates which restultset column to start with.
	 * @param      boolean $rehydrate Whether this object is being re-hydrated from the database.
	 * @return     int next starting column
	 * @throws     PropelException  - Any caught Exception will be rewrapped as a PropelException.
	 */
	public function hydrate($row, $startcol = 0, $rehydrate = false)
	{
		try {

			$this->id = ($row[$startcol + 0] !== null) ? (int) $row[$startcol + 0] : null;
			$this->tipo = ($row[$startcol + 1] !== null) ? (string) $row[$startcol + 1] : null;
			$this->nombre = ($row[$startcol + 2] !== null) ? (string) $row[$startcol + 2] : null;
			$this->apellidos = ($row[$startcol + 3] !== null) ? (string) $row[$startcol + 3] : null;
			$this->celular = ($row[$startcol + 4] !== null) ? (string) $row[$startcol + 4] : null;
			$this->telefono = ($row[$startcol + 5] !== null) ? (string) $row[$startcol + 5] : null;
			$this->direccion = ($row[$startcol + 6] !== null) ? (string) $row[$startcol + 6] : null;
			$this->email = ($row[$startcol + 7] !== null) ? (string) $row[$startcol + 7] : null;
			$this->clave = ($row[$startcol + 8] !== null) ? (string) $row[$startcol + 8] : null;
			$this->fechaalta = ($row[$startcol + 9] !== null) ? (string) $row[$startcol + 9] : null;
			$this->fechabaja = ($row[$startcol + 10] !== null) ? (string) $row[$startcol + 10] : null;
			$this->habilitado = ($row[$startcol + 11] !== null) ? (boolean) $row[$startcol + 11] : null;
			$this->resetModified();

			$this->setNew(false);

			if ($rehydrate) {
				$this->ensureConsistency();
			}

			// FIXME - using NUM_COLUMNS may be clearer.
			return $startcol + 12; // 12 = UsuarioPeer::NUM_COLUMNS - UsuarioPeer::NUM_LAZY_LOAD_COLUMNS).

		} catch (Exception $e) {
			throw new PropelException("Error populating Usuario object", $e);
		}
	}

	/**
	 * Checks and repairs the internal consistency of the object.
	 *
	 * This method is executed after an already-instantiated object is re-hydrated
	 * from the database.  It exists to check any foreign keys to make sure that
	 * the objects related to the current object are correct based on foreign key.
	 *
	 * You can override this method in the stub class, but you should always invoke
	 * the base method from the overridden method (i.e. parent::ensureConsistency()),
	 * in case your model changes.
	 *
	 * @throws     PropelException
	 */
	public function ensureConsistency()
	{

	} // ensureConsistency

	/**
	 * Reloads this object from datastore based on primary key and (optionally) resets all associated objects.
	 *
	 * This will only work if the object has been saved and has a valid primary key set.
	 *
	 * @param      boolean $deep (optional) Whether to also de-associated any related objects.
	 * @param      PropelPDO $con (optional) The PropelPDO connection to use.
	 * @return     void
	 * @throws     PropelException - if this object is deleted, unsaved or doesn't have pk match in db
	 */
	public function reload($deep = false, PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("Cannot reload a deleted object.");
		}

		if ($this->isNew()) {
			throw new PropelException("Cannot reload an unsaved object.");
		}

		if ($con === null) {
			$con = Propel::getConnection(UsuarioPeer::DATABASE_NAME, Propel::CONNECTION_READ);
		}

		// We don't need to alter the object instance pool; we're just modifying this instance
		// already in the pool.

		$stmt = UsuarioPeer::doSelectStmt($this->buildPkeyCriteria(), $con);
		$row = $stmt->fetch(PDO::FETCH_NUM);
		$stmt->closeCursor();
		if (!$row) {
			throw new PropelException('Cannot find matching row in the database to reload object values.');
		}
		$this->hydrate($row, 0, true); // rehydrate

		if ($deep) {  // also de-associate any related objects?

			$this->collPropietarios = null;
			$this->lastPropietarioCriteria = null;

			$this->collBancos = null;
			$this->lastBancoCriteria = null;

			$this->collEmpresas = null;
			$this->lastEmpresaCriteria = null;

			$this->collTallers = null;
			$this->lastTallerCriteria = null;

			$this->collAseguradoras = null;
			$this->lastAseguradoraCriteria = null;

			$this->collDespachos = null;
			$this->lastDespachoCriteria = null;

			$this->collChofers = null;
			$this->lastChoferCriteria = null;

			$this->collMovils = null;
			$this->lastMovilCriteria = null;

			$this->collMultas = null;
			$this->lastMultaCriteria = null;

			$this->collAccidentes = null;
			$this->lastAccidenteCriteria = null;

			$this->collRecaudacions = null;
			$this->lastRecaudacionCriteria = null;

			$this->collGastorecaudacions = null;
			$this->lastGastorecaudacionCriteria = null;

			$this->collGastomovils = null;
			$this->lastGastomovilCriteria = null;

			$this->collGastoempresas = null;
			$this->lastGastoempresaCriteria = null;

			$this->collConfiguracions = null;
			$this->lastConfiguracionCriteria = null;

			$this->collPagoaseguradoras = null;
			$this->lastPagoaseguradoraCriteria = null;

			$this->collTrabajotallers = null;
			$this->lastTrabajotallerCriteria = null;

			$this->collAdelantos = null;
			$this->lastAdelantoCriteria = null;

			$this->collPagoadelantos = null;
			$this->lastPagoadelantoCriteria = null;

			$this->collEmpresaPropietarios = null;
			$this->lastEmpresaPropietarioCriteria = null;

			$this->collMovilAseguradoras = null;
			$this->lastMovilAseguradoraCriteria = null;

			$this->collMovilEmpresas = null;
			$this->lastMovilEmpresaCriteria = null;

			$this->collChoferEmpresas = null;
			$this->lastChoferEmpresaCriteria = null;

		} // if (deep)
	}

	/**
	 * Removes this object from datastore and sets delete attribute.
	 *
	 * @param      PropelPDO $con
	 * @return     void
	 * @throws     PropelException
	 * @see        BaseObject::setDeleted()
	 * @see        BaseObject::isDeleted()
	 */
	public function delete(PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("This object has already been deleted.");
		}

		if ($con === null) {
			$con = Propel::getConnection(UsuarioPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}
		
		$con->beginTransaction();
		try {
			$ret = $this->preDelete($con);
			// symfony_behaviors behavior
			foreach (sfMixer::getCallables('BaseUsuario:delete:pre') as $callable)
			{
			  if (call_user_func($callable, $this, $con))
			  {
			    $con->commit();
			
			    return;
			  }
			}

			if ($ret) {
				UsuarioPeer::doDelete($this, $con);
				$this->postDelete($con);
				// symfony_behaviors behavior
				foreach (sfMixer::getCallables('BaseUsuario:delete:post') as $callable)
				{
				  call_user_func($callable, $this, $con);
				}

				$this->setDeleted(true);
				$con->commit();
			} else {
				$con->commit();
			}
		} catch (PropelException $e) {
			$con->rollBack();
			throw $e;
		}
	}

	/**
	 * Persists this object to the database.
	 *
	 * If the object is new, it inserts it; otherwise an update is performed.
	 * All modified related objects will also be persisted in the doSave()
	 * method.  This method wraps all precipitate database operations in a
	 * single transaction.
	 *
	 * @param      PropelPDO $con
	 * @return     int The number of rows affected by this insert/update and any referring fk objects' save() operations.
	 * @throws     PropelException
	 * @see        doSave()
	 */
	public function save(PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("You cannot save an object that has been deleted.");
		}

		if ($con === null) {
			$con = Propel::getConnection(UsuarioPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}
		
		$con->beginTransaction();
		$isInsert = $this->isNew();
		try {
			$ret = $this->preSave($con);
			// symfony_behaviors behavior
			foreach (sfMixer::getCallables('BaseUsuario:save:pre') as $callable)
			{
			  if (is_integer($affectedRows = call_user_func($callable, $this, $con)))
			  {
			    $con->commit();
			
			    return $affectedRows;
			  }
			}

			if ($isInsert) {
				$ret = $ret && $this->preInsert($con);
			} else {
				$ret = $ret && $this->preUpdate($con);
			}
			if ($ret) {
				$affectedRows = $this->doSave($con);
				if ($isInsert) {
					$this->postInsert($con);
				} else {
					$this->postUpdate($con);
				}
				$this->postSave($con);
				// symfony_behaviors behavior
				foreach (sfMixer::getCallables('BaseUsuario:save:post') as $callable)
				{
				  call_user_func($callable, $this, $con, $affectedRows);
				}

				UsuarioPeer::addInstanceToPool($this);
			} else {
				$affectedRows = 0;
			}
			$con->commit();
			return $affectedRows;
		} catch (PropelException $e) {
			$con->rollBack();
			throw $e;
		}
	}

	/**
	 * Performs the work of inserting or updating the row in the database.
	 *
	 * If the object is new, it inserts it; otherwise an update is performed.
	 * All related objects are also updated in this method.
	 *
	 * @param      PropelPDO $con
	 * @return     int The number of rows affected by this insert/update and any referring fk objects' save() operations.
	 * @throws     PropelException
	 * @see        save()
	 */
	protected function doSave(PropelPDO $con)
	{
		$affectedRows = 0; // initialize var to track total num of affected rows
		if (!$this->alreadyInSave) {
			$this->alreadyInSave = true;

			if ($this->isNew() ) {
				$this->modifiedColumns[] = UsuarioPeer::ID;
			}

			// If this object has been modified, then save it to the database.
			if ($this->isModified()) {
				if ($this->isNew()) {
					$pk = UsuarioPeer::doInsert($this, $con);
					$affectedRows += 1; // we are assuming that there is only 1 row per doInsert() which
										 // should always be true here (even though technically
										 // BasePeer::doInsert() can insert multiple rows).

					$this->setId($pk);  //[IMV] update autoincrement primary key

					$this->setNew(false);
				} else {
					$affectedRows += UsuarioPeer::doUpdate($this, $con);
				}

				$this->resetModified(); // [HL] After being saved an object is no longer 'modified'
			}

			if ($this->collPropietarios !== null) {
				foreach ($this->collPropietarios as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collBancos !== null) {
				foreach ($this->collBancos as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collEmpresas !== null) {
				foreach ($this->collEmpresas as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collTallers !== null) {
				foreach ($this->collTallers as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collAseguradoras !== null) {
				foreach ($this->collAseguradoras as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collDespachos !== null) {
				foreach ($this->collDespachos as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collChofers !== null) {
				foreach ($this->collChofers as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collMovils !== null) {
				foreach ($this->collMovils as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collMultas !== null) {
				foreach ($this->collMultas as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collAccidentes !== null) {
				foreach ($this->collAccidentes as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collRecaudacions !== null) {
				foreach ($this->collRecaudacions as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collGastorecaudacions !== null) {
				foreach ($this->collGastorecaudacions as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collGastomovils !== null) {
				foreach ($this->collGastomovils as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collGastoempresas !== null) {
				foreach ($this->collGastoempresas as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collConfiguracions !== null) {
				foreach ($this->collConfiguracions as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collPagoaseguradoras !== null) {
				foreach ($this->collPagoaseguradoras as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collTrabajotallers !== null) {
				foreach ($this->collTrabajotallers as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collAdelantos !== null) {
				foreach ($this->collAdelantos as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collPagoadelantos !== null) {
				foreach ($this->collPagoadelantos as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collEmpresaPropietarios !== null) {
				foreach ($this->collEmpresaPropietarios as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collMovilAseguradoras !== null) {
				foreach ($this->collMovilAseguradoras as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collMovilEmpresas !== null) {
				foreach ($this->collMovilEmpresas as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collChoferEmpresas !== null) {
				foreach ($this->collChoferEmpresas as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			$this->alreadyInSave = false;

		}
		return $affectedRows;
	} // doSave()

	/**
	 * Array of ValidationFailed objects.
	 * @var        array ValidationFailed[]
	 */
	protected $validationFailures = array();

	/**
	 * Gets any ValidationFailed objects that resulted from last call to validate().
	 *
	 *
	 * @return     array ValidationFailed[]
	 * @see        validate()
	 */
	public function getValidationFailures()
	{
		return $this->validationFailures;
	}

	/**
	 * Validates the objects modified field values and all objects related to this table.
	 *
	 * If $columns is either a column name or an array of column names
	 * only those columns are validated.
	 *
	 * @param      mixed $columns Column name or an array of column names.
	 * @return     boolean Whether all columns pass validation.
	 * @see        doValidate()
	 * @see        getValidationFailures()
	 */
	public function validate($columns = null)
	{
		$res = $this->doValidate($columns);
		if ($res === true) {
			$this->validationFailures = array();
			return true;
		} else {
			$this->validationFailures = $res;
			return false;
		}
	}

	/**
	 * This function performs the validation work for complex object models.
	 *
	 * In addition to checking the current object, all related objects will
	 * also be validated.  If all pass then <code>true</code> is returned; otherwise
	 * an aggreagated array of ValidationFailed objects will be returned.
	 *
	 * @param      array $columns Array of column names to validate.
	 * @return     mixed <code>true</code> if all validations pass; array of <code>ValidationFailed</code> objets otherwise.
	 */
	protected function doValidate($columns = null)
	{
		if (!$this->alreadyInValidation) {
			$this->alreadyInValidation = true;
			$retval = null;

			$failureMap = array();


			if (($retval = UsuarioPeer::doValidate($this, $columns)) !== true) {
				$failureMap = array_merge($failureMap, $retval);
			}


				if ($this->collPropietarios !== null) {
					foreach ($this->collPropietarios as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collBancos !== null) {
					foreach ($this->collBancos as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collEmpresas !== null) {
					foreach ($this->collEmpresas as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collTallers !== null) {
					foreach ($this->collTallers as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collAseguradoras !== null) {
					foreach ($this->collAseguradoras as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collDespachos !== null) {
					foreach ($this->collDespachos as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collChofers !== null) {
					foreach ($this->collChofers as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collMovils !== null) {
					foreach ($this->collMovils as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collMultas !== null) {
					foreach ($this->collMultas as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collAccidentes !== null) {
					foreach ($this->collAccidentes as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collRecaudacions !== null) {
					foreach ($this->collRecaudacions as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collGastorecaudacions !== null) {
					foreach ($this->collGastorecaudacions as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collGastomovils !== null) {
					foreach ($this->collGastomovils as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collGastoempresas !== null) {
					foreach ($this->collGastoempresas as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collConfiguracions !== null) {
					foreach ($this->collConfiguracions as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collPagoaseguradoras !== null) {
					foreach ($this->collPagoaseguradoras as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collTrabajotallers !== null) {
					foreach ($this->collTrabajotallers as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collAdelantos !== null) {
					foreach ($this->collAdelantos as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collPagoadelantos !== null) {
					foreach ($this->collPagoadelantos as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collEmpresaPropietarios !== null) {
					foreach ($this->collEmpresaPropietarios as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collMovilAseguradoras !== null) {
					foreach ($this->collMovilAseguradoras as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collMovilEmpresas !== null) {
					foreach ($this->collMovilEmpresas as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collChoferEmpresas !== null) {
					foreach ($this->collChoferEmpresas as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}


			$this->alreadyInValidation = false;
		}

		return (!empty($failureMap) ? $failureMap : true);
	}

	/**
	 * Retrieves a field from the object by name passed in as a string.
	 *
	 * @param      string $name name
	 * @param      string $type The type of fieldname the $name is of:
	 *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
	 * @return     mixed Value of field.
	 */
	public function getByName($name, $type = BasePeer::TYPE_PHPNAME)
	{
		$pos = UsuarioPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
		$field = $this->getByPosition($pos);
		return $field;
	}

	/**
	 * Retrieves a field from the object by Position as specified in the xml schema.
	 * Zero-based.
	 *
	 * @param      int $pos position in xml schema
	 * @return     mixed Value of field at $pos
	 */
	public function getByPosition($pos)
	{
		switch($pos) {
			case 0:
				return $this->getId();
				break;
			case 1:
				return $this->getTipo();
				break;
			case 2:
				return $this->getNombre();
				break;
			case 3:
				return $this->getApellidos();
				break;
			case 4:
				return $this->getCelular();
				break;
			case 5:
				return $this->getTelefono();
				break;
			case 6:
				return $this->getDireccion();
				break;
			case 7:
				return $this->getEmail();
				break;
			case 8:
				return $this->getClave();
				break;
			case 9:
				return $this->getFechaalta();
				break;
			case 10:
				return $this->getFechabaja();
				break;
			case 11:
				return $this->getHabilitado();
				break;
			default:
				return null;
				break;
		} // switch()
	}

	/**
	 * Exports the object as an array.
	 *
	 * You can specify the key type of the array by passing one of the class
	 * type constants.
	 *
	 * @param      string $keyType (optional) One of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                        BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM. Defaults to BasePeer::TYPE_PHPNAME.
	 * @param      boolean $includeLazyLoadColumns (optional) Whether to include lazy loaded columns.  Defaults to TRUE.
	 * @return     an associative array containing the field names (as keys) and field values
	 */
	public function toArray($keyType = BasePeer::TYPE_PHPNAME, $includeLazyLoadColumns = true)
	{
		$keys = UsuarioPeer::getFieldNames($keyType);
		$result = array(
			$keys[0] => $this->getId(),
			$keys[1] => $this->getTipo(),
			$keys[2] => $this->getNombre(),
			$keys[3] => $this->getApellidos(),
			$keys[4] => $this->getCelular(),
			$keys[5] => $this->getTelefono(),
			$keys[6] => $this->getDireccion(),
			$keys[7] => $this->getEmail(),
			$keys[8] => $this->getClave(),
			$keys[9] => $this->getFechaalta(),
			$keys[10] => $this->getFechabaja(),
			$keys[11] => $this->getHabilitado(),
		);
		return $result;
	}

	/**
	 * Sets a field from the object by name passed in as a string.
	 *
	 * @param      string $name peer name
	 * @param      mixed $value field value
	 * @param      string $type The type of fieldname the $name is of:
	 *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
	 * @return     void
	 */
	public function setByName($name, $value, $type = BasePeer::TYPE_PHPNAME)
	{
		$pos = UsuarioPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
		return $this->setByPosition($pos, $value);
	}

	/**
	 * Sets a field from the object by Position as specified in the xml schema.
	 * Zero-based.
	 *
	 * @param      int $pos position in xml schema
	 * @param      mixed $value field value
	 * @return     void
	 */
	public function setByPosition($pos, $value)
	{
		switch($pos) {
			case 0:
				$this->setId($value);
				break;
			case 1:
				$this->setTipo($value);
				break;
			case 2:
				$this->setNombre($value);
				break;
			case 3:
				$this->setApellidos($value);
				break;
			case 4:
				$this->setCelular($value);
				break;
			case 5:
				$this->setTelefono($value);
				break;
			case 6:
				$this->setDireccion($value);
				break;
			case 7:
				$this->setEmail($value);
				break;
			case 8:
				$this->setClave($value);
				break;
			case 9:
				$this->setFechaalta($value);
				break;
			case 10:
				$this->setFechabaja($value);
				break;
			case 11:
				$this->setHabilitado($value);
				break;
		} // switch()
	}

	/**
	 * Populates the object using an array.
	 *
	 * This is particularly useful when populating an object from one of the
	 * request arrays (e.g. $_POST).  This method goes through the column
	 * names, checking to see whether a matching key exists in populated
	 * array. If so the setByName() method is called for that column.
	 *
	 * You can specify the key type of the array by additionally passing one
	 * of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME,
	 * BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
	 * The default key type is the column's phpname (e.g. 'AuthorId')
	 *
	 * @param      array  $arr     An array to populate the object from.
	 * @param      string $keyType The type of keys the array uses.
	 * @return     void
	 */
	public function fromArray($arr, $keyType = BasePeer::TYPE_PHPNAME)
	{
		$keys = UsuarioPeer::getFieldNames($keyType);

		if (array_key_exists($keys[0], $arr)) $this->setId($arr[$keys[0]]);
		if (array_key_exists($keys[1], $arr)) $this->setTipo($arr[$keys[1]]);
		if (array_key_exists($keys[2], $arr)) $this->setNombre($arr[$keys[2]]);
		if (array_key_exists($keys[3], $arr)) $this->setApellidos($arr[$keys[3]]);
		if (array_key_exists($keys[4], $arr)) $this->setCelular($arr[$keys[4]]);
		if (array_key_exists($keys[5], $arr)) $this->setTelefono($arr[$keys[5]]);
		if (array_key_exists($keys[6], $arr)) $this->setDireccion($arr[$keys[6]]);
		if (array_key_exists($keys[7], $arr)) $this->setEmail($arr[$keys[7]]);
		if (array_key_exists($keys[8], $arr)) $this->setClave($arr[$keys[8]]);
		if (array_key_exists($keys[9], $arr)) $this->setFechaalta($arr[$keys[9]]);
		if (array_key_exists($keys[10], $arr)) $this->setFechabaja($arr[$keys[10]]);
		if (array_key_exists($keys[11], $arr)) $this->setHabilitado($arr[$keys[11]]);
	}

	/**
	 * Build a Criteria object containing the values of all modified columns in this object.
	 *
	 * @return     Criteria The Criteria object containing all modified values.
	 */
	public function buildCriteria()
	{
		$criteria = new Criteria(UsuarioPeer::DATABASE_NAME);

		if ($this->isColumnModified(UsuarioPeer::ID)) $criteria->add(UsuarioPeer::ID, $this->id);
		if ($this->isColumnModified(UsuarioPeer::TIPO)) $criteria->add(UsuarioPeer::TIPO, $this->tipo);
		if ($this->isColumnModified(UsuarioPeer::NOMBRE)) $criteria->add(UsuarioPeer::NOMBRE, $this->nombre);
		if ($this->isColumnModified(UsuarioPeer::APELLIDOS)) $criteria->add(UsuarioPeer::APELLIDOS, $this->apellidos);
		if ($this->isColumnModified(UsuarioPeer::CELULAR)) $criteria->add(UsuarioPeer::CELULAR, $this->celular);
		if ($this->isColumnModified(UsuarioPeer::TELEFONO)) $criteria->add(UsuarioPeer::TELEFONO, $this->telefono);
		if ($this->isColumnModified(UsuarioPeer::DIRECCION)) $criteria->add(UsuarioPeer::DIRECCION, $this->direccion);
		if ($this->isColumnModified(UsuarioPeer::EMAIL)) $criteria->add(UsuarioPeer::EMAIL, $this->email);
		if ($this->isColumnModified(UsuarioPeer::CLAVE)) $criteria->add(UsuarioPeer::CLAVE, $this->clave);
		if ($this->isColumnModified(UsuarioPeer::FECHAALTA)) $criteria->add(UsuarioPeer::FECHAALTA, $this->fechaalta);
		if ($this->isColumnModified(UsuarioPeer::FECHABAJA)) $criteria->add(UsuarioPeer::FECHABAJA, $this->fechabaja);
		if ($this->isColumnModified(UsuarioPeer::HABILITADO)) $criteria->add(UsuarioPeer::HABILITADO, $this->habilitado);

		return $criteria;
	}

	/**
	 * Builds a Criteria object containing the primary key for this object.
	 *
	 * Unlike buildCriteria() this method includes the primary key values regardless
	 * of whether or not they have been modified.
	 *
	 * @return     Criteria The Criteria object containing value(s) for primary key(s).
	 */
	public function buildPkeyCriteria()
	{
		$criteria = new Criteria(UsuarioPeer::DATABASE_NAME);

		$criteria->add(UsuarioPeer::ID, $this->id);

		return $criteria;
	}

	/**
	 * Returns the primary key for this object (row).
	 * @return     int
	 */
	public function getPrimaryKey()
	{
		return $this->getId();
	}

	/**
	 * Generic method to set the primary key (id column).
	 *
	 * @param      int $key Primary key.
	 * @return     void
	 */
	public function setPrimaryKey($key)
	{
		$this->setId($key);
	}

	/**
	 * Sets contents of passed object to values from current object.
	 *
	 * If desired, this method can also make copies of all associated (fkey referrers)
	 * objects.
	 *
	 * @param      object $copyObj An object of Usuario (or compatible) type.
	 * @param      boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
	 * @throws     PropelException
	 */
	public function copyInto($copyObj, $deepCopy = false)
	{

		$copyObj->setTipo($this->tipo);

		$copyObj->setNombre($this->nombre);

		$copyObj->setApellidos($this->apellidos);

		$copyObj->setCelular($this->celular);

		$copyObj->setTelefono($this->telefono);

		$copyObj->setDireccion($this->direccion);

		$copyObj->setEmail($this->email);

		$copyObj->setClave($this->clave);

		$copyObj->setFechaalta($this->fechaalta);

		$copyObj->setFechabaja($this->fechabaja);

		$copyObj->setHabilitado($this->habilitado);


		if ($deepCopy) {
			// important: temporarily setNew(false) because this affects the behavior of
			// the getter/setter methods for fkey referrer objects.
			$copyObj->setNew(false);

			foreach ($this->getPropietarios() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addPropietario($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getBancos() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addBanco($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getEmpresas() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addEmpresa($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getTallers() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addTaller($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getAseguradoras() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addAseguradora($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getDespachos() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addDespacho($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getChofers() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addChofer($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getMovils() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addMovil($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getMultas() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addMulta($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getAccidentes() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addAccidente($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getRecaudacions() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addRecaudacion($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getGastorecaudacions() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addGastorecaudacion($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getGastomovils() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addGastomovil($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getGastoempresas() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addGastoempresa($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getConfiguracions() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addConfiguracion($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getPagoaseguradoras() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addPagoaseguradora($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getTrabajotallers() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addTrabajotaller($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getAdelantos() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addAdelanto($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getPagoadelantos() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addPagoadelanto($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getEmpresaPropietarios() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addEmpresaPropietario($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getMovilAseguradoras() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addMovilAseguradora($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getMovilEmpresas() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addMovilEmpresa($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getChoferEmpresas() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addChoferEmpresa($relObj->copy($deepCopy));
				}
			}

		} // if ($deepCopy)


		$copyObj->setNew(true);

		$copyObj->setId(NULL); // this is a auto-increment column, so set to default value

	}

	/**
	 * Makes a copy of this object that will be inserted as a new row in table when saved.
	 * It creates a new object filling in the simple attributes, but skipping any primary
	 * keys that are defined for the table.
	 *
	 * If desired, this method can also make copies of all associated (fkey referrers)
	 * objects.
	 *
	 * @param      boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
	 * @return     Usuario Clone of current object.
	 * @throws     PropelException
	 */
	public function copy($deepCopy = false)
	{
		// we use get_class(), because this might be a subclass
		$clazz = get_class($this);
		$copyObj = new $clazz();
		$this->copyInto($copyObj, $deepCopy);
		return $copyObj;
	}

	/**
	 * Returns a peer instance associated with this om.
	 *
	 * Since Peer classes are not to have any instance attributes, this method returns the
	 * same instance for all member of this class. The method could therefore
	 * be static, but this would prevent one from overriding the behavior.
	 *
	 * @return     UsuarioPeer
	 */
	public function getPeer()
	{
		if (self::$peer === null) {
			self::$peer = new UsuarioPeer();
		}
		return self::$peer;
	}

	/**
	 * Clears out the collPropietarios collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addPropietarios()
	 */
	public function clearPropietarios()
	{
		$this->collPropietarios = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collPropietarios collection (array).
	 *
	 * By default this just sets the collPropietarios collection to an empty array (like clearcollPropietarios());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initPropietarios()
	{
		$this->collPropietarios = array();
	}

	/**
	 * Gets an array of Propietario objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Usuario has previously been saved, it will retrieve
	 * related Propietarios from storage. If this Usuario is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array Propietario[]
	 * @throws     PropelException
	 */
	public function getPropietarios($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(UsuarioPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collPropietarios === null) {
			if ($this->isNew()) {
			   $this->collPropietarios = array();
			} else {

				$criteria->add(PropietarioPeer::USUARIO, $this->id);

				PropietarioPeer::addSelectColumns($criteria);
				$this->collPropietarios = PropietarioPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(PropietarioPeer::USUARIO, $this->id);

				PropietarioPeer::addSelectColumns($criteria);
				if (!isset($this->lastPropietarioCriteria) || !$this->lastPropietarioCriteria->equals($criteria)) {
					$this->collPropietarios = PropietarioPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastPropietarioCriteria = $criteria;
		return $this->collPropietarios;
	}

	/**
	 * Returns the number of related Propietario objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related Propietario objects.
	 * @throws     PropelException
	 */
	public function countPropietarios(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(UsuarioPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collPropietarios === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(PropietarioPeer::USUARIO, $this->id);

				$count = PropietarioPeer::doCount($criteria, false, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(PropietarioPeer::USUARIO, $this->id);

				if (!isset($this->lastPropietarioCriteria) || !$this->lastPropietarioCriteria->equals($criteria)) {
					$count = PropietarioPeer::doCount($criteria, false, $con);
				} else {
					$count = count($this->collPropietarios);
				}
			} else {
				$count = count($this->collPropietarios);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a Propietario object to this object
	 * through the Propietario foreign key attribute.
	 *
	 * @param      Propietario $l Propietario
	 * @return     void
	 * @throws     PropelException
	 */
	public function addPropietario(Propietario $l)
	{
		if ($this->collPropietarios === null) {
			$this->initPropietarios();
		}
		if (!in_array($l, $this->collPropietarios, true)) { // only add it if the **same** object is not already associated
			array_push($this->collPropietarios, $l);
			$l->setUsuarioRelatedByUsuario($this);
		}
	}

	/**
	 * Clears out the collBancos collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addBancos()
	 */
	public function clearBancos()
	{
		$this->collBancos = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collBancos collection (array).
	 *
	 * By default this just sets the collBancos collection to an empty array (like clearcollBancos());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initBancos()
	{
		$this->collBancos = array();
	}

	/**
	 * Gets an array of Banco objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Usuario has previously been saved, it will retrieve
	 * related Bancos from storage. If this Usuario is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array Banco[]
	 * @throws     PropelException
	 */
	public function getBancos($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(UsuarioPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collBancos === null) {
			if ($this->isNew()) {
			   $this->collBancos = array();
			} else {

				$criteria->add(BancoPeer::USUARIO, $this->id);

				BancoPeer::addSelectColumns($criteria);
				$this->collBancos = BancoPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(BancoPeer::USUARIO, $this->id);

				BancoPeer::addSelectColumns($criteria);
				if (!isset($this->lastBancoCriteria) || !$this->lastBancoCriteria->equals($criteria)) {
					$this->collBancos = BancoPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastBancoCriteria = $criteria;
		return $this->collBancos;
	}

	/**
	 * Returns the number of related Banco objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related Banco objects.
	 * @throws     PropelException
	 */
	public function countBancos(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(UsuarioPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collBancos === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(BancoPeer::USUARIO, $this->id);

				$count = BancoPeer::doCount($criteria, false, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(BancoPeer::USUARIO, $this->id);

				if (!isset($this->lastBancoCriteria) || !$this->lastBancoCriteria->equals($criteria)) {
					$count = BancoPeer::doCount($criteria, false, $con);
				} else {
					$count = count($this->collBancos);
				}
			} else {
				$count = count($this->collBancos);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a Banco object to this object
	 * through the Banco foreign key attribute.
	 *
	 * @param      Banco $l Banco
	 * @return     void
	 * @throws     PropelException
	 */
	public function addBanco(Banco $l)
	{
		if ($this->collBancos === null) {
			$this->initBancos();
		}
		if (!in_array($l, $this->collBancos, true)) { // only add it if the **same** object is not already associated
			array_push($this->collBancos, $l);
			$l->setUsuarioRelatedByUsuario($this);
		}
	}

	/**
	 * Clears out the collEmpresas collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addEmpresas()
	 */
	public function clearEmpresas()
	{
		$this->collEmpresas = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collEmpresas collection (array).
	 *
	 * By default this just sets the collEmpresas collection to an empty array (like clearcollEmpresas());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initEmpresas()
	{
		$this->collEmpresas = array();
	}

	/**
	 * Gets an array of Empresa objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Usuario has previously been saved, it will retrieve
	 * related Empresas from storage. If this Usuario is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array Empresa[]
	 * @throws     PropelException
	 */
	public function getEmpresas($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(UsuarioPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collEmpresas === null) {
			if ($this->isNew()) {
			   $this->collEmpresas = array();
			} else {

				$criteria->add(EmpresaPeer::USUARIO, $this->id);

				EmpresaPeer::addSelectColumns($criteria);
				$this->collEmpresas = EmpresaPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(EmpresaPeer::USUARIO, $this->id);

				EmpresaPeer::addSelectColumns($criteria);
				if (!isset($this->lastEmpresaCriteria) || !$this->lastEmpresaCriteria->equals($criteria)) {
					$this->collEmpresas = EmpresaPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastEmpresaCriteria = $criteria;
		return $this->collEmpresas;
	}

	/**
	 * Returns the number of related Empresa objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related Empresa objects.
	 * @throws     PropelException
	 */
	public function countEmpresas(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(UsuarioPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collEmpresas === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(EmpresaPeer::USUARIO, $this->id);

				$count = EmpresaPeer::doCount($criteria, false, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(EmpresaPeer::USUARIO, $this->id);

				if (!isset($this->lastEmpresaCriteria) || !$this->lastEmpresaCriteria->equals($criteria)) {
					$count = EmpresaPeer::doCount($criteria, false, $con);
				} else {
					$count = count($this->collEmpresas);
				}
			} else {
				$count = count($this->collEmpresas);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a Empresa object to this object
	 * through the Empresa foreign key attribute.
	 *
	 * @param      Empresa $l Empresa
	 * @return     void
	 * @throws     PropelException
	 */
	public function addEmpresa(Empresa $l)
	{
		if ($this->collEmpresas === null) {
			$this->initEmpresas();
		}
		if (!in_array($l, $this->collEmpresas, true)) { // only add it if the **same** object is not already associated
			array_push($this->collEmpresas, $l);
			$l->setUsuarioRelatedByUsuario($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Usuario is new, it will return
	 * an empty collection; or if this Usuario has previously
	 * been saved, it will retrieve related Empresas from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Usuario.
	 */
	public function getEmpresasJoinBanco($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(UsuarioPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collEmpresas === null) {
			if ($this->isNew()) {
				$this->collEmpresas = array();
			} else {

				$criteria->add(EmpresaPeer::USUARIO, $this->id);

				$this->collEmpresas = EmpresaPeer::doSelectJoinBanco($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(EmpresaPeer::USUARIO, $this->id);

			if (!isset($this->lastEmpresaCriteria) || !$this->lastEmpresaCriteria->equals($criteria)) {
				$this->collEmpresas = EmpresaPeer::doSelectJoinBanco($criteria, $con, $join_behavior);
			}
		}
		$this->lastEmpresaCriteria = $criteria;

		return $this->collEmpresas;
	}

	/**
	 * Clears out the collTallers collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addTallers()
	 */
	public function clearTallers()
	{
		$this->collTallers = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collTallers collection (array).
	 *
	 * By default this just sets the collTallers collection to an empty array (like clearcollTallers());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initTallers()
	{
		$this->collTallers = array();
	}

	/**
	 * Gets an array of Taller objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Usuario has previously been saved, it will retrieve
	 * related Tallers from storage. If this Usuario is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array Taller[]
	 * @throws     PropelException
	 */
	public function getTallers($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(UsuarioPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collTallers === null) {
			if ($this->isNew()) {
			   $this->collTallers = array();
			} else {

				$criteria->add(TallerPeer::USUARIO, $this->id);

				TallerPeer::addSelectColumns($criteria);
				$this->collTallers = TallerPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(TallerPeer::USUARIO, $this->id);

				TallerPeer::addSelectColumns($criteria);
				if (!isset($this->lastTallerCriteria) || !$this->lastTallerCriteria->equals($criteria)) {
					$this->collTallers = TallerPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastTallerCriteria = $criteria;
		return $this->collTallers;
	}

	/**
	 * Returns the number of related Taller objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related Taller objects.
	 * @throws     PropelException
	 */
	public function countTallers(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(UsuarioPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collTallers === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(TallerPeer::USUARIO, $this->id);

				$count = TallerPeer::doCount($criteria, false, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(TallerPeer::USUARIO, $this->id);

				if (!isset($this->lastTallerCriteria) || !$this->lastTallerCriteria->equals($criteria)) {
					$count = TallerPeer::doCount($criteria, false, $con);
				} else {
					$count = count($this->collTallers);
				}
			} else {
				$count = count($this->collTallers);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a Taller object to this object
	 * through the Taller foreign key attribute.
	 *
	 * @param      Taller $l Taller
	 * @return     void
	 * @throws     PropelException
	 */
	public function addTaller(Taller $l)
	{
		if ($this->collTallers === null) {
			$this->initTallers();
		}
		if (!in_array($l, $this->collTallers, true)) { // only add it if the **same** object is not already associated
			array_push($this->collTallers, $l);
			$l->setUsuarioRelatedByUsuario($this);
		}
	}

	/**
	 * Clears out the collAseguradoras collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addAseguradoras()
	 */
	public function clearAseguradoras()
	{
		$this->collAseguradoras = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collAseguradoras collection (array).
	 *
	 * By default this just sets the collAseguradoras collection to an empty array (like clearcollAseguradoras());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initAseguradoras()
	{
		$this->collAseguradoras = array();
	}

	/**
	 * Gets an array of Aseguradora objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Usuario has previously been saved, it will retrieve
	 * related Aseguradoras from storage. If this Usuario is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array Aseguradora[]
	 * @throws     PropelException
	 */
	public function getAseguradoras($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(UsuarioPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collAseguradoras === null) {
			if ($this->isNew()) {
			   $this->collAseguradoras = array();
			} else {

				$criteria->add(AseguradoraPeer::USUARIO, $this->id);

				AseguradoraPeer::addSelectColumns($criteria);
				$this->collAseguradoras = AseguradoraPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(AseguradoraPeer::USUARIO, $this->id);

				AseguradoraPeer::addSelectColumns($criteria);
				if (!isset($this->lastAseguradoraCriteria) || !$this->lastAseguradoraCriteria->equals($criteria)) {
					$this->collAseguradoras = AseguradoraPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastAseguradoraCriteria = $criteria;
		return $this->collAseguradoras;
	}

	/**
	 * Returns the number of related Aseguradora objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related Aseguradora objects.
	 * @throws     PropelException
	 */
	public function countAseguradoras(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(UsuarioPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collAseguradoras === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(AseguradoraPeer::USUARIO, $this->id);

				$count = AseguradoraPeer::doCount($criteria, false, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(AseguradoraPeer::USUARIO, $this->id);

				if (!isset($this->lastAseguradoraCriteria) || !$this->lastAseguradoraCriteria->equals($criteria)) {
					$count = AseguradoraPeer::doCount($criteria, false, $con);
				} else {
					$count = count($this->collAseguradoras);
				}
			} else {
				$count = count($this->collAseguradoras);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a Aseguradora object to this object
	 * through the Aseguradora foreign key attribute.
	 *
	 * @param      Aseguradora $l Aseguradora
	 * @return     void
	 * @throws     PropelException
	 */
	public function addAseguradora(Aseguradora $l)
	{
		if ($this->collAseguradoras === null) {
			$this->initAseguradoras();
		}
		if (!in_array($l, $this->collAseguradoras, true)) { // only add it if the **same** object is not already associated
			array_push($this->collAseguradoras, $l);
			$l->setUsuarioRelatedByUsuario($this);
		}
	}

	/**
	 * Clears out the collDespachos collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addDespachos()
	 */
	public function clearDespachos()
	{
		$this->collDespachos = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collDespachos collection (array).
	 *
	 * By default this just sets the collDespachos collection to an empty array (like clearcollDespachos());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initDespachos()
	{
		$this->collDespachos = array();
	}

	/**
	 * Gets an array of Despacho objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Usuario has previously been saved, it will retrieve
	 * related Despachos from storage. If this Usuario is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array Despacho[]
	 * @throws     PropelException
	 */
	public function getDespachos($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(UsuarioPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collDespachos === null) {
			if ($this->isNew()) {
			   $this->collDespachos = array();
			} else {

				$criteria->add(DespachoPeer::USUARIO, $this->id);

				DespachoPeer::addSelectColumns($criteria);
				$this->collDespachos = DespachoPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(DespachoPeer::USUARIO, $this->id);

				DespachoPeer::addSelectColumns($criteria);
				if (!isset($this->lastDespachoCriteria) || !$this->lastDespachoCriteria->equals($criteria)) {
					$this->collDespachos = DespachoPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastDespachoCriteria = $criteria;
		return $this->collDespachos;
	}

	/**
	 * Returns the number of related Despacho objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related Despacho objects.
	 * @throws     PropelException
	 */
	public function countDespachos(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(UsuarioPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collDespachos === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(DespachoPeer::USUARIO, $this->id);

				$count = DespachoPeer::doCount($criteria, false, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(DespachoPeer::USUARIO, $this->id);

				if (!isset($this->lastDespachoCriteria) || !$this->lastDespachoCriteria->equals($criteria)) {
					$count = DespachoPeer::doCount($criteria, false, $con);
				} else {
					$count = count($this->collDespachos);
				}
			} else {
				$count = count($this->collDespachos);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a Despacho object to this object
	 * through the Despacho foreign key attribute.
	 *
	 * @param      Despacho $l Despacho
	 * @return     void
	 * @throws     PropelException
	 */
	public function addDespacho(Despacho $l)
	{
		if ($this->collDespachos === null) {
			$this->initDespachos();
		}
		if (!in_array($l, $this->collDespachos, true)) { // only add it if the **same** object is not already associated
			array_push($this->collDespachos, $l);
			$l->setUsuarioRelatedByUsuario($this);
		}
	}

	/**
	 * Clears out the collChofers collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addChofers()
	 */
	public function clearChofers()
	{
		$this->collChofers = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collChofers collection (array).
	 *
	 * By default this just sets the collChofers collection to an empty array (like clearcollChofers());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initChofers()
	{
		$this->collChofers = array();
	}

	/**
	 * Gets an array of Chofer objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Usuario has previously been saved, it will retrieve
	 * related Chofers from storage. If this Usuario is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array Chofer[]
	 * @throws     PropelException
	 */
	public function getChofers($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(UsuarioPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collChofers === null) {
			if ($this->isNew()) {
			   $this->collChofers = array();
			} else {

				$criteria->add(ChoferPeer::USUARIO, $this->id);

				ChoferPeer::addSelectColumns($criteria);
				$this->collChofers = ChoferPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(ChoferPeer::USUARIO, $this->id);

				ChoferPeer::addSelectColumns($criteria);
				if (!isset($this->lastChoferCriteria) || !$this->lastChoferCriteria->equals($criteria)) {
					$this->collChofers = ChoferPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastChoferCriteria = $criteria;
		return $this->collChofers;
	}

	/**
	 * Returns the number of related Chofer objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related Chofer objects.
	 * @throws     PropelException
	 */
	public function countChofers(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(UsuarioPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collChofers === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(ChoferPeer::USUARIO, $this->id);

				$count = ChoferPeer::doCount($criteria, false, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(ChoferPeer::USUARIO, $this->id);

				if (!isset($this->lastChoferCriteria) || !$this->lastChoferCriteria->equals($criteria)) {
					$count = ChoferPeer::doCount($criteria, false, $con);
				} else {
					$count = count($this->collChofers);
				}
			} else {
				$count = count($this->collChofers);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a Chofer object to this object
	 * through the Chofer foreign key attribute.
	 *
	 * @param      Chofer $l Chofer
	 * @return     void
	 * @throws     PropelException
	 */
	public function addChofer(Chofer $l)
	{
		if ($this->collChofers === null) {
			$this->initChofers();
		}
		if (!in_array($l, $this->collChofers, true)) { // only add it if the **same** object is not already associated
			array_push($this->collChofers, $l);
			$l->setUsuarioRelatedByUsuario($this);
		}
	}

	/**
	 * Clears out the collMovils collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addMovils()
	 */
	public function clearMovils()
	{
		$this->collMovils = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collMovils collection (array).
	 *
	 * By default this just sets the collMovils collection to an empty array (like clearcollMovils());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initMovils()
	{
		$this->collMovils = array();
	}

	/**
	 * Gets an array of Movil objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Usuario has previously been saved, it will retrieve
	 * related Movils from storage. If this Usuario is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array Movil[]
	 * @throws     PropelException
	 */
	public function getMovils($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(UsuarioPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collMovils === null) {
			if ($this->isNew()) {
			   $this->collMovils = array();
			} else {

				$criteria->add(MovilPeer::USUARIO, $this->id);

				MovilPeer::addSelectColumns($criteria);
				$this->collMovils = MovilPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(MovilPeer::USUARIO, $this->id);

				MovilPeer::addSelectColumns($criteria);
				if (!isset($this->lastMovilCriteria) || !$this->lastMovilCriteria->equals($criteria)) {
					$this->collMovils = MovilPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastMovilCriteria = $criteria;
		return $this->collMovils;
	}

	/**
	 * Returns the number of related Movil objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related Movil objects.
	 * @throws     PropelException
	 */
	public function countMovils(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(UsuarioPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collMovils === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(MovilPeer::USUARIO, $this->id);

				$count = MovilPeer::doCount($criteria, false, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(MovilPeer::USUARIO, $this->id);

				if (!isset($this->lastMovilCriteria) || !$this->lastMovilCriteria->equals($criteria)) {
					$count = MovilPeer::doCount($criteria, false, $con);
				} else {
					$count = count($this->collMovils);
				}
			} else {
				$count = count($this->collMovils);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a Movil object to this object
	 * through the Movil foreign key attribute.
	 *
	 * @param      Movil $l Movil
	 * @return     void
	 * @throws     PropelException
	 */
	public function addMovil(Movil $l)
	{
		if ($this->collMovils === null) {
			$this->initMovils();
		}
		if (!in_array($l, $this->collMovils, true)) { // only add it if the **same** object is not already associated
			array_push($this->collMovils, $l);
			$l->setUsuarioRelatedByUsuario($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Usuario is new, it will return
	 * an empty collection; or if this Usuario has previously
	 * been saved, it will retrieve related Movils from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Usuario.
	 */
	public function getMovilsJoinDespacho($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(UsuarioPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collMovils === null) {
			if ($this->isNew()) {
				$this->collMovils = array();
			} else {

				$criteria->add(MovilPeer::USUARIO, $this->id);

				$this->collMovils = MovilPeer::doSelectJoinDespacho($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(MovilPeer::USUARIO, $this->id);

			if (!isset($this->lastMovilCriteria) || !$this->lastMovilCriteria->equals($criteria)) {
				$this->collMovils = MovilPeer::doSelectJoinDespacho($criteria, $con, $join_behavior);
			}
		}
		$this->lastMovilCriteria = $criteria;

		return $this->collMovils;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Usuario is new, it will return
	 * an empty collection; or if this Usuario has previously
	 * been saved, it will retrieve related Movils from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Usuario.
	 */
	public function getMovilsJoinAseguradora($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(UsuarioPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collMovils === null) {
			if ($this->isNew()) {
				$this->collMovils = array();
			} else {

				$criteria->add(MovilPeer::USUARIO, $this->id);

				$this->collMovils = MovilPeer::doSelectJoinAseguradora($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(MovilPeer::USUARIO, $this->id);

			if (!isset($this->lastMovilCriteria) || !$this->lastMovilCriteria->equals($criteria)) {
				$this->collMovils = MovilPeer::doSelectJoinAseguradora($criteria, $con, $join_behavior);
			}
		}
		$this->lastMovilCriteria = $criteria;

		return $this->collMovils;
	}

	/**
	 * Clears out the collMultas collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addMultas()
	 */
	public function clearMultas()
	{
		$this->collMultas = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collMultas collection (array).
	 *
	 * By default this just sets the collMultas collection to an empty array (like clearcollMultas());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initMultas()
	{
		$this->collMultas = array();
	}

	/**
	 * Gets an array of Multa objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Usuario has previously been saved, it will retrieve
	 * related Multas from storage. If this Usuario is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array Multa[]
	 * @throws     PropelException
	 */
	public function getMultas($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(UsuarioPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collMultas === null) {
			if ($this->isNew()) {
			   $this->collMultas = array();
			} else {

				$criteria->add(MultaPeer::USUARIO, $this->id);

				MultaPeer::addSelectColumns($criteria);
				$this->collMultas = MultaPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(MultaPeer::USUARIO, $this->id);

				MultaPeer::addSelectColumns($criteria);
				if (!isset($this->lastMultaCriteria) || !$this->lastMultaCriteria->equals($criteria)) {
					$this->collMultas = MultaPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastMultaCriteria = $criteria;
		return $this->collMultas;
	}

	/**
	 * Returns the number of related Multa objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related Multa objects.
	 * @throws     PropelException
	 */
	public function countMultas(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(UsuarioPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collMultas === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(MultaPeer::USUARIO, $this->id);

				$count = MultaPeer::doCount($criteria, false, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(MultaPeer::USUARIO, $this->id);

				if (!isset($this->lastMultaCriteria) || !$this->lastMultaCriteria->equals($criteria)) {
					$count = MultaPeer::doCount($criteria, false, $con);
				} else {
					$count = count($this->collMultas);
				}
			} else {
				$count = count($this->collMultas);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a Multa object to this object
	 * through the Multa foreign key attribute.
	 *
	 * @param      Multa $l Multa
	 * @return     void
	 * @throws     PropelException
	 */
	public function addMulta(Multa $l)
	{
		if ($this->collMultas === null) {
			$this->initMultas();
		}
		if (!in_array($l, $this->collMultas, true)) { // only add it if the **same** object is not already associated
			array_push($this->collMultas, $l);
			$l->setUsuarioRelatedByUsuario($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Usuario is new, it will return
	 * an empty collection; or if this Usuario has previously
	 * been saved, it will retrieve related Multas from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Usuario.
	 */
	public function getMultasJoinChofer($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(UsuarioPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collMultas === null) {
			if ($this->isNew()) {
				$this->collMultas = array();
			} else {

				$criteria->add(MultaPeer::USUARIO, $this->id);

				$this->collMultas = MultaPeer::doSelectJoinChofer($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(MultaPeer::USUARIO, $this->id);

			if (!isset($this->lastMultaCriteria) || !$this->lastMultaCriteria->equals($criteria)) {
				$this->collMultas = MultaPeer::doSelectJoinChofer($criteria, $con, $join_behavior);
			}
		}
		$this->lastMultaCriteria = $criteria;

		return $this->collMultas;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Usuario is new, it will return
	 * an empty collection; or if this Usuario has previously
	 * been saved, it will retrieve related Multas from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Usuario.
	 */
	public function getMultasJoinMovil($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(UsuarioPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collMultas === null) {
			if ($this->isNew()) {
				$this->collMultas = array();
			} else {

				$criteria->add(MultaPeer::USUARIO, $this->id);

				$this->collMultas = MultaPeer::doSelectJoinMovil($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(MultaPeer::USUARIO, $this->id);

			if (!isset($this->lastMultaCriteria) || !$this->lastMultaCriteria->equals($criteria)) {
				$this->collMultas = MultaPeer::doSelectJoinMovil($criteria, $con, $join_behavior);
			}
		}
		$this->lastMultaCriteria = $criteria;

		return $this->collMultas;
	}

	/**
	 * Clears out the collAccidentes collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addAccidentes()
	 */
	public function clearAccidentes()
	{
		$this->collAccidentes = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collAccidentes collection (array).
	 *
	 * By default this just sets the collAccidentes collection to an empty array (like clearcollAccidentes());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initAccidentes()
	{
		$this->collAccidentes = array();
	}

	/**
	 * Gets an array of Accidente objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Usuario has previously been saved, it will retrieve
	 * related Accidentes from storage. If this Usuario is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array Accidente[]
	 * @throws     PropelException
	 */
	public function getAccidentes($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(UsuarioPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collAccidentes === null) {
			if ($this->isNew()) {
			   $this->collAccidentes = array();
			} else {

				$criteria->add(AccidentePeer::USUARIO, $this->id);

				AccidentePeer::addSelectColumns($criteria);
				$this->collAccidentes = AccidentePeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(AccidentePeer::USUARIO, $this->id);

				AccidentePeer::addSelectColumns($criteria);
				if (!isset($this->lastAccidenteCriteria) || !$this->lastAccidenteCriteria->equals($criteria)) {
					$this->collAccidentes = AccidentePeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastAccidenteCriteria = $criteria;
		return $this->collAccidentes;
	}

	/**
	 * Returns the number of related Accidente objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related Accidente objects.
	 * @throws     PropelException
	 */
	public function countAccidentes(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(UsuarioPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collAccidentes === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(AccidentePeer::USUARIO, $this->id);

				$count = AccidentePeer::doCount($criteria, false, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(AccidentePeer::USUARIO, $this->id);

				if (!isset($this->lastAccidenteCriteria) || !$this->lastAccidenteCriteria->equals($criteria)) {
					$count = AccidentePeer::doCount($criteria, false, $con);
				} else {
					$count = count($this->collAccidentes);
				}
			} else {
				$count = count($this->collAccidentes);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a Accidente object to this object
	 * through the Accidente foreign key attribute.
	 *
	 * @param      Accidente $l Accidente
	 * @return     void
	 * @throws     PropelException
	 */
	public function addAccidente(Accidente $l)
	{
		if ($this->collAccidentes === null) {
			$this->initAccidentes();
		}
		if (!in_array($l, $this->collAccidentes, true)) { // only add it if the **same** object is not already associated
			array_push($this->collAccidentes, $l);
			$l->setUsuarioRelatedByUsuario($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Usuario is new, it will return
	 * an empty collection; or if this Usuario has previously
	 * been saved, it will retrieve related Accidentes from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Usuario.
	 */
	public function getAccidentesJoinChofer($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(UsuarioPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collAccidentes === null) {
			if ($this->isNew()) {
				$this->collAccidentes = array();
			} else {

				$criteria->add(AccidentePeer::USUARIO, $this->id);

				$this->collAccidentes = AccidentePeer::doSelectJoinChofer($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(AccidentePeer::USUARIO, $this->id);

			if (!isset($this->lastAccidenteCriteria) || !$this->lastAccidenteCriteria->equals($criteria)) {
				$this->collAccidentes = AccidentePeer::doSelectJoinChofer($criteria, $con, $join_behavior);
			}
		}
		$this->lastAccidenteCriteria = $criteria;

		return $this->collAccidentes;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Usuario is new, it will return
	 * an empty collection; or if this Usuario has previously
	 * been saved, it will retrieve related Accidentes from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Usuario.
	 */
	public function getAccidentesJoinMovil($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(UsuarioPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collAccidentes === null) {
			if ($this->isNew()) {
				$this->collAccidentes = array();
			} else {

				$criteria->add(AccidentePeer::USUARIO, $this->id);

				$this->collAccidentes = AccidentePeer::doSelectJoinMovil($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(AccidentePeer::USUARIO, $this->id);

			if (!isset($this->lastAccidenteCriteria) || !$this->lastAccidenteCriteria->equals($criteria)) {
				$this->collAccidentes = AccidentePeer::doSelectJoinMovil($criteria, $con, $join_behavior);
			}
		}
		$this->lastAccidenteCriteria = $criteria;

		return $this->collAccidentes;
	}

	/**
	 * Clears out the collRecaudacions collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addRecaudacions()
	 */
	public function clearRecaudacions()
	{
		$this->collRecaudacions = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collRecaudacions collection (array).
	 *
	 * By default this just sets the collRecaudacions collection to an empty array (like clearcollRecaudacions());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initRecaudacions()
	{
		$this->collRecaudacions = array();
	}

	/**
	 * Gets an array of Recaudacion objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Usuario has previously been saved, it will retrieve
	 * related Recaudacions from storage. If this Usuario is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array Recaudacion[]
	 * @throws     PropelException
	 */
	public function getRecaudacions($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(UsuarioPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collRecaudacions === null) {
			if ($this->isNew()) {
			   $this->collRecaudacions = array();
			} else {

				$criteria->add(RecaudacionPeer::USUARIO, $this->id);

				RecaudacionPeer::addSelectColumns($criteria);
				$this->collRecaudacions = RecaudacionPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(RecaudacionPeer::USUARIO, $this->id);

				RecaudacionPeer::addSelectColumns($criteria);
				if (!isset($this->lastRecaudacionCriteria) || !$this->lastRecaudacionCriteria->equals($criteria)) {
					$this->collRecaudacions = RecaudacionPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastRecaudacionCriteria = $criteria;
		return $this->collRecaudacions;
	}

	/**
	 * Returns the number of related Recaudacion objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related Recaudacion objects.
	 * @throws     PropelException
	 */
	public function countRecaudacions(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(UsuarioPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collRecaudacions === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(RecaudacionPeer::USUARIO, $this->id);

				$count = RecaudacionPeer::doCount($criteria, false, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(RecaudacionPeer::USUARIO, $this->id);

				if (!isset($this->lastRecaudacionCriteria) || !$this->lastRecaudacionCriteria->equals($criteria)) {
					$count = RecaudacionPeer::doCount($criteria, false, $con);
				} else {
					$count = count($this->collRecaudacions);
				}
			} else {
				$count = count($this->collRecaudacions);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a Recaudacion object to this object
	 * through the Recaudacion foreign key attribute.
	 *
	 * @param      Recaudacion $l Recaudacion
	 * @return     void
	 * @throws     PropelException
	 */
	public function addRecaudacion(Recaudacion $l)
	{
		if ($this->collRecaudacions === null) {
			$this->initRecaudacions();
		}
		if (!in_array($l, $this->collRecaudacions, true)) { // only add it if the **same** object is not already associated
			array_push($this->collRecaudacions, $l);
			$l->setUsuarioRelatedByUsuario($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Usuario is new, it will return
	 * an empty collection; or if this Usuario has previously
	 * been saved, it will retrieve related Recaudacions from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Usuario.
	 */
	public function getRecaudacionsJoinMovil($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(UsuarioPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collRecaudacions === null) {
			if ($this->isNew()) {
				$this->collRecaudacions = array();
			} else {

				$criteria->add(RecaudacionPeer::USUARIO, $this->id);

				$this->collRecaudacions = RecaudacionPeer::doSelectJoinMovil($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(RecaudacionPeer::USUARIO, $this->id);

			if (!isset($this->lastRecaudacionCriteria) || !$this->lastRecaudacionCriteria->equals($criteria)) {
				$this->collRecaudacions = RecaudacionPeer::doSelectJoinMovil($criteria, $con, $join_behavior);
			}
		}
		$this->lastRecaudacionCriteria = $criteria;

		return $this->collRecaudacions;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Usuario is new, it will return
	 * an empty collection; or if this Usuario has previously
	 * been saved, it will retrieve related Recaudacions from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Usuario.
	 */
	public function getRecaudacionsJoinChofer($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(UsuarioPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collRecaudacions === null) {
			if ($this->isNew()) {
				$this->collRecaudacions = array();
			} else {

				$criteria->add(RecaudacionPeer::USUARIO, $this->id);

				$this->collRecaudacions = RecaudacionPeer::doSelectJoinChofer($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(RecaudacionPeer::USUARIO, $this->id);

			if (!isset($this->lastRecaudacionCriteria) || !$this->lastRecaudacionCriteria->equals($criteria)) {
				$this->collRecaudacions = RecaudacionPeer::doSelectJoinChofer($criteria, $con, $join_behavior);
			}
		}
		$this->lastRecaudacionCriteria = $criteria;

		return $this->collRecaudacions;
	}

	/**
	 * Clears out the collGastorecaudacions collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addGastorecaudacions()
	 */
	public function clearGastorecaudacions()
	{
		$this->collGastorecaudacions = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collGastorecaudacions collection (array).
	 *
	 * By default this just sets the collGastorecaudacions collection to an empty array (like clearcollGastorecaudacions());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initGastorecaudacions()
	{
		$this->collGastorecaudacions = array();
	}

	/**
	 * Gets an array of Gastorecaudacion objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Usuario has previously been saved, it will retrieve
	 * related Gastorecaudacions from storage. If this Usuario is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array Gastorecaudacion[]
	 * @throws     PropelException
	 */
	public function getGastorecaudacions($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(UsuarioPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collGastorecaudacions === null) {
			if ($this->isNew()) {
			   $this->collGastorecaudacions = array();
			} else {

				$criteria->add(GastorecaudacionPeer::USUARIO, $this->id);

				GastorecaudacionPeer::addSelectColumns($criteria);
				$this->collGastorecaudacions = GastorecaudacionPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(GastorecaudacionPeer::USUARIO, $this->id);

				GastorecaudacionPeer::addSelectColumns($criteria);
				if (!isset($this->lastGastorecaudacionCriteria) || !$this->lastGastorecaudacionCriteria->equals($criteria)) {
					$this->collGastorecaudacions = GastorecaudacionPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastGastorecaudacionCriteria = $criteria;
		return $this->collGastorecaudacions;
	}

	/**
	 * Returns the number of related Gastorecaudacion objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related Gastorecaudacion objects.
	 * @throws     PropelException
	 */
	public function countGastorecaudacions(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(UsuarioPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collGastorecaudacions === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(GastorecaudacionPeer::USUARIO, $this->id);

				$count = GastorecaudacionPeer::doCount($criteria, false, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(GastorecaudacionPeer::USUARIO, $this->id);

				if (!isset($this->lastGastorecaudacionCriteria) || !$this->lastGastorecaudacionCriteria->equals($criteria)) {
					$count = GastorecaudacionPeer::doCount($criteria, false, $con);
				} else {
					$count = count($this->collGastorecaudacions);
				}
			} else {
				$count = count($this->collGastorecaudacions);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a Gastorecaudacion object to this object
	 * through the Gastorecaudacion foreign key attribute.
	 *
	 * @param      Gastorecaudacion $l Gastorecaudacion
	 * @return     void
	 * @throws     PropelException
	 */
	public function addGastorecaudacion(Gastorecaudacion $l)
	{
		if ($this->collGastorecaudacions === null) {
			$this->initGastorecaudacions();
		}
		if (!in_array($l, $this->collGastorecaudacions, true)) { // only add it if the **same** object is not already associated
			array_push($this->collGastorecaudacions, $l);
			$l->setUsuarioRelatedByUsuario($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Usuario is new, it will return
	 * an empty collection; or if this Usuario has previously
	 * been saved, it will retrieve related Gastorecaudacions from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Usuario.
	 */
	public function getGastorecaudacionsJoinRecaudacion($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(UsuarioPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collGastorecaudacions === null) {
			if ($this->isNew()) {
				$this->collGastorecaudacions = array();
			} else {

				$criteria->add(GastorecaudacionPeer::USUARIO, $this->id);

				$this->collGastorecaudacions = GastorecaudacionPeer::doSelectJoinRecaudacion($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(GastorecaudacionPeer::USUARIO, $this->id);

			if (!isset($this->lastGastorecaudacionCriteria) || !$this->lastGastorecaudacionCriteria->equals($criteria)) {
				$this->collGastorecaudacions = GastorecaudacionPeer::doSelectJoinRecaudacion($criteria, $con, $join_behavior);
			}
		}
		$this->lastGastorecaudacionCriteria = $criteria;

		return $this->collGastorecaudacions;
	}

	/**
	 * Clears out the collGastomovils collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addGastomovils()
	 */
	public function clearGastomovils()
	{
		$this->collGastomovils = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collGastomovils collection (array).
	 *
	 * By default this just sets the collGastomovils collection to an empty array (like clearcollGastomovils());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initGastomovils()
	{
		$this->collGastomovils = array();
	}

	/**
	 * Gets an array of Gastomovil objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Usuario has previously been saved, it will retrieve
	 * related Gastomovils from storage. If this Usuario is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array Gastomovil[]
	 * @throws     PropelException
	 */
	public function getGastomovils($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(UsuarioPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collGastomovils === null) {
			if ($this->isNew()) {
			   $this->collGastomovils = array();
			} else {

				$criteria->add(GastomovilPeer::USUARIO, $this->id);

				GastomovilPeer::addSelectColumns($criteria);
				$this->collGastomovils = GastomovilPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(GastomovilPeer::USUARIO, $this->id);

				GastomovilPeer::addSelectColumns($criteria);
				if (!isset($this->lastGastomovilCriteria) || !$this->lastGastomovilCriteria->equals($criteria)) {
					$this->collGastomovils = GastomovilPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastGastomovilCriteria = $criteria;
		return $this->collGastomovils;
	}

	/**
	 * Returns the number of related Gastomovil objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related Gastomovil objects.
	 * @throws     PropelException
	 */
	public function countGastomovils(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(UsuarioPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collGastomovils === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(GastomovilPeer::USUARIO, $this->id);

				$count = GastomovilPeer::doCount($criteria, false, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(GastomovilPeer::USUARIO, $this->id);

				if (!isset($this->lastGastomovilCriteria) || !$this->lastGastomovilCriteria->equals($criteria)) {
					$count = GastomovilPeer::doCount($criteria, false, $con);
				} else {
					$count = count($this->collGastomovils);
				}
			} else {
				$count = count($this->collGastomovils);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a Gastomovil object to this object
	 * through the Gastomovil foreign key attribute.
	 *
	 * @param      Gastomovil $l Gastomovil
	 * @return     void
	 * @throws     PropelException
	 */
	public function addGastomovil(Gastomovil $l)
	{
		if ($this->collGastomovils === null) {
			$this->initGastomovils();
		}
		if (!in_array($l, $this->collGastomovils, true)) { // only add it if the **same** object is not already associated
			array_push($this->collGastomovils, $l);
			$l->setUsuarioRelatedByUsuario($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Usuario is new, it will return
	 * an empty collection; or if this Usuario has previously
	 * been saved, it will retrieve related Gastomovils from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Usuario.
	 */
	public function getGastomovilsJoinMovil($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(UsuarioPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collGastomovils === null) {
			if ($this->isNew()) {
				$this->collGastomovils = array();
			} else {

				$criteria->add(GastomovilPeer::USUARIO, $this->id);

				$this->collGastomovils = GastomovilPeer::doSelectJoinMovil($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(GastomovilPeer::USUARIO, $this->id);

			if (!isset($this->lastGastomovilCriteria) || !$this->lastGastomovilCriteria->equals($criteria)) {
				$this->collGastomovils = GastomovilPeer::doSelectJoinMovil($criteria, $con, $join_behavior);
			}
		}
		$this->lastGastomovilCriteria = $criteria;

		return $this->collGastomovils;
	}

	/**
	 * Clears out the collGastoempresas collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addGastoempresas()
	 */
	public function clearGastoempresas()
	{
		$this->collGastoempresas = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collGastoempresas collection (array).
	 *
	 * By default this just sets the collGastoempresas collection to an empty array (like clearcollGastoempresas());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initGastoempresas()
	{
		$this->collGastoempresas = array();
	}

	/**
	 * Gets an array of Gastoempresa objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Usuario has previously been saved, it will retrieve
	 * related Gastoempresas from storage. If this Usuario is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array Gastoempresa[]
	 * @throws     PropelException
	 */
	public function getGastoempresas($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(UsuarioPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collGastoempresas === null) {
			if ($this->isNew()) {
			   $this->collGastoempresas = array();
			} else {

				$criteria->add(GastoempresaPeer::USUARIO, $this->id);

				GastoempresaPeer::addSelectColumns($criteria);
				$this->collGastoempresas = GastoempresaPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(GastoempresaPeer::USUARIO, $this->id);

				GastoempresaPeer::addSelectColumns($criteria);
				if (!isset($this->lastGastoempresaCriteria) || !$this->lastGastoempresaCriteria->equals($criteria)) {
					$this->collGastoempresas = GastoempresaPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastGastoempresaCriteria = $criteria;
		return $this->collGastoempresas;
	}

	/**
	 * Returns the number of related Gastoempresa objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related Gastoempresa objects.
	 * @throws     PropelException
	 */
	public function countGastoempresas(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(UsuarioPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collGastoempresas === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(GastoempresaPeer::USUARIO, $this->id);

				$count = GastoempresaPeer::doCount($criteria, false, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(GastoempresaPeer::USUARIO, $this->id);

				if (!isset($this->lastGastoempresaCriteria) || !$this->lastGastoempresaCriteria->equals($criteria)) {
					$count = GastoempresaPeer::doCount($criteria, false, $con);
				} else {
					$count = count($this->collGastoempresas);
				}
			} else {
				$count = count($this->collGastoempresas);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a Gastoempresa object to this object
	 * through the Gastoempresa foreign key attribute.
	 *
	 * @param      Gastoempresa $l Gastoempresa
	 * @return     void
	 * @throws     PropelException
	 */
	public function addGastoempresa(Gastoempresa $l)
	{
		if ($this->collGastoempresas === null) {
			$this->initGastoempresas();
		}
		if (!in_array($l, $this->collGastoempresas, true)) { // only add it if the **same** object is not already associated
			array_push($this->collGastoempresas, $l);
			$l->setUsuarioRelatedByUsuario($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Usuario is new, it will return
	 * an empty collection; or if this Usuario has previously
	 * been saved, it will retrieve related Gastoempresas from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Usuario.
	 */
	public function getGastoempresasJoinEmpresa($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(UsuarioPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collGastoempresas === null) {
			if ($this->isNew()) {
				$this->collGastoempresas = array();
			} else {

				$criteria->add(GastoempresaPeer::USUARIO, $this->id);

				$this->collGastoempresas = GastoempresaPeer::doSelectJoinEmpresa($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(GastoempresaPeer::USUARIO, $this->id);

			if (!isset($this->lastGastoempresaCriteria) || !$this->lastGastoempresaCriteria->equals($criteria)) {
				$this->collGastoempresas = GastoempresaPeer::doSelectJoinEmpresa($criteria, $con, $join_behavior);
			}
		}
		$this->lastGastoempresaCriteria = $criteria;

		return $this->collGastoempresas;
	}

	/**
	 * Clears out the collConfiguracions collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addConfiguracions()
	 */
	public function clearConfiguracions()
	{
		$this->collConfiguracions = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collConfiguracions collection (array).
	 *
	 * By default this just sets the collConfiguracions collection to an empty array (like clearcollConfiguracions());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initConfiguracions()
	{
		$this->collConfiguracions = array();
	}

	/**
	 * Gets an array of Configuracion objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Usuario has previously been saved, it will retrieve
	 * related Configuracions from storage. If this Usuario is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array Configuracion[]
	 * @throws     PropelException
	 */
	public function getConfiguracions($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(UsuarioPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collConfiguracions === null) {
			if ($this->isNew()) {
			   $this->collConfiguracions = array();
			} else {

				$criteria->add(ConfiguracionPeer::IDUSUARIO, $this->id);

				ConfiguracionPeer::addSelectColumns($criteria);
				$this->collConfiguracions = ConfiguracionPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(ConfiguracionPeer::IDUSUARIO, $this->id);

				ConfiguracionPeer::addSelectColumns($criteria);
				if (!isset($this->lastConfiguracionCriteria) || !$this->lastConfiguracionCriteria->equals($criteria)) {
					$this->collConfiguracions = ConfiguracionPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastConfiguracionCriteria = $criteria;
		return $this->collConfiguracions;
	}

	/**
	 * Returns the number of related Configuracion objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related Configuracion objects.
	 * @throws     PropelException
	 */
	public function countConfiguracions(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(UsuarioPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collConfiguracions === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(ConfiguracionPeer::IDUSUARIO, $this->id);

				$count = ConfiguracionPeer::doCount($criteria, false, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(ConfiguracionPeer::IDUSUARIO, $this->id);

				if (!isset($this->lastConfiguracionCriteria) || !$this->lastConfiguracionCriteria->equals($criteria)) {
					$count = ConfiguracionPeer::doCount($criteria, false, $con);
				} else {
					$count = count($this->collConfiguracions);
				}
			} else {
				$count = count($this->collConfiguracions);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a Configuracion object to this object
	 * through the Configuracion foreign key attribute.
	 *
	 * @param      Configuracion $l Configuracion
	 * @return     void
	 * @throws     PropelException
	 */
	public function addConfiguracion(Configuracion $l)
	{
		if ($this->collConfiguracions === null) {
			$this->initConfiguracions();
		}
		if (!in_array($l, $this->collConfiguracions, true)) { // only add it if the **same** object is not already associated
			array_push($this->collConfiguracions, $l);
			$l->setUsuario($this);
		}
	}

	/**
	 * Clears out the collPagoaseguradoras collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addPagoaseguradoras()
	 */
	public function clearPagoaseguradoras()
	{
		$this->collPagoaseguradoras = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collPagoaseguradoras collection (array).
	 *
	 * By default this just sets the collPagoaseguradoras collection to an empty array (like clearcollPagoaseguradoras());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initPagoaseguradoras()
	{
		$this->collPagoaseguradoras = array();
	}

	/**
	 * Gets an array of Pagoaseguradora objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Usuario has previously been saved, it will retrieve
	 * related Pagoaseguradoras from storage. If this Usuario is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array Pagoaseguradora[]
	 * @throws     PropelException
	 */
	public function getPagoaseguradoras($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(UsuarioPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collPagoaseguradoras === null) {
			if ($this->isNew()) {
			   $this->collPagoaseguradoras = array();
			} else {

				$criteria->add(PagoaseguradoraPeer::USUARIO, $this->id);

				PagoaseguradoraPeer::addSelectColumns($criteria);
				$this->collPagoaseguradoras = PagoaseguradoraPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(PagoaseguradoraPeer::USUARIO, $this->id);

				PagoaseguradoraPeer::addSelectColumns($criteria);
				if (!isset($this->lastPagoaseguradoraCriteria) || !$this->lastPagoaseguradoraCriteria->equals($criteria)) {
					$this->collPagoaseguradoras = PagoaseguradoraPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastPagoaseguradoraCriteria = $criteria;
		return $this->collPagoaseguradoras;
	}

	/**
	 * Returns the number of related Pagoaseguradora objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related Pagoaseguradora objects.
	 * @throws     PropelException
	 */
	public function countPagoaseguradoras(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(UsuarioPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collPagoaseguradoras === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(PagoaseguradoraPeer::USUARIO, $this->id);

				$count = PagoaseguradoraPeer::doCount($criteria, false, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(PagoaseguradoraPeer::USUARIO, $this->id);

				if (!isset($this->lastPagoaseguradoraCriteria) || !$this->lastPagoaseguradoraCriteria->equals($criteria)) {
					$count = PagoaseguradoraPeer::doCount($criteria, false, $con);
				} else {
					$count = count($this->collPagoaseguradoras);
				}
			} else {
				$count = count($this->collPagoaseguradoras);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a Pagoaseguradora object to this object
	 * through the Pagoaseguradora foreign key attribute.
	 *
	 * @param      Pagoaseguradora $l Pagoaseguradora
	 * @return     void
	 * @throws     PropelException
	 */
	public function addPagoaseguradora(Pagoaseguradora $l)
	{
		if ($this->collPagoaseguradoras === null) {
			$this->initPagoaseguradoras();
		}
		if (!in_array($l, $this->collPagoaseguradoras, true)) { // only add it if the **same** object is not already associated
			array_push($this->collPagoaseguradoras, $l);
			$l->setUsuarioRelatedByUsuario($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Usuario is new, it will return
	 * an empty collection; or if this Usuario has previously
	 * been saved, it will retrieve related Pagoaseguradoras from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Usuario.
	 */
	public function getPagoaseguradorasJoinEmpresa($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(UsuarioPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collPagoaseguradoras === null) {
			if ($this->isNew()) {
				$this->collPagoaseguradoras = array();
			} else {

				$criteria->add(PagoaseguradoraPeer::USUARIO, $this->id);

				$this->collPagoaseguradoras = PagoaseguradoraPeer::doSelectJoinEmpresa($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(PagoaseguradoraPeer::USUARIO, $this->id);

			if (!isset($this->lastPagoaseguradoraCriteria) || !$this->lastPagoaseguradoraCriteria->equals($criteria)) {
				$this->collPagoaseguradoras = PagoaseguradoraPeer::doSelectJoinEmpresa($criteria, $con, $join_behavior);
			}
		}
		$this->lastPagoaseguradoraCriteria = $criteria;

		return $this->collPagoaseguradoras;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Usuario is new, it will return
	 * an empty collection; or if this Usuario has previously
	 * been saved, it will retrieve related Pagoaseguradoras from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Usuario.
	 */
	public function getPagoaseguradorasJoinAseguradora($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(UsuarioPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collPagoaseguradoras === null) {
			if ($this->isNew()) {
				$this->collPagoaseguradoras = array();
			} else {

				$criteria->add(PagoaseguradoraPeer::USUARIO, $this->id);

				$this->collPagoaseguradoras = PagoaseguradoraPeer::doSelectJoinAseguradora($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(PagoaseguradoraPeer::USUARIO, $this->id);

			if (!isset($this->lastPagoaseguradoraCriteria) || !$this->lastPagoaseguradoraCriteria->equals($criteria)) {
				$this->collPagoaseguradoras = PagoaseguradoraPeer::doSelectJoinAseguradora($criteria, $con, $join_behavior);
			}
		}
		$this->lastPagoaseguradoraCriteria = $criteria;

		return $this->collPagoaseguradoras;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Usuario is new, it will return
	 * an empty collection; or if this Usuario has previously
	 * been saved, it will retrieve related Pagoaseguradoras from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Usuario.
	 */
	public function getPagoaseguradorasJoinMovil($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(UsuarioPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collPagoaseguradoras === null) {
			if ($this->isNew()) {
				$this->collPagoaseguradoras = array();
			} else {

				$criteria->add(PagoaseguradoraPeer::USUARIO, $this->id);

				$this->collPagoaseguradoras = PagoaseguradoraPeer::doSelectJoinMovil($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(PagoaseguradoraPeer::USUARIO, $this->id);

			if (!isset($this->lastPagoaseguradoraCriteria) || !$this->lastPagoaseguradoraCriteria->equals($criteria)) {
				$this->collPagoaseguradoras = PagoaseguradoraPeer::doSelectJoinMovil($criteria, $con, $join_behavior);
			}
		}
		$this->lastPagoaseguradoraCriteria = $criteria;

		return $this->collPagoaseguradoras;
	}

	/**
	 * Clears out the collTrabajotallers collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addTrabajotallers()
	 */
	public function clearTrabajotallers()
	{
		$this->collTrabajotallers = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collTrabajotallers collection (array).
	 *
	 * By default this just sets the collTrabajotallers collection to an empty array (like clearcollTrabajotallers());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initTrabajotallers()
	{
		$this->collTrabajotallers = array();
	}

	/**
	 * Gets an array of Trabajotaller objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Usuario has previously been saved, it will retrieve
	 * related Trabajotallers from storage. If this Usuario is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array Trabajotaller[]
	 * @throws     PropelException
	 */
	public function getTrabajotallers($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(UsuarioPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collTrabajotallers === null) {
			if ($this->isNew()) {
			   $this->collTrabajotallers = array();
			} else {

				$criteria->add(TrabajotallerPeer::USUARIO, $this->id);

				TrabajotallerPeer::addSelectColumns($criteria);
				$this->collTrabajotallers = TrabajotallerPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(TrabajotallerPeer::USUARIO, $this->id);

				TrabajotallerPeer::addSelectColumns($criteria);
				if (!isset($this->lastTrabajotallerCriteria) || !$this->lastTrabajotallerCriteria->equals($criteria)) {
					$this->collTrabajotallers = TrabajotallerPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastTrabajotallerCriteria = $criteria;
		return $this->collTrabajotallers;
	}

	/**
	 * Returns the number of related Trabajotaller objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related Trabajotaller objects.
	 * @throws     PropelException
	 */
	public function countTrabajotallers(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(UsuarioPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collTrabajotallers === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(TrabajotallerPeer::USUARIO, $this->id);

				$count = TrabajotallerPeer::doCount($criteria, false, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(TrabajotallerPeer::USUARIO, $this->id);

				if (!isset($this->lastTrabajotallerCriteria) || !$this->lastTrabajotallerCriteria->equals($criteria)) {
					$count = TrabajotallerPeer::doCount($criteria, false, $con);
				} else {
					$count = count($this->collTrabajotallers);
				}
			} else {
				$count = count($this->collTrabajotallers);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a Trabajotaller object to this object
	 * through the Trabajotaller foreign key attribute.
	 *
	 * @param      Trabajotaller $l Trabajotaller
	 * @return     void
	 * @throws     PropelException
	 */
	public function addTrabajotaller(Trabajotaller $l)
	{
		if ($this->collTrabajotallers === null) {
			$this->initTrabajotallers();
		}
		if (!in_array($l, $this->collTrabajotallers, true)) { // only add it if the **same** object is not already associated
			array_push($this->collTrabajotallers, $l);
			$l->setUsuarioRelatedByUsuario($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Usuario is new, it will return
	 * an empty collection; or if this Usuario has previously
	 * been saved, it will retrieve related Trabajotallers from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Usuario.
	 */
	public function getTrabajotallersJoinMovil($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(UsuarioPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collTrabajotallers === null) {
			if ($this->isNew()) {
				$this->collTrabajotallers = array();
			} else {

				$criteria->add(TrabajotallerPeer::USUARIO, $this->id);

				$this->collTrabajotallers = TrabajotallerPeer::doSelectJoinMovil($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(TrabajotallerPeer::USUARIO, $this->id);

			if (!isset($this->lastTrabajotallerCriteria) || !$this->lastTrabajotallerCriteria->equals($criteria)) {
				$this->collTrabajotallers = TrabajotallerPeer::doSelectJoinMovil($criteria, $con, $join_behavior);
			}
		}
		$this->lastTrabajotallerCriteria = $criteria;

		return $this->collTrabajotallers;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Usuario is new, it will return
	 * an empty collection; or if this Usuario has previously
	 * been saved, it will retrieve related Trabajotallers from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Usuario.
	 */
	public function getTrabajotallersJoinTaller($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(UsuarioPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collTrabajotallers === null) {
			if ($this->isNew()) {
				$this->collTrabajotallers = array();
			} else {

				$criteria->add(TrabajotallerPeer::USUARIO, $this->id);

				$this->collTrabajotallers = TrabajotallerPeer::doSelectJoinTaller($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(TrabajotallerPeer::USUARIO, $this->id);

			if (!isset($this->lastTrabajotallerCriteria) || !$this->lastTrabajotallerCriteria->equals($criteria)) {
				$this->collTrabajotallers = TrabajotallerPeer::doSelectJoinTaller($criteria, $con, $join_behavior);
			}
		}
		$this->lastTrabajotallerCriteria = $criteria;

		return $this->collTrabajotallers;
	}

	/**
	 * Clears out the collAdelantos collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addAdelantos()
	 */
	public function clearAdelantos()
	{
		$this->collAdelantos = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collAdelantos collection (array).
	 *
	 * By default this just sets the collAdelantos collection to an empty array (like clearcollAdelantos());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initAdelantos()
	{
		$this->collAdelantos = array();
	}

	/**
	 * Gets an array of Adelanto objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Usuario has previously been saved, it will retrieve
	 * related Adelantos from storage. If this Usuario is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array Adelanto[]
	 * @throws     PropelException
	 */
	public function getAdelantos($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(UsuarioPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collAdelantos === null) {
			if ($this->isNew()) {
			   $this->collAdelantos = array();
			} else {

				$criteria->add(AdelantoPeer::USUARIO, $this->id);

				AdelantoPeer::addSelectColumns($criteria);
				$this->collAdelantos = AdelantoPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(AdelantoPeer::USUARIO, $this->id);

				AdelantoPeer::addSelectColumns($criteria);
				if (!isset($this->lastAdelantoCriteria) || !$this->lastAdelantoCriteria->equals($criteria)) {
					$this->collAdelantos = AdelantoPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastAdelantoCriteria = $criteria;
		return $this->collAdelantos;
	}

	/**
	 * Returns the number of related Adelanto objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related Adelanto objects.
	 * @throws     PropelException
	 */
	public function countAdelantos(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(UsuarioPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collAdelantos === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(AdelantoPeer::USUARIO, $this->id);

				$count = AdelantoPeer::doCount($criteria, false, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(AdelantoPeer::USUARIO, $this->id);

				if (!isset($this->lastAdelantoCriteria) || !$this->lastAdelantoCriteria->equals($criteria)) {
					$count = AdelantoPeer::doCount($criteria, false, $con);
				} else {
					$count = count($this->collAdelantos);
				}
			} else {
				$count = count($this->collAdelantos);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a Adelanto object to this object
	 * through the Adelanto foreign key attribute.
	 *
	 * @param      Adelanto $l Adelanto
	 * @return     void
	 * @throws     PropelException
	 */
	public function addAdelanto(Adelanto $l)
	{
		if ($this->collAdelantos === null) {
			$this->initAdelantos();
		}
		if (!in_array($l, $this->collAdelantos, true)) { // only add it if the **same** object is not already associated
			array_push($this->collAdelantos, $l);
			$l->setUsuarioRelatedByUsuario($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Usuario is new, it will return
	 * an empty collection; or if this Usuario has previously
	 * been saved, it will retrieve related Adelantos from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Usuario.
	 */
	public function getAdelantosJoinChofer($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(UsuarioPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collAdelantos === null) {
			if ($this->isNew()) {
				$this->collAdelantos = array();
			} else {

				$criteria->add(AdelantoPeer::USUARIO, $this->id);

				$this->collAdelantos = AdelantoPeer::doSelectJoinChofer($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(AdelantoPeer::USUARIO, $this->id);

			if (!isset($this->lastAdelantoCriteria) || !$this->lastAdelantoCriteria->equals($criteria)) {
				$this->collAdelantos = AdelantoPeer::doSelectJoinChofer($criteria, $con, $join_behavior);
			}
		}
		$this->lastAdelantoCriteria = $criteria;

		return $this->collAdelantos;
	}

	/**
	 * Clears out the collPagoadelantos collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addPagoadelantos()
	 */
	public function clearPagoadelantos()
	{
		$this->collPagoadelantos = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collPagoadelantos collection (array).
	 *
	 * By default this just sets the collPagoadelantos collection to an empty array (like clearcollPagoadelantos());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initPagoadelantos()
	{
		$this->collPagoadelantos = array();
	}

	/**
	 * Gets an array of Pagoadelanto objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Usuario has previously been saved, it will retrieve
	 * related Pagoadelantos from storage. If this Usuario is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array Pagoadelanto[]
	 * @throws     PropelException
	 */
	public function getPagoadelantos($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(UsuarioPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collPagoadelantos === null) {
			if ($this->isNew()) {
			   $this->collPagoadelantos = array();
			} else {

				$criteria->add(PagoadelantoPeer::USUARIO, $this->id);

				PagoadelantoPeer::addSelectColumns($criteria);
				$this->collPagoadelantos = PagoadelantoPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(PagoadelantoPeer::USUARIO, $this->id);

				PagoadelantoPeer::addSelectColumns($criteria);
				if (!isset($this->lastPagoadelantoCriteria) || !$this->lastPagoadelantoCriteria->equals($criteria)) {
					$this->collPagoadelantos = PagoadelantoPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastPagoadelantoCriteria = $criteria;
		return $this->collPagoadelantos;
	}

	/**
	 * Returns the number of related Pagoadelanto objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related Pagoadelanto objects.
	 * @throws     PropelException
	 */
	public function countPagoadelantos(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(UsuarioPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collPagoadelantos === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(PagoadelantoPeer::USUARIO, $this->id);

				$count = PagoadelantoPeer::doCount($criteria, false, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(PagoadelantoPeer::USUARIO, $this->id);

				if (!isset($this->lastPagoadelantoCriteria) || !$this->lastPagoadelantoCriteria->equals($criteria)) {
					$count = PagoadelantoPeer::doCount($criteria, false, $con);
				} else {
					$count = count($this->collPagoadelantos);
				}
			} else {
				$count = count($this->collPagoadelantos);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a Pagoadelanto object to this object
	 * through the Pagoadelanto foreign key attribute.
	 *
	 * @param      Pagoadelanto $l Pagoadelanto
	 * @return     void
	 * @throws     PropelException
	 */
	public function addPagoadelanto(Pagoadelanto $l)
	{
		if ($this->collPagoadelantos === null) {
			$this->initPagoadelantos();
		}
		if (!in_array($l, $this->collPagoadelantos, true)) { // only add it if the **same** object is not already associated
			array_push($this->collPagoadelantos, $l);
			$l->setUsuarioRelatedByUsuario($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Usuario is new, it will return
	 * an empty collection; or if this Usuario has previously
	 * been saved, it will retrieve related Pagoadelantos from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Usuario.
	 */
	public function getPagoadelantosJoinAdelanto($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(UsuarioPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collPagoadelantos === null) {
			if ($this->isNew()) {
				$this->collPagoadelantos = array();
			} else {

				$criteria->add(PagoadelantoPeer::USUARIO, $this->id);

				$this->collPagoadelantos = PagoadelantoPeer::doSelectJoinAdelanto($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(PagoadelantoPeer::USUARIO, $this->id);

			if (!isset($this->lastPagoadelantoCriteria) || !$this->lastPagoadelantoCriteria->equals($criteria)) {
				$this->collPagoadelantos = PagoadelantoPeer::doSelectJoinAdelanto($criteria, $con, $join_behavior);
			}
		}
		$this->lastPagoadelantoCriteria = $criteria;

		return $this->collPagoadelantos;
	}

	/**
	 * Clears out the collEmpresaPropietarios collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addEmpresaPropietarios()
	 */
	public function clearEmpresaPropietarios()
	{
		$this->collEmpresaPropietarios = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collEmpresaPropietarios collection (array).
	 *
	 * By default this just sets the collEmpresaPropietarios collection to an empty array (like clearcollEmpresaPropietarios());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initEmpresaPropietarios()
	{
		$this->collEmpresaPropietarios = array();
	}

	/**
	 * Gets an array of EmpresaPropietario objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Usuario has previously been saved, it will retrieve
	 * related EmpresaPropietarios from storage. If this Usuario is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array EmpresaPropietario[]
	 * @throws     PropelException
	 */
	public function getEmpresaPropietarios($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(UsuarioPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collEmpresaPropietarios === null) {
			if ($this->isNew()) {
			   $this->collEmpresaPropietarios = array();
			} else {

				$criteria->add(EmpresaPropietarioPeer::USUARIO, $this->id);

				EmpresaPropietarioPeer::addSelectColumns($criteria);
				$this->collEmpresaPropietarios = EmpresaPropietarioPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(EmpresaPropietarioPeer::USUARIO, $this->id);

				EmpresaPropietarioPeer::addSelectColumns($criteria);
				if (!isset($this->lastEmpresaPropietarioCriteria) || !$this->lastEmpresaPropietarioCriteria->equals($criteria)) {
					$this->collEmpresaPropietarios = EmpresaPropietarioPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastEmpresaPropietarioCriteria = $criteria;
		return $this->collEmpresaPropietarios;
	}

	/**
	 * Returns the number of related EmpresaPropietario objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related EmpresaPropietario objects.
	 * @throws     PropelException
	 */
	public function countEmpresaPropietarios(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(UsuarioPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collEmpresaPropietarios === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(EmpresaPropietarioPeer::USUARIO, $this->id);

				$count = EmpresaPropietarioPeer::doCount($criteria, false, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(EmpresaPropietarioPeer::USUARIO, $this->id);

				if (!isset($this->lastEmpresaPropietarioCriteria) || !$this->lastEmpresaPropietarioCriteria->equals($criteria)) {
					$count = EmpresaPropietarioPeer::doCount($criteria, false, $con);
				} else {
					$count = count($this->collEmpresaPropietarios);
				}
			} else {
				$count = count($this->collEmpresaPropietarios);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a EmpresaPropietario object to this object
	 * through the EmpresaPropietario foreign key attribute.
	 *
	 * @param      EmpresaPropietario $l EmpresaPropietario
	 * @return     void
	 * @throws     PropelException
	 */
	public function addEmpresaPropietario(EmpresaPropietario $l)
	{
		if ($this->collEmpresaPropietarios === null) {
			$this->initEmpresaPropietarios();
		}
		if (!in_array($l, $this->collEmpresaPropietarios, true)) { // only add it if the **same** object is not already associated
			array_push($this->collEmpresaPropietarios, $l);
			$l->setUsuarioRelatedByUsuario($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Usuario is new, it will return
	 * an empty collection; or if this Usuario has previously
	 * been saved, it will retrieve related EmpresaPropietarios from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Usuario.
	 */
	public function getEmpresaPropietariosJoinEmpresa($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(UsuarioPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collEmpresaPropietarios === null) {
			if ($this->isNew()) {
				$this->collEmpresaPropietarios = array();
			} else {

				$criteria->add(EmpresaPropietarioPeer::USUARIO, $this->id);

				$this->collEmpresaPropietarios = EmpresaPropietarioPeer::doSelectJoinEmpresa($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(EmpresaPropietarioPeer::USUARIO, $this->id);

			if (!isset($this->lastEmpresaPropietarioCriteria) || !$this->lastEmpresaPropietarioCriteria->equals($criteria)) {
				$this->collEmpresaPropietarios = EmpresaPropietarioPeer::doSelectJoinEmpresa($criteria, $con, $join_behavior);
			}
		}
		$this->lastEmpresaPropietarioCriteria = $criteria;

		return $this->collEmpresaPropietarios;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Usuario is new, it will return
	 * an empty collection; or if this Usuario has previously
	 * been saved, it will retrieve related EmpresaPropietarios from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Usuario.
	 */
	public function getEmpresaPropietariosJoinPropietario($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(UsuarioPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collEmpresaPropietarios === null) {
			if ($this->isNew()) {
				$this->collEmpresaPropietarios = array();
			} else {

				$criteria->add(EmpresaPropietarioPeer::USUARIO, $this->id);

				$this->collEmpresaPropietarios = EmpresaPropietarioPeer::doSelectJoinPropietario($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(EmpresaPropietarioPeer::USUARIO, $this->id);

			if (!isset($this->lastEmpresaPropietarioCriteria) || !$this->lastEmpresaPropietarioCriteria->equals($criteria)) {
				$this->collEmpresaPropietarios = EmpresaPropietarioPeer::doSelectJoinPropietario($criteria, $con, $join_behavior);
			}
		}
		$this->lastEmpresaPropietarioCriteria = $criteria;

		return $this->collEmpresaPropietarios;
	}

	/**
	 * Clears out the collMovilAseguradoras collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addMovilAseguradoras()
	 */
	public function clearMovilAseguradoras()
	{
		$this->collMovilAseguradoras = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collMovilAseguradoras collection (array).
	 *
	 * By default this just sets the collMovilAseguradoras collection to an empty array (like clearcollMovilAseguradoras());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initMovilAseguradoras()
	{
		$this->collMovilAseguradoras = array();
	}

	/**
	 * Gets an array of MovilAseguradora objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Usuario has previously been saved, it will retrieve
	 * related MovilAseguradoras from storage. If this Usuario is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array MovilAseguradora[]
	 * @throws     PropelException
	 */
	public function getMovilAseguradoras($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(UsuarioPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collMovilAseguradoras === null) {
			if ($this->isNew()) {
			   $this->collMovilAseguradoras = array();
			} else {

				$criteria->add(MovilAseguradoraPeer::USUARIO, $this->id);

				MovilAseguradoraPeer::addSelectColumns($criteria);
				$this->collMovilAseguradoras = MovilAseguradoraPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(MovilAseguradoraPeer::USUARIO, $this->id);

				MovilAseguradoraPeer::addSelectColumns($criteria);
				if (!isset($this->lastMovilAseguradoraCriteria) || !$this->lastMovilAseguradoraCriteria->equals($criteria)) {
					$this->collMovilAseguradoras = MovilAseguradoraPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastMovilAseguradoraCriteria = $criteria;
		return $this->collMovilAseguradoras;
	}

	/**
	 * Returns the number of related MovilAseguradora objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related MovilAseguradora objects.
	 * @throws     PropelException
	 */
	public function countMovilAseguradoras(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(UsuarioPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collMovilAseguradoras === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(MovilAseguradoraPeer::USUARIO, $this->id);

				$count = MovilAseguradoraPeer::doCount($criteria, false, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(MovilAseguradoraPeer::USUARIO, $this->id);

				if (!isset($this->lastMovilAseguradoraCriteria) || !$this->lastMovilAseguradoraCriteria->equals($criteria)) {
					$count = MovilAseguradoraPeer::doCount($criteria, false, $con);
				} else {
					$count = count($this->collMovilAseguradoras);
				}
			} else {
				$count = count($this->collMovilAseguradoras);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a MovilAseguradora object to this object
	 * through the MovilAseguradora foreign key attribute.
	 *
	 * @param      MovilAseguradora $l MovilAseguradora
	 * @return     void
	 * @throws     PropelException
	 */
	public function addMovilAseguradora(MovilAseguradora $l)
	{
		if ($this->collMovilAseguradoras === null) {
			$this->initMovilAseguradoras();
		}
		if (!in_array($l, $this->collMovilAseguradoras, true)) { // only add it if the **same** object is not already associated
			array_push($this->collMovilAseguradoras, $l);
			$l->setUsuarioRelatedByUsuario($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Usuario is new, it will return
	 * an empty collection; or if this Usuario has previously
	 * been saved, it will retrieve related MovilAseguradoras from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Usuario.
	 */
	public function getMovilAseguradorasJoinMovil($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(UsuarioPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collMovilAseguradoras === null) {
			if ($this->isNew()) {
				$this->collMovilAseguradoras = array();
			} else {

				$criteria->add(MovilAseguradoraPeer::USUARIO, $this->id);

				$this->collMovilAseguradoras = MovilAseguradoraPeer::doSelectJoinMovil($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(MovilAseguradoraPeer::USUARIO, $this->id);

			if (!isset($this->lastMovilAseguradoraCriteria) || !$this->lastMovilAseguradoraCriteria->equals($criteria)) {
				$this->collMovilAseguradoras = MovilAseguradoraPeer::doSelectJoinMovil($criteria, $con, $join_behavior);
			}
		}
		$this->lastMovilAseguradoraCriteria = $criteria;

		return $this->collMovilAseguradoras;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Usuario is new, it will return
	 * an empty collection; or if this Usuario has previously
	 * been saved, it will retrieve related MovilAseguradoras from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Usuario.
	 */
	public function getMovilAseguradorasJoinAseguradora($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(UsuarioPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collMovilAseguradoras === null) {
			if ($this->isNew()) {
				$this->collMovilAseguradoras = array();
			} else {

				$criteria->add(MovilAseguradoraPeer::USUARIO, $this->id);

				$this->collMovilAseguradoras = MovilAseguradoraPeer::doSelectJoinAseguradora($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(MovilAseguradoraPeer::USUARIO, $this->id);

			if (!isset($this->lastMovilAseguradoraCriteria) || !$this->lastMovilAseguradoraCriteria->equals($criteria)) {
				$this->collMovilAseguradoras = MovilAseguradoraPeer::doSelectJoinAseguradora($criteria, $con, $join_behavior);
			}
		}
		$this->lastMovilAseguradoraCriteria = $criteria;

		return $this->collMovilAseguradoras;
	}

	/**
	 * Clears out the collMovilEmpresas collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addMovilEmpresas()
	 */
	public function clearMovilEmpresas()
	{
		$this->collMovilEmpresas = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collMovilEmpresas collection (array).
	 *
	 * By default this just sets the collMovilEmpresas collection to an empty array (like clearcollMovilEmpresas());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initMovilEmpresas()
	{
		$this->collMovilEmpresas = array();
	}

	/**
	 * Gets an array of MovilEmpresa objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Usuario has previously been saved, it will retrieve
	 * related MovilEmpresas from storage. If this Usuario is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array MovilEmpresa[]
	 * @throws     PropelException
	 */
	public function getMovilEmpresas($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(UsuarioPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collMovilEmpresas === null) {
			if ($this->isNew()) {
			   $this->collMovilEmpresas = array();
			} else {

				$criteria->add(MovilEmpresaPeer::USUARIO, $this->id);

				MovilEmpresaPeer::addSelectColumns($criteria);
				$this->collMovilEmpresas = MovilEmpresaPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(MovilEmpresaPeer::USUARIO, $this->id);

				MovilEmpresaPeer::addSelectColumns($criteria);
				if (!isset($this->lastMovilEmpresaCriteria) || !$this->lastMovilEmpresaCriteria->equals($criteria)) {
					$this->collMovilEmpresas = MovilEmpresaPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastMovilEmpresaCriteria = $criteria;
		return $this->collMovilEmpresas;
	}

	/**
	 * Returns the number of related MovilEmpresa objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related MovilEmpresa objects.
	 * @throws     PropelException
	 */
	public function countMovilEmpresas(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(UsuarioPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collMovilEmpresas === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(MovilEmpresaPeer::USUARIO, $this->id);

				$count = MovilEmpresaPeer::doCount($criteria, false, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(MovilEmpresaPeer::USUARIO, $this->id);

				if (!isset($this->lastMovilEmpresaCriteria) || !$this->lastMovilEmpresaCriteria->equals($criteria)) {
					$count = MovilEmpresaPeer::doCount($criteria, false, $con);
				} else {
					$count = count($this->collMovilEmpresas);
				}
			} else {
				$count = count($this->collMovilEmpresas);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a MovilEmpresa object to this object
	 * through the MovilEmpresa foreign key attribute.
	 *
	 * @param      MovilEmpresa $l MovilEmpresa
	 * @return     void
	 * @throws     PropelException
	 */
	public function addMovilEmpresa(MovilEmpresa $l)
	{
		if ($this->collMovilEmpresas === null) {
			$this->initMovilEmpresas();
		}
		if (!in_array($l, $this->collMovilEmpresas, true)) { // only add it if the **same** object is not already associated
			array_push($this->collMovilEmpresas, $l);
			$l->setUsuarioRelatedByUsuario($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Usuario is new, it will return
	 * an empty collection; or if this Usuario has previously
	 * been saved, it will retrieve related MovilEmpresas from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Usuario.
	 */
	public function getMovilEmpresasJoinMovil($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(UsuarioPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collMovilEmpresas === null) {
			if ($this->isNew()) {
				$this->collMovilEmpresas = array();
			} else {

				$criteria->add(MovilEmpresaPeer::USUARIO, $this->id);

				$this->collMovilEmpresas = MovilEmpresaPeer::doSelectJoinMovil($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(MovilEmpresaPeer::USUARIO, $this->id);

			if (!isset($this->lastMovilEmpresaCriteria) || !$this->lastMovilEmpresaCriteria->equals($criteria)) {
				$this->collMovilEmpresas = MovilEmpresaPeer::doSelectJoinMovil($criteria, $con, $join_behavior);
			}
		}
		$this->lastMovilEmpresaCriteria = $criteria;

		return $this->collMovilEmpresas;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Usuario is new, it will return
	 * an empty collection; or if this Usuario has previously
	 * been saved, it will retrieve related MovilEmpresas from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Usuario.
	 */
	public function getMovilEmpresasJoinEmpresa($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(UsuarioPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collMovilEmpresas === null) {
			if ($this->isNew()) {
				$this->collMovilEmpresas = array();
			} else {

				$criteria->add(MovilEmpresaPeer::USUARIO, $this->id);

				$this->collMovilEmpresas = MovilEmpresaPeer::doSelectJoinEmpresa($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(MovilEmpresaPeer::USUARIO, $this->id);

			if (!isset($this->lastMovilEmpresaCriteria) || !$this->lastMovilEmpresaCriteria->equals($criteria)) {
				$this->collMovilEmpresas = MovilEmpresaPeer::doSelectJoinEmpresa($criteria, $con, $join_behavior);
			}
		}
		$this->lastMovilEmpresaCriteria = $criteria;

		return $this->collMovilEmpresas;
	}

	/**
	 * Clears out the collChoferEmpresas collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addChoferEmpresas()
	 */
	public function clearChoferEmpresas()
	{
		$this->collChoferEmpresas = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collChoferEmpresas collection (array).
	 *
	 * By default this just sets the collChoferEmpresas collection to an empty array (like clearcollChoferEmpresas());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initChoferEmpresas()
	{
		$this->collChoferEmpresas = array();
	}

	/**
	 * Gets an array of ChoferEmpresa objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Usuario has previously been saved, it will retrieve
	 * related ChoferEmpresas from storage. If this Usuario is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array ChoferEmpresa[]
	 * @throws     PropelException
	 */
	public function getChoferEmpresas($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(UsuarioPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collChoferEmpresas === null) {
			if ($this->isNew()) {
			   $this->collChoferEmpresas = array();
			} else {

				$criteria->add(ChoferEmpresaPeer::USUARIO, $this->id);

				ChoferEmpresaPeer::addSelectColumns($criteria);
				$this->collChoferEmpresas = ChoferEmpresaPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(ChoferEmpresaPeer::USUARIO, $this->id);

				ChoferEmpresaPeer::addSelectColumns($criteria);
				if (!isset($this->lastChoferEmpresaCriteria) || !$this->lastChoferEmpresaCriteria->equals($criteria)) {
					$this->collChoferEmpresas = ChoferEmpresaPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastChoferEmpresaCriteria = $criteria;
		return $this->collChoferEmpresas;
	}

	/**
	 * Returns the number of related ChoferEmpresa objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related ChoferEmpresa objects.
	 * @throws     PropelException
	 */
	public function countChoferEmpresas(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(UsuarioPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collChoferEmpresas === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(ChoferEmpresaPeer::USUARIO, $this->id);

				$count = ChoferEmpresaPeer::doCount($criteria, false, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(ChoferEmpresaPeer::USUARIO, $this->id);

				if (!isset($this->lastChoferEmpresaCriteria) || !$this->lastChoferEmpresaCriteria->equals($criteria)) {
					$count = ChoferEmpresaPeer::doCount($criteria, false, $con);
				} else {
					$count = count($this->collChoferEmpresas);
				}
			} else {
				$count = count($this->collChoferEmpresas);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a ChoferEmpresa object to this object
	 * through the ChoferEmpresa foreign key attribute.
	 *
	 * @param      ChoferEmpresa $l ChoferEmpresa
	 * @return     void
	 * @throws     PropelException
	 */
	public function addChoferEmpresa(ChoferEmpresa $l)
	{
		if ($this->collChoferEmpresas === null) {
			$this->initChoferEmpresas();
		}
		if (!in_array($l, $this->collChoferEmpresas, true)) { // only add it if the **same** object is not already associated
			array_push($this->collChoferEmpresas, $l);
			$l->setUsuarioRelatedByUsuario($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Usuario is new, it will return
	 * an empty collection; or if this Usuario has previously
	 * been saved, it will retrieve related ChoferEmpresas from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Usuario.
	 */
	public function getChoferEmpresasJoinChofer($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(UsuarioPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collChoferEmpresas === null) {
			if ($this->isNew()) {
				$this->collChoferEmpresas = array();
			} else {

				$criteria->add(ChoferEmpresaPeer::USUARIO, $this->id);

				$this->collChoferEmpresas = ChoferEmpresaPeer::doSelectJoinChofer($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(ChoferEmpresaPeer::USUARIO, $this->id);

			if (!isset($this->lastChoferEmpresaCriteria) || !$this->lastChoferEmpresaCriteria->equals($criteria)) {
				$this->collChoferEmpresas = ChoferEmpresaPeer::doSelectJoinChofer($criteria, $con, $join_behavior);
			}
		}
		$this->lastChoferEmpresaCriteria = $criteria;

		return $this->collChoferEmpresas;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Usuario is new, it will return
	 * an empty collection; or if this Usuario has previously
	 * been saved, it will retrieve related ChoferEmpresas from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Usuario.
	 */
	public function getChoferEmpresasJoinEmpresa($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(UsuarioPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collChoferEmpresas === null) {
			if ($this->isNew()) {
				$this->collChoferEmpresas = array();
			} else {

				$criteria->add(ChoferEmpresaPeer::USUARIO, $this->id);

				$this->collChoferEmpresas = ChoferEmpresaPeer::doSelectJoinEmpresa($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(ChoferEmpresaPeer::USUARIO, $this->id);

			if (!isset($this->lastChoferEmpresaCriteria) || !$this->lastChoferEmpresaCriteria->equals($criteria)) {
				$this->collChoferEmpresas = ChoferEmpresaPeer::doSelectJoinEmpresa($criteria, $con, $join_behavior);
			}
		}
		$this->lastChoferEmpresaCriteria = $criteria;

		return $this->collChoferEmpresas;
	}

	/**
	 * Resets all collections of referencing foreign keys.
	 *
	 * This method is a user-space workaround for PHP's inability to garbage collect objects
	 * with circular references.  This is currently necessary when using Propel in certain
	 * daemon or large-volumne/high-memory operations.
	 *
	 * @param      boolean $deep Whether to also clear the references on all associated objects.
	 */
	public function clearAllReferences($deep = false)
	{
		if ($deep) {
			if ($this->collPropietarios) {
				foreach ((array) $this->collPropietarios as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collBancos) {
				foreach ((array) $this->collBancos as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collEmpresas) {
				foreach ((array) $this->collEmpresas as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collTallers) {
				foreach ((array) $this->collTallers as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collAseguradoras) {
				foreach ((array) $this->collAseguradoras as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collDespachos) {
				foreach ((array) $this->collDespachos as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collChofers) {
				foreach ((array) $this->collChofers as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collMovils) {
				foreach ((array) $this->collMovils as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collMultas) {
				foreach ((array) $this->collMultas as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collAccidentes) {
				foreach ((array) $this->collAccidentes as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collRecaudacions) {
				foreach ((array) $this->collRecaudacions as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collGastorecaudacions) {
				foreach ((array) $this->collGastorecaudacions as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collGastomovils) {
				foreach ((array) $this->collGastomovils as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collGastoempresas) {
				foreach ((array) $this->collGastoempresas as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collConfiguracions) {
				foreach ((array) $this->collConfiguracions as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collPagoaseguradoras) {
				foreach ((array) $this->collPagoaseguradoras as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collTrabajotallers) {
				foreach ((array) $this->collTrabajotallers as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collAdelantos) {
				foreach ((array) $this->collAdelantos as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collPagoadelantos) {
				foreach ((array) $this->collPagoadelantos as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collEmpresaPropietarios) {
				foreach ((array) $this->collEmpresaPropietarios as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collMovilAseguradoras) {
				foreach ((array) $this->collMovilAseguradoras as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collMovilEmpresas) {
				foreach ((array) $this->collMovilEmpresas as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collChoferEmpresas) {
				foreach ((array) $this->collChoferEmpresas as $o) {
					$o->clearAllReferences($deep);
				}
			}
		} // if ($deep)

		$this->collPropietarios = null;
		$this->collBancos = null;
		$this->collEmpresas = null;
		$this->collTallers = null;
		$this->collAseguradoras = null;
		$this->collDespachos = null;
		$this->collChofers = null;
		$this->collMovils = null;
		$this->collMultas = null;
		$this->collAccidentes = null;
		$this->collRecaudacions = null;
		$this->collGastorecaudacions = null;
		$this->collGastomovils = null;
		$this->collGastoempresas = null;
		$this->collConfiguracions = null;
		$this->collPagoaseguradoras = null;
		$this->collTrabajotallers = null;
		$this->collAdelantos = null;
		$this->collPagoadelantos = null;
		$this->collEmpresaPropietarios = null;
		$this->collMovilAseguradoras = null;
		$this->collMovilEmpresas = null;
		$this->collChoferEmpresas = null;
	}

	// symfony_behaviors behavior
	
	/**
	 * Calls methods defined via {@link sfMixer}.
	 */
	public function __call($method, $arguments)
	{
	  if (!$callable = sfMixer::getCallable('BaseUsuario:'.$method))
	  {
	    throw new sfException(sprintf('Call to undefined method BaseUsuario::%s', $method));
	  }
	
	  array_unshift($arguments, $this);
	
	  return call_user_func_array($callable, $arguments);
	}

} // BaseUsuario
